# WORK ORDER — B-6: KI-022 Validator Spike-Sensitivity Controlled Calibration

מסמך ביצוע ל-Claude Code · Stage A, Blocker B-6 · מאושר ע"י Roy (Project Owner)
מקור סמכות: `PREFLIGHT_B6.md` v1 (הכרעה: PROCEED — אופציה D, נובמבר-2022 תחילה, KI-008 מותנה) · `WORK_ORDER_PROTOCOL.md` v1.0 (FROZEN) · KI-022, D-071
**כפוף ל-`WORK_ORDER_PROTOCOL.md`** — Lessons Learned + DoD Checklist בדו"ח הסיום (§3.9/§4).

---

## 1. מטרה מדויקת ומה B-6 סוגר

**מטרה:** לברר — בניסוי-כיול מבוקר, לא שינוי אד-הוק — האם `Validator.spike_z_threshold=8.0` (`src/data/validator.py`) נכון, יתר-רגיש, או תת-רגיש, מול דאטה אמיתי. D-071 קבע במפורש שאסור לשנות את הערך ללא ניסוי כזה.

**מה B-6 סוגר בפועל, אם ההכרעה מובילה לשינוי-ערך או לאישוש:**
- KI-022 (`docs/KNOWN_ISSUES.md`) — closed, עם הפניה לניסוי-הכיול ותוצאתו.
- (מותנה, לפי §6 Commit 4 למטה) KI-008 (RA לא-רשום) — closed, אם נרשם RA חדש לפרמטר; נשאר פתוח אם 8.0 אושש.

**מה B-6 אינו סוגר:** שום סעיף RRR ישירות (KI-022 אינו סעיף-RRR פורמלי — ר' `PREFLIGHT_B6.md` §א.3).

## 2. Scope / Non-Goals

**Scope:**
- `Validator.spike_z_threshold` (ברירת-מחדל 8.0) — הפרמטר שהניסוח המקורי ב-KI-022/D-071 מתמקד בו.
- אם המתודולוגיה שתיבחר דורשת זאת: `spike_baseline_window` (500) כפרמטר-משנה נבחן.

**Non-Goals (חובה בכל תיעוד סוגר):**
1. **`gap_threshold` (5min) אינו בסקופ** — D-071 מצא ש-Gap-Detection כבר מוסבר היטב (17/18 rollover, 1 Thanksgiving), ללא אינדיקציה לרגישות-יתר. שינוי נפרד, לא כאן, אלא אם יתגלה בעיה.
2. **B-6 אינו נוגע ב-KI-010** (לוח-חדשות, B-7 נפרד).
3. **B-6 אינו סוגר RRR** — לא סעיף פורמלי, השפעה עקיפה בלבד.
4. **B-6 אינו נוגע ב-KI-023** (Holdout enforcement) — לא קשור.
5. **B-6 אינו משנה את `Validator` עצמו כברירת-מחדל אוטומטית** — כל שינוי-ערך עובר Decision Proposal + אישור Roy מפורש, בדיוק כמו RA-10/B-4 (D-074).

## 3. אילו נתונים אמיתיים נדרשים

- **בסיס:** נובמבר-2022 (כבר קיים ומאומת ב-Sandbox, D-070/D-072) — מספיק לשלב-1 (Grid-Sweep ראשוני).
- **הרחבה אפשרית (מאושרת עקרונית כמחקר-המשך, לא אוטומטית):** 2-3 חודשים נוספים ממשטרי-תנודתיות שונים מתוך 39 החודשים הקיימים על מחשב-הבית — תוצע רק אם התוצאה על נובמבר-2022 בלבד (שלבים A+B) עמומה; גם אז — ללא שינוי-ערך, ר' §6 Commit 3.
- **אין צורך בדאטה חדש/הורדה נוספת** — כל 39 החודשים כבר קיימים (Batch 1-7).

## 4. אילו ATs / Quality Gates מושפעים

- **AT:** אין AT פורמלי קיים ל-Spike-Detection (מאומת, `PREFLIGHT_B6.md` §א.6) — B-6 הוא ניסוי-כיול חדש, לא הרצת-AT-קיים-מול-דאטה-אמיתי (בשונה מ-B-5). אם יוחלט להוסיף AT חדש (למשל "AT-0.8 Spike calibration sanity") — זו הרחבת-Scope הדורשת אישור נפרד, לא מובנית-מאליה כאן.
- **Quality Gates:** לא מושפעים ישירות אלא אם ערך משתנה בפועל — אז `pytest -q`/`ruff`/`lint-imports` יורצו מחדש לוודא היעדר-רגרסיה (כמו B-4 Commit 3), בדיוק כמו כל שינוי-קונפיג.
- **RRR:** ללא השפעה ישירה על שום סעיף (ר' §1).

## 5. חלוקת ביצוע Sandbox מול מחשב-הבית

| שלב | היכן | תלוי-ב |
|---|---|---|
| שלב A — Grid-Sweep סביב 8.0 (נובמבר-2022) | **Sandbox** | קיים מקומית, `analyze_spikes.py` תומך כבר ב-`--spike-z-threshold`, ללא שינוי קוד |
| שלב B — הצלבה מול דוח-הספרד האמיתי (B-4) | **Sandbox** | פלט `run_full_spread_report.py` (B-4) כבר קיים ומתועד — לא נדרשת הרצה חוזרת |
| הרחבה ל-2-3 חודשים נוספים (רק אם A+B עמומים) | **מחשב-הבית** | `data/ticks/`/`data/raw/` המלאים אינם ב-Sandbox — הצעה נפרדת, לא אוטומטית |
| עדכון `config/parameters.yaml`/RA-29 (רק אם ערך משתנה) | **Sandbox** (קובץ-config בלבד, לא דאטה) | Decision Proposal מאושר קודם |

## 6. Commit Plan (מאושר, לפי אופציה D)

**Commit 1 — שלב A: Grid-Sweep (Sandbox, נובמבר-2022 האמיתי).**
- הרצת `scripts/diagnostics/analyze_spikes.py --spike-z-threshold <v>` (ללא שינוי קוד) על סדרת ערכים סביב 8.0 (למשל 5/6/7/8/9/10/12 — הסדרה המדויקת תוצג לפני הרצה).
- לכל ערך: ספירת-Flags, גודל-move-חציוני/p95, אחוז מכלל-הטיקים.
- ניתוח: יציבות בין ערכים שכנים (±1-2), האם 8.0 על "מישור" או "צוק" (עקרון-RA-08).
- **פלט Raw בלבד, ללא מסקנת-ערך-סופית בשלב זה.**

**Commit 2 — שלב B: הצלבה מול דוח-הספרד האמיתי של B-4 (Sandbox).**
- לכל ערך-Threshold משלב A: השוואת גודל-ה-Move של הספייקים המדוגללים מול הספרד-האמיתי/p95/שעות-הרחבת-ספרד (מ-`run_full_spread_report.py`, B-4/D-074 — פלט קיים, לא הרצה חוזרת).
- ניתוח: האם ה-Threshold-ים ה"יציבים" משלב A גם עומדים במבחן-הסבירות-הכלכלי (ספייק גדול משמעותית מרעש-ספרד רגיל).
- **עדיין ללא מסקנת-ערך-סופית.**

**Commit 3 — Decision Proposal (עצירה, לא Commit-קוד).**
- הצגת תוצאות A+B יחד: האם התמונה חד-משמעית (Sandbox/נובמבר-2022 מספיק) או עמומה (נדרשת הרחבת-דאטה, §5).
- אם חד-משמעית: הצעה מנומקת (ערך-נוכחי → ערך-מוצע-או-אישוש → השפעה), עצירה, אישור Roy מפורש — **בדיוק כמו B-4 Commit 2-3**.
- אם עמומה: הצעת-המשך-מחקר (הרחבת-דאטה למחשב-הבית) בלבד — **אין שינוי-ערך**.

**Commit 4 (מותנה בתוצאת Commit 3) — יישום ההכרעה.**
- אם שונה ערך: עדכון `config/parameters.yaml` (`Validator` עצמו נשאר קוד ללא שינוי — רק ה-Default המוזרק) + רישום RA-29 (`docs/RESEARCH_ASSUMPTIONS_V1.md`) + סגירת KI-008.
- אם אושש 8.0: אין שינוי-config — KI-008 נשאר פתוח (הכרעה 3, `PREFLIGHT_B6.md`).
- בכל מקרה: D-entry חדש (D-076) מתעד את ההכרעה.
- `pytest -q`/`ruff`/`lint-imports` מורצים מחדש אם config השתנה — לוודא היעדר-רגרסיה.

**Commit 5 — תיעוד סוגר** (`WORK_ORDER_B6_CLOSURE.md`, KI-022 closed, `docs/KNOWN_ISSUES.md` מעודכן).

## 7. השפעה צפויה על RRR

**אין השפעה ישירה על שום סעיף RRR.** KI-022 אינו סעיף פורמלי (ר' §1). RRR נשאר 6/9 GO ללא שינוי כתוצאה מ-B-6 עצמו — כל שינוי-סטטוס-RRR עתידי (אם בכלל) ידרוש הכרעה נפרדת, לא נגזר אוטומטית מסגירת KI-022.

## 8. תנאי עצירה מיידית

- כל ניסיון לשנות את `Validator`/הערך בפועל **לפני** Decision Proposal מפורש ואישור Roy → אסור, עצור.
- תוצאת Grid-Sweep עמומה/לא-חד-משמעית על נובמבר-2022 בלבד → אל תמציא מסקנה, דווח ובקש אישור להרחבת-דאטה (§5/§6 Commit 2).
- כל נגיעה (בטעות או לא) בחודשים מעבר לטווח-המאושר, או בחלון-Holdout (יולי-דצמבר 2025) → עצור מיידית.
- אם המתודולוגיה הנבחרת מתגלה כבלתי-ישימה בפועל (למשל: הצלבה מול Spread לא מניבה איתות ברור) → עצור, דווח, אל תעבור לחלופה אחרת בשקט.
- כל סטייה מסמך↔קוד נוספת שמתגלה → עצור, דווח, המתן.

## 9. דו"ח סיום (פורמט חובה — `WORK_ORDER_PROTOCOL.md` §3+§4)

זהה למבנה שהופעל ב-B-1..B-5 (12 סעיפים + DoD 9/9 + Lessons Learned + מדד-פרויקט), בתוספת ציון מפורש שארבעת ה-Non-Goals (§2) מופיעים בגוף הדוח, וטבלת-RRR מלאה (9 סעיפים) המראה במפורש **ללא שינוי** כתוצאה מ-B-6 (ר' §7). קובץ הסגירה: `WORK_ORDER_B6_CLOSURE.md`.
