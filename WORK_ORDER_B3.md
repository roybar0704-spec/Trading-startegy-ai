# WORK ORDER — B-3: Real-Data Diagnostic Validation Gate

מסמך ביצוע ל-Claude Code · Stage A, Blocker B-3 · מאושר ע"י Roy (Project Owner)
מקור סמכות: `PREFLIGHT_B3.md` v1 · `WORK_ORDER_PROTOCOL.md` v1.0 (FROZEN)
**כפוף ל-`WORK_ORDER_PROTOCOL.md`** — Lessons Learned + DoD Checklist בדו"ח הסיום (§3.9/§4).

---

## 0. הקשר ותפקיד

אתה Senior Quant Engineer בפרויקט מחקר XAUUSD דטרמיניסטי. עד כה (Phase 0–3, B-1, B-2) **כל** אימות המנוע — Structure/BOS/Sweep, FVG/iFVG, Setup Stream, Orchestrator, דטרמיניזם רב-זרועות, אינטראקציית מכסה — בוצע **אך ורק** מול Fixtures סינתטיים. `T3.4` (הריצה האמיתית הראשונה, מוגנת ע"י Research Readiness Review לפי `docs/RESEARCH_READINESS_REVIEW.md`) מעולם לא בוצעה. **RRR נשאר NO-GO** על 3 סעיפים (RA-10 לא כויל, 3 שנות דאטה חסרות, KI-010 לוח-חדשות אמיתי לא אותר) — B-3 **אינו** נועד לסגור אף אחד מהם.

B-3 הוא **Risk Reduction בלבד**: לחשוף עכשיו, בזול ובאופן מבוקר, את הסיכון שהמנוע כולו מעולם לא נגע בדאטה אמיתי — סיכון מאותו סוג שכבר התממש פעמיים (KI-015: באג M4 שנחשף רק בתרחיש-ריבוי מורכב; KI-022: רגישות-כיול Validator שנחשפה רק בחשיפה ראשונה לדאטה אמיתי).

**זה כל ה-Scope. שום דבר מעבר לזה.**

**שלוש שכבות נפרדות — אין לערבב:**
1. **זמינות דאטה** (Commit 0, Data Availability Gate) — תנאי-מקדים חיצוני. אספקת קבצים בלבד, לא חלק מ-Scope/AC/DoD של B-3 עצמו.
2. **Diagnostic Run** (Commit 1–3) — זה ה-Scope בפועל של B-3, וזה מה שנסגר/נפתח כ-Blocker.
3. **RRR Gate** (`docs/RESEARCH_READINESS_REVIEW.md`) — נשאר לגמרי מחוץ ל-B-3, במצבו (NO-GO), ללא תלות בתוצאת B-3.

## 1. כללים קשיחים

1. אין שינויי Scope. B-3 אינו סוגר RRR, אינו פותח Phase 4/T3.4, ואינו סוגר אף KI קיים אלא אם ההרצה עצמה מוכיחה כשל.
2. **הכלל המרכזי (בהשראת B-2 §1.2): אין שינוי קוד מראש.** קודם מריצים את ה-Diagnostic; **רק אם** הוא מוכיח כשל בפועל — זה הופך למשימת-תיקון בהיקף מינימלי. אם ההרצה עוברת נקי — זו תיעוד גרידא, אין נגיעה בקוד.
3. **אין שינויי Schema.** אם מתגלה שנדרש — עצור, דווח, המתן לאישור Roy.
4. **אין מסקנות מחקריות מההרצה** — לא Win Rate, לא PnL, לא R-multiple, לא כל מדד ביצועים. B-3 בודק **שהמנוע רץ נכון**, לא **מה התוצאה**. כל ניסוח שמרמז על מסקנה מחקרית (גם באגביות, בדוח הסיום) הוא הפרת Non-Goal, לא רק "לא רצוי".
5. **Non-Goals הבאים חייבים להופיע במפורש בכל תיעוד סוגר** (Work Order זה, Closure Report, D-entry): (א) KI-010 אינו מאומת — אין לוח חדשות אמיתי, ולכן Blackout/`news_cross` **לא נבדקים כלל** בהרצה הזו. (ב) KI-022 אינו נסגר — כל ממצא/אנומליה בהרצה חייב להיבדק תחילה מול חלונות-הזמן המדוגללים הידועים (D-071) לפני שהוא מסווג כבאג-מנוע חדש. (ג) אין מסקנות מחקריות (ר' סעיף 4 לעיל).
6. אין נגיעה בקבצים שכבר סגרו Blockers קודמים (Stability Rule, D-026) — שכפול מקומי עדיף על refactor.
7. כל בדיקה/הרצה מתועדת בפלט אמיתי בלבד.
8. קומיטים קטנים; חבילת ה-pytest הקיימת (**149 בדיקות**) נשארת ירוקה בסוף כל Commit, **ללא שינוי במספר** — סקריפט ה-Diagnostic של B-3 **אינו** נכנס ל-`pytest`/`tests/`, כי הוא תלוי בדאטה אמיתי (`data/`, מוחרג מ-git) שאינו זמין ב-clone טרי; הכללתו כ-AT רגיל הייתה שוברת רפרודוקביליות של הסוויטה הסטנדרטית. הוא נשאר סקריפט-אבחון עצמאי תחת `scripts/diagnostics/` — אותו דפוס בדיוק כמו `scripts/diagnostics/analyze_spikes.py` (KI-001).
9. עבודה על branch: `stage-a/b3-real-data-diagnostic-gate`.

## 2. החלטות מאושרות מראש (אין לפתוח מחדש)

- Scope = Real-Data Diagnostic Validation Gate, Risk Reduction בלבד. לא KI-010, לא KI-022, לא שום עבודת-כיול.
- אין שינוי בחוקי אסטרטגיה, אין שינוי ב-Entry Models מעבר לתיקון-מינימלי-במידת-הצורך שיאושר בנפרד.
- D הפנוי הבא לתיעוד סגירה: **D-072**.
- לא נדרש מספר AT חדש (ר' §1.8 — לא נכנס לסוויטת ה-Regression).

## 3. שלב 0 — Pre-flight

**בוצע.** `PREFLIGHT_B3.md` v1. הכרעה: **PROCEED בתנאי** — ר' §ד שם. תנאי מרכזי: Commit 0 (למטה) מוגדר **Data Availability Gate בלבד** — Roy אישר חלופה B: אספקה ישירה של קובצי נובמבר 2022 הקיימים. אין ניסיון-רשת ואין נגיעה ב-downloader כחלק מ-B-3.

## 4. יישום — סדר קומיטים

**Commit 0 — Data Availability Gate (תנאי-מקדים בלבד, לא חלק מה-Diagnostic עצמו, לא חלק מפתרון KI-001).**
- **מטרה יחידה:** להביא dataset אמיתי-ומאומת (נובמבר 2022) לסביבת-הביצוע שבה ירוץ ה-Diagnostic. שום דבר מעבר לזה.
- **מפורשות מחוץ ל-Scope:** Commit 0 **אינו** חלק מפתרון/סגירה מחודשת של KI-001. **אין** לבצע ניסיון-רשת אמיתי (`scripts/backfill_full_range.py` מול `datafeed.dukascopy.com`) ו**אין** לגעת ב-`DukascopyDownloader`/`BrowserLikeTransport` כחלק מ-B-3, ללא אישור נפרד ומפורש בעתיד — לא כלול באישור הנוכחי.
- **מימוש בפועל (חלופה B, אושרה ע"י Roy):** Roy מספק ישירות את קובצי ה-Parquet של נובמבר 2022 (המאומתים כבר, D-069/D-070) לסביבת-הביצוע. לאחר קבלתם: אימות שלמות בלבד — התאמת `row_count`/`data_version` למה שתועד ב-D-069 (3,641,776 ticks), ללא הורדה, ללא שינוי קוד.
- **הפרדת-סגירה מפורשת:** Commit 0 **אינו** נכלל בקריטריוני-הסגירה (§5) או ב-DoD של B-3 עצמו — הצלחתו היא תנאי-סף חיצוני לתחילת B-3, לא חלק ממה ש-`WORK_ORDER_B3_CLOSURE.md` מכריז עליו כ-Closed. אין לדווח על Commit 0 כ"חלק מהישג B-3" בדוח הסיום — רק כתנאי-מקדים שהתקיים.
- ללא דאטה אמיתי בפועל בסביבת-הביצוע (מסופק ומאומת) — B-3 לא יכול להמשיך ל-Commit 1.

**Commit 1 — Diagnostic Run (`scripts/diagnostics/run_b3_real_data_diagnostic.py`, Read-Only בעיצובו).**
- טוען את ה-Ticks האמיתיים של נובמבר 2022 (מ-`TickParquetStore`, ללא שינוי).
- בונה נרות 1M/5M/4H דרך `BarBuilder` הקיים (ללא שינוי).
- מריץ `build_orchestrator` עם קונפיג מינימלי (מומלץ: תיק בודד, M2×S_body — התיק הממוקד עתידית ב-T3.4 — לצמצום היקף; החלטת-מימוש סופית ב-Commit עצמו) ולוח-חדשות **ריק** (Non-Goal מוצהר, ר' §1.5א).
- רץ עד סיום, או תופס קריסה עם traceback מלא.
- כותב פלט-ריצה מובנה (בדומה ל-`analyze_spikes.py`/`backfill_full_range.py`): כמה Bars/Setups/Orders/Trades נוצרו, האם הריצה הסתיימה נקי, כל Exception שנתפס.
- **אין** ניתוח סטטיסטי/מחקרי של התוצאה (ר' §1.4) — רק "רץ תקין / נכשל, ואיך".

**Commit 2 (מותנה) — רק אם Commit 1 מוכיח כשל אמיתי.**
- STOP מיידי. דיווח מדויק: מה קרה, מה היה צפוי, traceback מלא, השערת Root Cause.
- המתנה לאישור Roy על **היקף** התיקון המינימלי המוצע — בדיוק כמו B-2 Commit 2 (`32439a6`→`2138441`).
- אם נמצאים כמה כשלים נפרדים — כל אחד מדווח ומאושר **בנפרד**, לא באישור-גורף אחד.
- רק אחרי אישור: תיקון + Commit נפרד לתיקון עצמו (לא מוערם על Commit ה-Diagnostic).

**Commit 3 — תיעוד סוגר.**
- `docs/DECISIONS_LOG.md`: D-072 — מתעד את תוצאת ההרצה במלואה ("רץ נקי, ללא ממצא" **או** "נמצא כשל X, תוקן בהיקף מינימלי Y לאחר אישור Roy"), כולל את שלושת ה-Non-Goals במפורש.
- `docs/KNOWN_ISSUES.md`: KI חדש נפתח **רק** אם נמצא ממצא שלא תוקן באותו Blocker (Follow-up מתועד עם עדיפות ויעד).
- **חובה בדוח הסיום:** טבלת סטטוס RRR מעודכנת (9 הסעיפים) — כדי להראות מפורשות שהיא נשארת NO-GO, ולמנוע פרשנות שגויה ש-B-3 = אור-ירוק ל-Phase 4.

## 5. קריטריוני סגירה (B-3 AC)

- הרצת `build_orchestrator` מול חודש נובמבר 2022 האמיתי בוצעה בפועל בסביבת-הביצוע (לא רק Fixture) — עד סיום, או עד STOP מתועד לפי §7.
- הריצה מתועדת במלואה (פלט מובנה + D-072).
- 149 בדיקות ה-pytest הקיימות נשארות ירוקות, **ללא תוספת למספר** (ר' §1.8).
- כל כשל טופל אך ורק לפי STOP → דיווח → אישור Roy → תיקון מינימלי.
- שלושת ה-Non-Goals (KI-010, KI-022, אין מסקנות מחקריות) מופיעים במפורש בדוח הסיום.
- טבלת סטטוס RRR מעודכנת מופיעה בדוח הסיום, ומאשרת שהיא נשארת NO-GO (אלא אם שונתה באופן עצמאי).
- **הכרזת "Closed" רק בכפוף ל-Definition of Done המלא (פרוטוקול §4) — קובץ סגירה `WORK_ORDER_B3_CLOSURE.md`, שם שונה במפורש מקובץ ההוראות הזה.**

## 6. Non-Goals (חובה בכל תיעוד סוגר — לא רשות)

1. **KI-010 אינו מאומת.** אין לוח חדשות אמיתי בהרצה זו — Blackout/`news_cross` לא נבדקים כלל. אין להסיק דבר על התנהגות המנוע סביב חדשות מהרצה זו.
2. **KI-022 אינו נסגר.** רגישות-כיול ה-Validator נותרת שאלה פתוחה. כל ממצא בהרצה נבדק תחילה מול חלונות-הזמן המדוגללים (D-071) לפני סיווג כבאג-מנוע.
3. **אין מסקנות מחקריות מההרצה** — לא Win Rate, לא PnL, לא כל מדד-ביצועים. זו בדיקת "רץ נכון", לא "מה התוצאה".
4. **B-3 אינו סוגר RRR ואינו פותח Phase 4/T3.4** — RRR נשאר במצבו (NO-GO) ללא תלות בתוצאת B-3, אלא אם עבודה נפרדת (למשל סגירת KI-010, כיול RA-10) תשנה זאת.

## 7. תנאי עצירה מיידית

- הקבצים שסופקו (Commit 0) אינם תואמים את מה שתועד ב-D-069 (`row_count`/`data_version` לא תואמים) — עצור, דווח, אל תנחש/תתקן בשקט; המתן להכרעת Roy.
- כל ניסיון (מכוון או לא) לבצע פעולת-רשת אמיתית או לגעת ב-Downloader/Transport במסגרת B-3 — אסור ללא אישור נפרד; אם עולה צורך כזה, עצור ודווח לפני כל ביצוע.
- ה-Diagnostic Run קורס או מתנהג בניגוד לציפייה → עצור, דווח מפורט (§4 Commit 2), המתן לאישור Roy על היקף תיקון.
- נדרש שינוי Schema → עצור.
- כל ניסוח בדוח שמרמז על מסקנה מחקרית (גם לא-מכוון) → עצור, נסח מחדש לפני שליחה.
- כל סטייה מסמך↔קוד נוספת שמתגלה → עצור, דווח, המתן.

## 8. דו"ח סיום (פורמט חובה — `WORK_ORDER_PROTOCOL.md` §3+§4)

זהה למבנה שהופעל בהצלחה ב-B-1/B-2 (11 סעיפים + DoD 9/9 + Lessons Learned + מדד-פרויקט), **בתוספת** טבלת סטטוס RRR מעודכנת (§5 לעיל) ואישור מפורש ששלושת ה-Non-Goals (§6) מופיעים בגוף הדוח. קובץ הסגירה: `WORK_ORDER_B3_CLOSURE.md`.
