# WORK ORDER — B-5: AT-0.* מול דאטה אמיתי + Quality Gates מחדש

מסמך ביצוע ל-Claude Code · Stage A, Blocker B-5 · מאושר ע"י Roy (Project Owner)
מקור סמכות: `PREFLIGHT_B5.md` v1 (הכרעה: PROCEED, שלוש הכרעות-Scope) · `WORK_ORDER_PROTOCOL.md` v1.0 (FROZEN) · `RRR_COMPLETION_PLAN.md`
**כפוף ל-`WORK_ORDER_PROTOCOL.md`** — Lessons Learned + DoD Checklist בדו"ח הסיום (§3.9/§4).

---

## 0. הקשר ותפקיד

אתה Senior Quant Engineer בפרויקט מחקר XAUUSD דטרמיניסטי. B-4 (RA-10) נסגר (`93f4b3c`) — RRR עומד על 5/9 GO.
B-5 סוגר **שני** סעיפי-RRR בבת-אחת: סעיף 4 (AT-0.\* מול דאטה אמיתי) וסעיף 5 (Quality Gates ירוקים מחדש).
**זה כל ה-Scope.** לא KI-010 (B-7), לא KI-022 (B-6).

## 1. הכרעות-Scope מאושרות (ר' `PREFLIGHT_B5.md` §ד — פירוט מלא, נימוקים, טבלאות)

1. **חלוקת-ביצוע:** Sandbox = AT-0.1, AT-0.4, AT-0.6 (GO אם ניתן עם הדאטה האמיתי הקיים, אחרת לתעד Fixture limitation במפורש), AT-0.7 (בדיקת-מנגנון בלבד). מחשב-הבית = AT-0.2 וכל הרצה שדורשת סביבת-דאטה מלאה.
2. **AT-0.7:** **לא** מבוצעת הפרדה-פיזית-אמיתית של Holdout (יולי-דצמבר 2025) במסגרת B-5. רק בדיקת-מנגנון (`separate_holdout`/`HoldoutGuard`, ללא שינוי קוד) על עותק-דאטה-אמיתי או Fixture. ההפרדה הפיזית המלאה נדחית במפורש ל-Work Order נפרד עתידי (Hardening/KI-023).
3. **AT-0.3/AT-0.5:** שניהם **לא Gate** ל-B-5 — AT-0.3 מסומן "Covered by D-071", AT-0.5 מסומן "N/A" (בדיקת-זמן בלבד, ללא תלות-בדאטה).

## 2. כללים קשיחים

1. **אין שינויי Scope.** B-5 = RRR סעיף 4 (AT-0.\*) + סעיף 5 (Quality Gates) בלבד. לא KI-010, לא KI-022 (מלבד שימוש-בעדות ב-D-071, ללא נגיעה ב-`Validator` עצמו/threshold שלו).
2. **אין שינויי קוד ב-`src/`.** B-5 מוסיף סקריפטי-אבחון/בדיקות-חדשות בלבד (בדפוס `run_full_spread_report.py`), לא נוגע ב-`Validator`/`BarBuilder`/`TickParquetStore`/`HoldoutGuard`/`DukascopyDownloader` הקיימים.
3. **אין שינויי Schema.** אם מתגלה שנדרש — עצור, דווח, המתן לאישור Roy.
4. **טווח-נתונים ל-AT-0.1/0.4/0.6:** נובמבר 2022 בלבד (הקובץ האמיתי-הקיים היחיד ב-Sandbox), אלא אם Roy מספק/מאשר טווח נוסף.
5. **AT-0.7 — בדיקת-מנגנון בלבד, על עותק של נובמבר-2022 האמיתי או Fixture. אסור לגעת ב-`data/ticks/` יולי-דצמבר 2025 או ב-`data/holdout/` בשום שלב.** הפרדה-פיזית-אמיתית של Holdout **אינה** בסקופ B-5 בשום צורה — נדחתה במפורש ל-Work Order נפרד עתידי (Hardening/KI-023).
6. **AT-0.2 — מחשב-הבית בלבד**, מותנה באימות מוקדם שקיים `data/raw/` תקין (§ב.1 ב-Pre-Flight).
7. **Quality Gates (`scripts/ci.sh`) — כאן ב-Sandbox**, ללא תלות בדאטה-אמיתי.
8. כל בדיקה/הרצה מתועדת בפלט אמיתי בלבד — Raw Paste, לא תמצית.
9. עבודה על branch: `stage-a/b5-at0-real-data`.

## 3. שלב 0 — Pre-flight

**בוצע.** `PREFLIGHT_B5.md` v1 (+עדכון להכרעה סופית). הכרעה: **PROCEED** — שלוש הכרעות-Scope (§1 לעיל) אושרו במפורש ע"י Roy, 2026-08-07.

## 4. יישום — סדר קומיטים

**Commit 1 — סקריפטים חדשים ל-AT-0.1/0.4/0.6 (+ בדיקת-מנגנון AT-0.7) מול דאטה אמיתי (Sandbox, נובמבר 2022).**
- `scripts/diagnostics/run_at0_real_data_checks.py` (או קובץ נפרד לכל AT — להכריע בזמן-כתיבה): טוען `data/ticks/XAUUSD/2022/11.parquet` דרך `TickParquetStore.read_month` (ללא שינוי), מריץ את אותן בדיקות-Assertion שה-Fixture-tests מריצות (integrity/DST/bar-tick) — אך מול הדאטה האמיתי.
- AT-0.6: אם ניתן לממש במלואו מול הדאטה האמיתי הקיים — GO; אם מתגלה מגבלה טכנית שמונעת זאת — לא לדלג בשקט, לתעד במפורש כ-Fixture limitation בדוח-הסיום (ולא להסוות כ-"עבר").
- AT-0.7: סקריפט/בדיקה נפרדת שמריצה את `separate_holdout`/`HoldoutGuard` הקיימים (ללא שינוי קוד) על עותק של נובמבר-2022 האמיתי (או Fixture, אם עותק אמיתי לא מעשי טכנית) — **לא** נוגעת ב-`data/ticks/` בכלל, רק בעותק/Store זמני.
- AT-0.3: **לא** נכתב סקריפט חדש — קומיט זה כולל רק הפניה מתועדת ל-D-071 כעדות.
- Read-Only בעיצובו, לא נוגע ב-`src/`.

**Commit 2 — AT-0.2 Evidence (מחשב-הבית).**
- Roy מריץ בדיקת-מטמון (`data/raw/`) + סקריפט-immutability חדש (יאופיין במדויק אחרי אימות §ב.1 ב-Pre-Flight — קודם לוודא ש-`data/raw/` קיים ותקין).
- Claude מאמת Evidence גולמי.

**Commit 3 — Quality Gates מחדש (Sandbox).**
- `bash scripts/ci.sh` (או שקילותו) — פלט מלא, ששת השערים.
- כל כשל מדווח, לא מוסתר; אם יש כשל — Decision Proposal לפני תיקון (לא תיקון-אד-הוק בשקט).

**Commit 4 — תיעוד סוגר.**
- `docs/RESEARCH_READINESS_REVIEW.md`: סעיפים 4+5 מסומנים GO (או NO-GO מתועד אם Quality Gates נכשל), עם ציון מפורש ש-AT-0.3/0.5 אינם Gate (Covered by D-071 / N/A) ושהפרדת-Holdout הפיזית **לא** בוצעה (מופנית ל-Follow-up KI-023).
- D-entry (D-075) מתעד את כל תוצאות B-5 (אילו ATs רצו איפה, תוצאות Quality Gates, ההכרעה שלא לבצע הפרדה-פיזית).
- טבלת-RRR מלאה (9 סעיפים) בדוח-הסיום.

## 5. קריטריוני סגירה (B-5 AC)

- AT-0.1/0.4 רצו בפועל מול נובמבר-2022 האמיתי, לא Fixture.
- AT-0.6 רץ בפועל מול נובמבר-2022 האמיתי, **או** מגבלה טכנית מתועדת במפורש (לא הסתרה שקטה).
- AT-0.7 (בדיקת-מנגנון בלבד) רצה בפועל על עותק/Fixture — **לא** בוצעה הפרדה-פיזית-אמיתית.
- AT-0.2 רץ בפועל מול `data/raw/` האמיתי (מחשב-הבית) — או Decision Proposal מתועד אם `data/raw/` לא קיים/לא ניתן-לבדיקה.
- AT-0.3/AT-0.5 מתועדים במפורש כ"Covered by D-071"/"N/A" — לא Gate, לא נדרשת הרצה.
- `scripts/ci.sh` רץ במלואו, פלט מלא מתועד, ששת השערים ירוקים או כשל מתועד עם Decision Proposal.
- `docs/RESEARCH_READINESS_REVIEW.md` סעיפים 4+5 מעודכנים.
- **הכרזת "Closed" רק בכפוף ל-DoD המלא (`WORK_ORDER_PROTOCOL.md` §4)** — קובץ סגירה: `WORK_ORDER_B5_CLOSURE.md`.

## 6. Non-Goals (חובה בכל תיעוד סוגר)

1. **B-5 אינו סוגר RRR במלואו** — סעיפים 7 (KI-010) ו-9 (שיפוט-פתוח) נשארים.
2. **B-5 אינו נוגע ב-`Validator`/`spike_z_threshold`** — KI-022 נשאר עבודה נפרדת (B-6). שימוש ב-D-071 כעדות ≠ סגירת KI-022.
3. **B-5 אינו מבצע הפרדה-פיזית-אמיתית של Holdout ואינו בונה מנגנון-אכיפה מרכזי (KI-023)** — רק בדיקת-מנגנון על עותק/Fixture. ההפרדה הפיזית המלאה (יולי-דצמבר 2025) והפתרון הארכיטקטוני ל-KI-023 נשארים Work Order נפרד עתידי (Hardening).
4. **B-5 אינו נוגע ב-KI-010.**

## 7. תנאי עצירה מיידית

- כל תוצאת-בדיקה שאינה תואמת למצופה (למשל AT-0.1 real-data נכשל) → עצור, דווח, אל תתקן בשקט.
- `scripts/ci.sh` נכשל בשער כלשהו → עצור, דווח כ-Decision Proposal (תקן/דחה-ותעד ב-KNOWN_ISSUES), אל תמשיך להצהיר GO.
- `data/raw/` לא קיים/לא תקין על מחשב-הבית → עצור, דווח כאן, אל תמציא-חלופה לבד.
- כל ניסיון (בטעות או לא) לגעת בפועל ב-`data/ticks/` יולי-דצמבר 2025 או ב-`data/holdout/` בהקשר של AT-0.7 → עצור מיידית — B-5 מוגבל לבדיקת-מנגנון על עותק/Fixture בלבד (§1.2).
- כל ניסיון (בטעות או לא) לגעת בחודשים מחוץ לטווח-המאושר (נובמבר 2022 לבדיקות ה-Sandbox) → עצור מיידית.
- כל סטייה מסמך↔קוד נוספת שמתגלה → עצור, דווח, המתן.

## 8. דו"ח סיום (פורמט חובה — `WORK_ORDER_PROTOCOL.md` §3+§4)

זהה למבנה שהופעל ב-B-1..B-4 (עד 12 סעיפים + DoD 9/9 + Lessons Learned + מדד-פרויקט), בתוספת טבלת-RRR מעודכנת (9 סעיפים) ואישור מפורש שארבעת ה-Non-Goals (§6) מופיעים בגוף הדוח. קובץ הסגירה: `WORK_ORDER_B5_CLOSURE.md`.
