/data/*
# B-1/D-067/C3: keep the run registry directory in git (runs.jsonl itself stays
# untracked -- it's a local, append-only log, not a versioned artifact).
!/data/registry
/data/registry/*
!/data/registry/.gitkeep
# B-7/KI-010: real economic-calendar CSV -- small, manually-compiled research
# artifact with no automated re-fetch pipeline (source blocked from Sandbox),
# feeds CalendarEngine/FillSimulator decisions directly -- tracked for
# reproducibility, unlike bulk tick data above.
!/data/news
benchmarks/_scratch_ticks/
*.parquet
*.duckdb
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.venv/
*.egg-info/
dist/
build/
.env

*_dump.txt
full_gaps_export.json
