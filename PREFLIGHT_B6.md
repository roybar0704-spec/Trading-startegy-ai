# PREFLIGHT_B6.md — KI-022: Validator Spike-Sensitivity Controlled Calibration

**Blocker:** B-6 — KI-022 (`Validator.spike_z_threshold` sensitivity, `src/data/validator.py`).
**מבוצע לפי:** `WORK_ORDER_PROTOCOL.md` §1. **מסמך תכנון בלבד — לא בוצע שום שינוי קוד, לא בוצע Commit.**

---

## א. הנחות מאומתות (הנחה → ראיה)

1. **`Validator` (`src/data/validator.py`) קיים ומוכן, ללא צורך בשינוי-מבנה.** מנגנון-הספייק: `dmid = |mid_t - mid_{t-1}|`, `baseline_std = rolling_std(dmid, window=500, min_samples=30)`, ספייק מדוגלל כש-`dmid > spike_z_threshold(8.0) * baseline_std`. `gap_threshold=5min` הוא מנגנון **נפרד** (Gaps, לא Spikes).
   ראיה: `src/data/validator.py:16-17, 90-146`.

2. **KI-022 (`docs/KNOWN_ISSUES.md`) ו-D-071 (`docs/DECISIONS_LOG.md`) כבר מגדירים את הבעיה במדויק:** אימות ראשון מול דאטה אמיתי (נובמבר 2022) חשף 2,998 flagged spikes בחודש בודד; גודל-move חציוני של הספייקים המדוגללים (0.2305) קטן מהספרד הטיפוסי (~0.40-0.45) — רמז לרגישות-יתר אפשרית. **הוחלט מפורשות ב-D-071: "לא לשנות threshold/window/לוגיקת-סיווג כעת ללא ניסוי-כיול מבוקר... נדרש Work Order ייעודי."**
   ראיה: `docs/KNOWN_ISSUES.md` KI-022, `docs/DECISIONS_LOG.md` D-071.

3. **KI-022 אינו סעיף פורמלי ב-RRR** (`docs/RESEARCH_READINESS_REVIEW.md` 9 הסעיפים — KI-022 לא מוזכר באף אחד מהם). ההשפעה על ה-RRR היא **עקיפה בלבד** (מחזק ביטחון מאחורי סעיף 4, שכבר מסומן GO-עם-הבהרות ב-B-5/D-075) — **B-6 אינו צפוי להפוך שום סעיף RRR בין GO ל-NO-GO**.
   ראיה: `RRR_COMPLETION_PLAN.md` §0 (ניתוח מפורש שכבר בוצע ואושר ע"י Roy לפני B-4); קריאה ישירה של `docs/RESEARCH_READINESS_REVIEW.md`.

4. **`analyze_spikes.py` (הסקריפט הקיים, ללא שינוי) כבר תומך ב-`--spike-z-threshold` ו-`--spike-baseline-window` כפרמטרי-CLI** — תשתית מוכנה ל-Grid-Sweep על ערכים שונים, ללא צורך לכתוב מהתחלה.
   ראיה: `scripts/diagnostics/analyze_spikes.py:74-75`.

5. **`spike_z_threshold`/`gap_threshold`/`spike_baseline_window` אינם רשומים כ-Research Assumption כלל** (KI-008, פתוח, `docs/KNOWN_ISSUES.md`) — מאומת: חיפוש ב-`docs/RESEARCH_ASSUMPTIONS_V1.md` לא מחזיר "spike"/"gap_threshold"/"spike_z". אם B-6 יגיע להכרעת-ערך, יש לרשום RA חדש (הפנוי הבא: **RA-29**, ר' §ד) לפי נוהל-העדכון הרגיל — לא שינוי-קוד שקט.
   ראיה: `docs/KNOWN_ISSUES.md` KI-008; חיפוש-טקסט ב-`docs/RESEARCH_ASSUMPTIONS_V1.md`.

6. **אין שום Acceptance Test פורמלי לזיהוי-Spikes** (בשונה מ-AT-0.3, שעוסק אך ורק ב-Gap Detection). מאומת: חיפוש "spike" ב-`docs/ACCEPTANCE_TESTS.md` לא מחזיר תוצאה. B-6 הוא **ניסוי-כיול חדש**, לא "הרצת AT קיים מול דאטה אמיתי" (בניגוד למבנה B-5).
   ראיה: `docs/ACCEPTANCE_TESTS.md` (חיפוש-טקסט).

7. **דאטה אמיתי זמין:** נובמבר-2022 קיים מקומית ב-Sandbox (D-070/D-072, כבר בשימוש ב-B-5) — מאפשר Grid-Sweep ראשוני מיידי. 38 החודשים הנותרים קיימים רק על מחשב-הבית (D-073 §א.5 preciden, `data/ticks/`/`data/raw/` gitignored).
   ראיה: אימות-בפועל (`ls`/`sha256sum` על `data/ticks/XAUUSD/2022/11.parquet`, בוצע קודם ב-B-5).

8. **RA-08 (`docs/RESEARCH_ASSUMPTIONS_V1.md`) מספק תקדים מתודולוגי קיים ומאושר בפרויקט:** "Sensitivity: הפרעה ±20% לשכני Grid" — עיקרון להבחין "רמה" (Threshold יציב) מ-"שיא בודד" (ערך שרירותי-מקרי). רלוונטי-בעקיפין כתקדים-מתודולוגי ל-B-6, גם אם לא הוצהר במפורש כחל על כיול-Validator.
   ראיה: `docs/RESEARCH_ASSUMPTIONS_V1.md` RA-08.

## ב. הנחות שלא ניתן לאמת (סיבה, השפעה, טיפול)

1. **איזו מתודולוגיית-כיול נכונה** — D-071/KI-022 מצהירים במפורש שזו "החלטה שצריכה Pre-Flight ייעודי משלה" וטרם הוגדרה בשום מסמך.
   **סיבה שלא ניתן לאמת:** אין תקדים מלא בפרויקט לכיול-סף-סטטיסטי מהסוג הזה (RA-08 קרוב אך לא זהה).
   **השפעה:** ליבת ה-Blocker כולה תלויה בזה.
   **טיפול:** מוצג כ-Decision Proposal מפורש ב-§ד — **לא מוכרע כאן חד-צדדית.**

2. **האם חודש-דוגמה אחד (נובמבר 2022) מספיק לכיול אמין, או שנדרשים כמה חודשים ממשטרי-תנודתיות שונים.**
   **סיבה שלא ניתן לאמת:** נובמבר 2022 כבר שימש לגילוי-הבעיה (D-071) — כיול על אותו חודש בלבד עלול "להתאים יתר-על-המידה" לאותו חודש ספציפי.
   **השפעה:** איכות-הכיול; לא סיכון-נתונים.
   **טיפול:** מוצג כשאלת-Scope ב-§ד, עם המלצה לגישה הדרגתית (Sandbox קודם, הרחבה למחשב-הבית אם נדרש).

## ג. סטיות תיעוד↔קוד שהתגלו

**אין סטיות חדשות.** KI-008 (הסתייגות RA לא-רשומה) כבר תועד מראש (Phase 2) — לא ממצא חדש, אך רלוונטי-ישירות ל-B-6 (ר' §א.5).

## ד. הכרעה

**PROCEED — כל שאלות-הScope הוכרעו במפורש ע"י Roy (2026-08-07).**

### הכרעה 1 — מתודולוגיה: אופציה D (שילוב A+B), דו-שלבית

**שלב A — Grid-Sweep סביב `spike_z_threshold=8.0`:** להריץ `analyze_spikes.py --spike-z-threshold <ערכים>` (הפרמטר כבר נתמך, ללא שינוי קוד) על סדרת ערכים סביב 8.0, ולנתח: (i) יציבות-התוצאות (האם ספירת-Flags/גודל-move-חציוני משתנים בהדרגה או בקפיצה חדה סביב ערכים שכנים), (ii) רגישות לשינוי-קטן (±1-2 סביב 8.0, בהשראת עקרון-±20%-של-RA-08), (iii) האם 8.0 עצמו יושב באזור-יציב ("מישור") או על "צוק".

**שלב B — הצלבה מול דוח-הספרד האמיתי של B-4:** להשוות את גודל-ה-Move של ספייקים מדוגללים (בכל Threshold מ-שלב A) מול ההתפלגות-האמיתית שכבר נמדדה (`scripts/diagnostics/run_full_spread_report.py`, B-4/D-074) — ספרד-אמיתי, p95, ושעות-הרחבת-ספרד — כדי לבדוק התאמה בין Flags-של-Validator לתנאי-שוק אמיתיים (לא רק יציבות-סטטיסטית מבודדת).

שני השלבים משלימים זה את זה — לא תחליף אחד לשני.

### הכרעה 2 — דאטה: הדרגתי, Sandbox תחילה

**שלב ראשון (חובה): Sandbox בלבד, מול נובמבר-2022 האמיתי הקיים.** אם התוצאה (שלבים A+B) חד-משמעית — מספיק. **אם עמומה — אין שינוי-ערך.** יוצג מחקר-המשך מוצע (הרחבת-דאטה ל-2-3 חודשים נוספים, מחשב-הבית) כהצעה נפרדת, לא מבוצע אוטומטית.

### הכרעה 3 — KI-008 (RA לא-רשום): מותנה בתוצאה

- **אם threshold משתנה בפועל** → RA חדש (RA-29) נרשם + KI-008 נסגר, לפי נוהל-העדכון (`RESEARCH_ASSUMPTIONS_V1.md:59-64`).
- **אם אין שינוי (8.0 אושש)** → KI-008 **נשאר פתוח** — לא נסגר "בחינם" רק כי בוצע ניסוי; אישוש-ערך-קיים אינו רישום-RA.

### הכרעה 4 — אין שינוי קוד/config לפני Evidence → Decision Proposal → אישור מפורש

בדיוק כמו B-4 (RA-10): שום עדכון בפועל ל-`config/parameters.yaml`/`src/data/validator.py` לפני שה-Evidence (שלבים A+B) הוצג, Decision Proposal מלא (ערך-נוכחי → ערך-מוצע-או-אישוש → השפעה) הוצג, ואישור Roy המפורש התקבל.

**שאר ה-Pre-Flight נקי — אין סתירה מהותית שנותרה. ממשיך ל-`WORK_ORDER_B6.md` (גרסה סופית).**
