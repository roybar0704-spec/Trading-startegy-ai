# PREFLIGHT_B3 — Pre-Flight Review, Blocker B-3 (Real-Data Diagnostic Validation Gate)

**גרסה 1.** מסמך חובה לפני קוד (`WORK_ORDER_PROTOCOL.md` §1) · Stage A, Blocker B-3 · **תוצר: הכרעה PROCEED/STOP בלבד. אין שינויי קוד במסמך זה.**

---

## Decision מאושר — לרישום

**Objective:** לחשוף — בסביבה מבוקרת, לפני הריצה האמיתית הראשונה המוגנת (T3.4) — האם המנוע (Structure→FVG→iFVG→Setup Stream→Orchestrator), שאומת עד כה **אך ורק** מול Fixtures סינתטיים, מתפקד כראוי כשמוזן דאטה אמיתי.

**Scope:** הרצת `build_orchestrator` מול חודש נובמבר 2022 האמיתי (היחיד שאי-פעם אומת מקצה-לקצה, D-069/D-070); תיעוד מלא של התוצאה; ללא מסקנות מחקריות.

**Non-goals (אושרו במפורש ע"י Roy — חובה בכל תיעוד):** KI-010 (לוח חדשות אמיתי) אינו מאומת בהרצה זו; KI-022 (כיול Validator) אינו נסגר; אין מסקנות מחקריות מההרצה; B-3 אינו סוגר RRR ואינו פותח Phase 4/T3.4.

**Acceptance Criteria:** (1) ההרצה בוצעה בפועל בסביבת-הביצוע מול נתוני נובמבר 2022 האמיתיים. (2) הריצה מתועדת במלואה. (3) 149 בדיקות pytest קיימות נשארות ירוקות ללא תוספת למספר. (4) כל כשל טופל אך ורק לפי STOP→דיווח→אישור Roy→תיקון מינימלי.

**DoD:** Pre-Flight מאושר לפני קוד · Work Order נכתב · הרצה בוצעה ותועדה · Self-Review אדוורסרי מבוצע · Closure Report מלא (כולל טבלת RRR מעודכנת) לפני הכרזת Closed.

**שלוש שכבות נפרדות (אושר ע"י Roy — אין לערבב בשום תיעוד סוגר):** (1) זמינות דאטה — Commit 0, Data Availability Gate, תנאי-מקדים חיצוני בלבד, לא חלק מ-AC/DoD של B-3. (2) Diagnostic Run — ה-Scope בפועל של B-3. (3) RRR Gate — נשאר לחלוטין מחוץ ל-B-3, במצבו הנוכחי (NO-GO), ללא תלות בתוצאה כאן.

---

## (א) הנחות מאומתות — הנחה → ראיה

| # | הנחה | ראיה |
|---|---|---|
| 1 | `scripts/backfill_full_range.py` מוכיח בעבר יצירת דאטה אמיתי תקין לנובמבר 2022 | D-069: `months_succeeded=1 months_failed=0 transport_calls=720 429s=0 errors=0`, 3,641,776 ticks, Parquet נכתב ואומת |
| 2 | `build_orchestrator`/`Orchestrator.run()` מוכחים לרוץ קצה-לקצה מול Fixtures סינתטיים, כולל pipeline 4H גולמי ללא זריעה ותרחישי multi-arm | KI-020/D-064 (`tests/test_full_pipeline_from_raw_4h_bars.py`), D-062/D-069 (`tests/test_full_pipeline_9_arms.py`, `tests/test_at3_15/16`) |
| 3 | אין מחסום ארכיטקטוני להזרקת דאטה אמיתי דרך אותו ממשק (D-037: עצמאות ממקור-נתונים) | `CalendarEngine`/`Orchestrator` מקבלים `Tick`/`Bar`/`NewsEvent` מוזרקים בלבד; אין ענף-קוד שמבחין מקור |
| 4 | 149 בדיקות pytest ירוקות על ה-branch הנוכחי (`24dc25f`, אחרי מיזוג B-2) | הורץ ואומת ישירות בסשן זה — `149 passed in 6.01s` |
| 5 | **בסביבת-הביצוע הנוכחית (session זה) אין דאטה אמיתית מקומית בכלל** | נבדק ישירות: `find data -maxdepth 4` → רק `data/registry/.gitkeep` ו-`data/backfill_state/logs` ריק (מה-dry-run הקודם); `/data/*` מוחרג ב-`.gitignore`; ה-container משוכפל מ-git בכל session חדש |

## (ב) הנחות שלא ניתן לאמת

1. **האם הקבצים שיסופקו ע"י Roy (חלופה B) יהיו זהים-בפועל למה שתועד ב-D-069/D-070.** לא ניתן לאמת מראש בלי לקבל אותם בפועל ולבדוק `row_count`/טווח-מחירים מול התיעוד. **אופן הטיפול:** בדיקת-שלמות מפורשת ב-Commit 0 עצמו (ר' §7 ב-`WORK_ORDER_B3.md`) — לא הנחה, בדיקה בזמן ריצה.
2. **כמה מתוך 9 התיקים להריץ ב-Diagnostic** — כל 9, או תיק בודד? לא חוסם-Preflight; המלצה (WORK_ORDER_B3.md §4 Commit 1): תיק בודד (M2×S_body, התיק הממוקד עתידית ב-T3.4) לצמצום היקף — הכרעה סופית ב-Commit עצמו.

**הערה:** שאלת-הרשת/ה-403 שהועלתה בגרסה קודמת של מסמך זה **הוסרה מה-Scope** — Roy הכריע במפורש (חלופה B) שהדאטה יסופק ישירות, וש-Commit 0 אינו חלק מפתרון KI-001 ואינו כולל שום ניסיון-רשת. השאלה נשארת פתוחה **מחוץ ל-B-3** (רלוונטית למועמד-Backfill המלא שהוצג בניתוח הקודם), לא כאן.

## (ג) סטיות תיעוד↔קוד שהתגלו

**אין סתירת קוד↔SPEC.** הפער היחיד שנמצא הוא **תשתיתי-סביבתי, לא ארכיטקטוני:** ה-Acceptance כפי שנוסח על-ידי Roy ("הרצה מול חודש הדאטה האמיתי **הקיים**") מניח קיום-דאטה בפועל, אך זה לא קיים בסביבת-הביצוע שבה ירוץ B-3 בפועל (ר' א.5). זו אינה סתירה מהותית בין תיעוד לקוד — אלא פער בין הנחת-התכנון (בשיחה) לבין מצב הקבצים בפועל, שהתגלה רק בבדיקה ישירה של קובצי המערכת.

## (ד) הכרעה

**PROCEED.**

אין סתירה ארכיטקטונית, אין הנחה קריטית שסותרת את ה-Scope עצמו. הפער התשתיתי שנמצא (א.5 — אין דאטה מקומי בסביבת-הביצוע) נסגר בהכרעת Roy: **Commit 0 מוגדר Data Availability Gate בלבד — חלופה B** — Roy מספק ישירות את קובצי ה-Parquet הקיימים של נובמבר 2022 (המאומתים כבר, D-069/D-070). אין ניסיון-רשת, אין נגיעה ב-Downloader/Transport, אין קשר לפתרון-מחדש של KI-001 — Commit 0 מצטמצם לקבלת הקבצים + בדיקת-שלמות מול D-069 (`row_count`/טווח-מחירים) בלבד.

אין להתחיל Commit 1 (ה-Diagnostic עצמו) לפני שהקבצים סופקו ואומתו כתואמים ל-D-069.
