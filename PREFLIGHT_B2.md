# PREFLIGHT_B2 — Pre-Flight Review, Blocker B-2 (Multi-Arm Robustness)

**גרסה 2 — Scope מצומצם בהחלטת Roy, בעקבות ממצא B2-C1 (גרסה 1 של מסמך זה).**
מסמך חובה לפני קוד (`WORK_ORDER_PROTOCOL.md` §1) · Stage A, Blocker B-2 · **תוצר: הכרעה PROCEED/STOP בלבד. אין שינויי קוד במסמך זה.**

---

## Decision מאושר (מעודכן) — לרישום

**Objective:** להוכיח שהרצת Multi-Arm מלאה נשארת דטרמיניסטית ויציבה גם בתרחישים המורכבים שלא כוסו עדיין.

**Scope:**
1. דטרמיניזם דו-ריצתי על תרחיש Multi-Arm מלא (אותה קלטת Fixtures, שתי הרצות נפרדות, Output/Journals זהים).
2. בדיקת אינטראקציית עומס: יותר מ-2 Setups שמגיעים ל-ARMED על אותו תיק/יום; לוודא שהתנהגות quota/cap מוגדרת, צפויה ומתועדת.
3. יצירת Acceptance Test חדש (AT-3.15) רק עבור הפערים שלא מכוסים.

**Non-goals:** אין שינוי בחוקי האסטרטגיה · אין שינוי ב-Entry Models · אין שינוי ב-Orchestrator מעבר למה שנדרש כדי לתקן כשל מוכח · אין טיפול ב-KI אחרים.

**Acceptance Criteria:** (1) שתי הרצות Multi-Arm זהות מפיקות תוצאה זהה. (2) אין coupling בין זרועות בתרחישי עומס. (3) התנהגות quota/cap מוכחת בטסט. (4) כל שינוי מתועד ב-Decision Log/Known Issues לפי הצורך.

**DoD:** Pre-Flight מאושר לפני קוד · Work Order נכתב · כל בדיקות הקבלה ירוקות · Self-Review אדוורסרי מבוצע · Closure Report מלא לפני הכרזת Closed.

---

## (א) הנחות מאומתות — מה כבר מוכח מול מה שעדיין חסר

### מה כבר מוכח (מחוץ ל-Scope של B-2 — לא לשכפל)

| # | מה מוכח | ראיה |
|---|---|---|
| 1 | 9 זרועות רצות יחד באותה ריצה | `tests/test_full_pipeline_9_arms.py::test_all_three_entry_models_fill_through_the_real_orchestrator` |
| 2 | בידוד מלא: 3 ממלאות, 6 נשארות ללא שינוי (`realized_equity`/`fills_today`) | `tests/test_full_pipeline_9_arms.py::test_nine_arms_stay_isolated_when_only_some_fill` (D-062) |
| 3 | `RunResult.fills`/`orders_cancelled` נספרים נכון | `tests/test_full_pipeline_9_arms.py::test_run_result_fills_and_cancelled_counters_are_wired` |

### מה עדיין חסר — זה ה-Scope המצומצם של B-2

| # | הפער | ראיה שהוא באמת פתוח |
|---|---|---|
| 1 | דטרמיניזם דו-ריצתי **על תרחיש-הריבוי הספציפי** (3 ממלאות, 6 לא) | כל פונקציות הטסט ב-`test_full_pipeline_9_arms.py` קוראות ל-`_scenario()` + `.run()` **פעם אחת בלבד** — אין השוואת שתי-ריצות בקובץ הזה. AT-3.14 (B-1) הוכיח דטרמיניזם, אבל על תרחיש שונה (fixture של D-064, שם זרוע אחת בלבד ממלאת) |
| 2 | אינטראקציית מכסה: **>2 Setups** ל-ARMED על אותו תיק/יום | מתועד במפורש כפער-פתוח בתוך KI-015 עצמו ("עדיין לא מכוסה"); אומת שוב עכשיו — חיפוש מקיף בכל `tests/*.py` לא מצא שילוב ARMED+quota מעבר לתרחיש דו-תיקי (AT-3.11, D-052) |
| 3 | רישום פורמלי כ-Acceptance Test | נבדק מול `docs/ACCEPTANCE_TESTS.md` במלואו — המספר הפנוי הבא: **AT-3.15** |

**הערה טכנית לשלב ה-Work Order (לא חוסמת):** D-047/D-052 כבר מגדירים מנגנון אכיפת-מכסה קיים (פר-תיק, ברגע ARM לא רק ברגע Fill) — כלומר יש קוד-אכיפה שטרם קיבל טסט תואם ל->2 Setups. זה מתיישב בדיוק עם ה-Non-goal שהגדרת: כותבים את הטסט קודם; אם הוא עובר — זה תיעוד גרידא (אין כשל, אין שינוי קוד); אם נכשל — זה "כשל מוכח" שנכנס להיקף התיקון כפי שה-Non-goal מתיר.

## (ב) הנחות שלא ניתן לאמת

- האם קיימים תרחישי-ריבוי נוספים (מעבר לזה שכבר קיים ב-`test_full_pipeline_9_arms.py`) עם סיכון-קופלינג דומה — לא ניתן להוכיח שלילה מוחלטת; ה-Scope מכסה את התרחישים הספציפיים שזוהו, לא "כל תרחיש אפשרי".
- אחד או שני מספרי AT (האם AT-3.15 יכסה גם דטרמיניזם וגם מכסה, או שיידרש גם AT-3.16) — פרט-מימוש קטן, לא חוסם את ה-Pre-Flight; יוכרע בשלב כתיבת ה-Work Order.

## (ג) סטיות תיעוד↔קוד

**ממצא B2-C1 (מגרסה 1 של מסמך זה) — נפתר.** הצמצום שביצעת ב-Scope הוא בדיוק התגובה הנכונה לממצא: אין עוד סתירה פתוחה בין המסמכים לקוד.

## (ד) למה ה-Scope החדש מספיק + הכרעה

ה-Scope המצומצם מכסה **בדיוק ורק** את שני הפערים המאומתים כפתוחים (דטרמיניזם-על-תרחיש-ריבוי, אינטראקציית-מכסה מעבר ל-2), ואת הרישום הפורמלי החסר. הוא לא משכפל את מה שכבר מוכח (טבלה למעלה), והוא נשאר בתוך ה-Non-goals שהגדרת — במיוחד "אין שינוי ב-Orchestrator מעבר למה שנדרש כדי לתקן כשל מוכח", שמונע over-engineering מראש.

**הכרעה: PROCEED.**

אין סתירה פתוחה, אין הנחה קריטית לא-מאומתת, ה-Scope תואם בדיוק את הראיות. אפשר לעבור לכתיבת `WORK_ORDER_B2.md` ברגע שתאשר את המסמך הזה.
