# KNOWN ISSUES
מעקב תקלות פתוחות. **Functional Gate דורש אפס פריטים פתוחים בחומרה critical/high.** פריטי medium/low מותרים אך מדווחים כחוב טכני ב-Project Health Report של כל Phase.

| ID | תיאור | חומרה | מודול | נפתח (Phase/תאריך) | סטטוס |
|---|---|---|---|---|---|
| KI-001 | סביבת הפיתוח (Claude Code sandbox) חוסמת גישת רשת ל-`datafeed.dukascopy.com` (403 ברמת ה-egress proxy). לא ניתן להוריד 3 שנות Ticks אמיתיים, לא ניתן להפיק דו"ח ספרד אמיתי לכיול RA-10/min_stop, ולא ניתן להריץ AT-0.1/AT-0.2/AT-0.7 מול דאטה אמיתי (רק מול Fixtures סינתטיים שמדמים את פורמט ה-wire במדויק). כל שאר T0.1–T0.6 מומשו ונבדקו במלואם. **נסגר (D-069):** Root cause = client fingerprint (User-Agent/headers/ALPN), לא IP/נפח-בקשות — מאומת בניסוי מבוקר (`scripts/diagnostics/test_h1_fingerprint.py`). תוקן ב-Production דרך הזרקת `BrowserLikeTransport` (`src/data/browser_transport.py`) ל-`DukascopyDownloader` הקיים ללא שינויו; אומת End-to-End על חודש שלם אמיתי (נובמבר 2022, 720/0/0 = transport_calls/429s/errors). | high | data | Phase 0 / 2026-07-08 | closed (ר' D-069) |
| KI-002 | `point_value=0.001` (סקאלת המרת המספר השלם הגולמי במבנה ה-tick של Dukascopy למחיר USD עבור XAUUSD) מבוסס על מוסכמה ציבורית מוכרת בכלי Dukascopy קוד-פתוח, אך לא אומת מול הורדה אמיתית (ר' KI-001). דורש אימות בפעם הראשונה שיש גישת רשת אמיתית. **נסגר (D-070):** אומת מול דאטה אמיתי (נובמבר 2022, 3,641,776 ticks) — טווח מחירים שפוענח תואם היסטורית באופן מדויק, כולל התאמה כמעט-לדולר לאירוע-שוק מתועד ומתוארך (15.11.2022). | medium | src/data/dukascopy_downloader.py | Phase 0 / 2026-07-08 | closed (ר' D-070) |
| KI-003 | `StateStore` (D-041) שומר היסטוריית גרסאות מלאה ל-Swing/FVG כדי ש-`as_of(ts)` יהיה נכון-לזמן-אמת גם בשאילתות רטרואקטיביות — אך `effective_ts` של גרסת FVG נגזר מ-`invalidated_at`/`confirmed_at` בלבד. עדכוני `mitigation_pct` **ביניים** (לא-סופיים, לפני 100%) אינם נושאים חותמת-זמן עצמאית, ולכן `as_of(ts)` שנשאל בין שני עדכוני מיטיגציה עשוי להחזיר ערך "מאוחר מדי" (לא future-lookahead מוחלט, אך לא מדויק-לזמן). לא נבדק/נחשף ב-Phase 1 כי הבדיקות שם לא מזינות Ticks אמיתיים ל-`on_price`. | low | src/store/state_store.py, src/fvg/mitigation.py | Phase 1 / 2026-07-08 | open |
| KI-004 | `D1BodyRatio.evaluate()` קיבל Default שקט ל-`body_vs_avg_n`/`ratio_min` (`params.get(key, default)`) שהעתיק את ערכי RA-17 המוצהרים ב-`config/parameters.yaml` לתוך קוד המודול. סיכון: אם `parameters.yaml` ישתנה (Grid/RA חדש), אף בדיקת Phase 1 לא הייתה תופסת סטייה, כי אף בדיקה לא טענה את קובץ הקונפיג בפועל — כל הקריאות ל-`FVGEngine` השתמשו ב-`displacement_params={}` ונשענו על ה-Default השקט. זוהה ב-Review עצמי של Phase 1 (לא ע"י בדיקה שנכשלה). **תוקן**: `D1BodyRatio.evaluate()` מעלה כעת `KeyError` מפורש אם `body_vs_avg_n`/`ratio_min` חסרים; נוסף `D1_DEFAULT_PARAMS` כקבוע מפורש-לייבוא; כל קריאות הבדיקה/Demo עודכנו להזריק אותו במפורש. | low | src/displacement/d1_body.py | Phase 1 / 2026-07-08 | closed (ר' D-043; מכוסה ע"י tests/test_at1_5_ranking.py + tests/fixtures/pipeline.py) |
| KI-005 | `RiskEngine.approve()` (D-047) בודק מכסה יומית לפי מילויים בפועל בלבד (`Portfolio.fills_today`), לא לפי "Setups חמושים במקביל" המלא כפי שה-SPEC מנסח (§10) — כי שריון-מכסה לפקודות Pending-לא-ממולאות דורש תיאום Setup Stream (T3.2) שעדיין לא קיים ב-Phase 2. AT-2.7 מתקיים במלואו תחת ההיקף הזה; אין השפעה על התוצאות של Phase 2 עצמה. **הוכרע (D-052, Design Review לפני Phase 3):** ייסגר במלואו כחלק מ-T3.2 — אכיפה פר-תיק ברגע ה-ARM, דרך `setup_arm_outcomes` (AT-3.11 נוסף). **תוקן (T3.2/T3.3, סטטוס עודכן במהלך Critical Review הסופי — התיקון עצמו כבר היה במקום, רק לא סומן סגור):** `RiskEngine.approve()` נקרא ב-`_route_intent` בדיוק ברגע ה-ARM, פר-תיק; `AT-3.11` (`tests/test_orchestrator.py::test_at3_11_quota_blocks_one_arm_but_not_another_on_the_same_setup`) ירוק ומאמת בידוד מכסה בין תיקים. | low | src/risk/engine.py, src/risk/portfolio.py | Phase 2 / 2026-07-08 | closed (ר' AT-3.11) |
| KI-006 | `Portfolio.apply_realized_pnl()` מוגדר ונבדק ביחידה (`tests/test_portfolio.py`), אך אינו מחובר לזרימת המילוי/סגירה בפועל — שום קוד ב-`src/` לא קורא לו (אומת ב-`grep -rn "apply_realized_pnl" src/`). כתוצאה מכך `Portfolio.realized_equity` נשאר קבוע על `initial_equity` דרך כל Phase 2, וכל Sizing (`size_units`) מבוסס בפועל רק על ההון ההתחלתי, לא ההון הממומש בפועל. אינו משפיע על Phase 2 (אין עדיין Orchestrator שמריץ עסקאות מקצה-לקצה), אך **חייב** להיות מחווט לזרימת ה-Exit/Fill ב-Phase 3, אחרת Sizing מכל Phase 4 ואילך יהיה שגוי בשקט. זוהה ב-Critical Review עצמי של Phase 2 (לא ע"י בדיקה שנכשלה). **תוקן (T3.3):** `Orchestrator._close_trade()` קורא ל-`apply_realized_pnl(net_pnl)` בכל Fill מסוג יציאה; מכוסה ב-`tests/test_orchestrator.py::test_end_to_end_armed_setup_fills_and_closes_trade_updates_equity`. | medium | src/risk/portfolio.py, src/backtest/orchestrator.py | Phase 2 / 2026-07-09 | closed (ר' T3.3; מכוסה ע"י tests/test_orchestrator.py) |
| KI-007 | `SpreadReport`/`build_spread_report(ticks, symbol)` (`src/data/spread_report.py`) אינו מבצע חלוקת-זמן (Time-Windowing) — הוא צובר `median_spread` לפי שעת-יום על פני **כל** טווח ה-Ticks שהוזן, ללא הפרדה בין עבר/עתיד ביחס לכל נקודת-זמן בבקטסט. ב-Phase 2 `RiskEngine.approve()` צורך את `SpreadReport` דרך `MarketContext.median_spread(hour_et)` (ר' `StateStore.median_spread`) כדי לחשב `min_stop_distance`. כל עוד ה-Phase 2 tests מזריקים `SpreadReport` מבוקר-ידנית דרך Fixtures (`tests/fixtures/spread.py`), אין דליפת מידע בפועל. **הוכרע (D-049, אושר ע"י המשתמש):** ב-v1, `median_spread` חייב תמיד להיגזר מ-Rolling/Expanding SpreadReport (רק נתונים זמינים עד `now`) — לא מאגרגט תקופתי מלא. **תוקן (D-055):** `ExpandingSpreadReport` (`src/data/spread_report.py`) מומש ומחווט בפועל ל-Orchestrator — `_apply_tick` מזין אותו בכל Tick (Stage 1, לפני כל שאילתת `median_spread` לאותו timestamp); `SpreadReport`/`build_spread_report` המקוריים נשארים ללא שינוי, ייעודם ל-T0.6/כיול RA-10 בלבד ואינם משמשים עוד לקבלת החלטות חיות. מכוסה ב-AT-3.12 (`tests/test_at3_12_spread_pit.py`) — כולל הוכחת אי-תלות-בעתיד ברמת יחידה וברמת Orchestrator. | high | src/data/spread_report.py, src/store/state_store.py, src/backtest/orchestrator.py | Phase 2 / 2026-07-09 | closed (ר' D-055; מכוסה ע"י tests/test_at3_12_spread_pit.py) |
| KI-008 | `Validator` (`src/data/validator.py`) משתמש בשני ספים מוצהרים כקבועים בקוד — `DEFAULT_GAP_THRESHOLD = 5min` ו-`DEFAULT_SPIKE_Z_THRESHOLD = 8.0` — שאינם רשומים כ-RA ב-`docs/RESEARCH_ASSUMPTIONS_V1.md` (אומת: אין אזכור `spike_z`/`gap_threshold` שם). אין כאן סיכון של Lookahead/Bias בפועל — אנומליות מדוּגללות בלבד (`CLAUDE.md`: "אין try/except pass על נתונים"), לא נמחקות ולא מתוקנות בשקט. אך שני הספים משפיעים על אילו טווחי דאטה מסומנים כחריגים, ולכן הם בפועל בחירת-מחקר (RA) שלא תועדה ככזו — שינוי עתידי שלהם לא יעבור את נוהל ה-RA המתועד. **נסגר-חלקית (B-6, Commit 4):** `spike_z_threshold` נסגר ונרשם כ-RA-29 (`docs/RESEARCH_ASSUMPTIONS_V1.md`, D-076) — מחווט כעת דרך `config/parameters.yaml::validator.spike_z_threshold` → `src/config/models.py::ValidatorParams` → הזרקה מפורשת ל-`Validator`, לא קבוע-שקוף-לא-מתועד. **`gap_threshold` (5min) נשאר לא-רשום — open, Follow-up נפרד** (D-071 מצא שהוא תקין, לא נבדק/כויל ב-B-6, מחוץ ל-Scope). | low | src/data/validator.py | Phase 2 / 2026-07-09 | partially closed (spike_z_threshold — B-6/D-076/RA-29; gap_threshold נשאר open) |

| KI-009 | `Execution Delay` (SPEC §12, PHASE_PLAN T2.1) לא היה ממומש כלל — ברירת המחדל (0ms) לא הייתה מושפעת (אף AT-2.x לא דרש אותו), אך יכולת ריצות-העמידות (250/500ms) חסרה לגמרי. זוהה ב-Critical Review עצמי של Phase 2 (לא ע"י בדיקה שנכשלה). **תוקן**: `FillSimulator(execution_delay_ms: int = 0)` מומש (ר' D-050); AT-2.8 נוסף ומכוסה ב-`tests/test_at2_8_execution_delay.py`; כל 45 הבדיקות הקודמות עברו ללא שינוי (מוכיח שברירת המחדל היא No-Op מוחלט). | medium | src/execution/fill_simulator.py | Phase 2 / 2026-07-09 | closed (ר' D-050; מכוסה ע"י tests/test_at2_8_execution_delay.py) |

| KI-010 | לוח חדשות היסטורי אמיתי (RA-23: CSV, "אדום", USD בלבד) עדיין לא אותר/נבדק — Phase 0 (שהיה אמור לקבוע זמינות מקור, ר' RA-23) נחסם ע"י KI-001 (חסימת רשת כללית) ולא בדק ספציפית מקור ללוח חדשות (ייתכן שונה מ-Dukascopy). זהה ב-Design Review של Phase 3, לפני בניית `CalendarEngine` (T3.1). **אינו חוסם כתיבת/בדיקת קוד**: `CalendarEngine` ייבנה עצמאי-ממקור-נתונים (D-037) — צורך `list[NewsEvent]` מוזרק, בדיוק כמו `Tick`/`Bar`; AT-3.9 ירוץ מול Fixture סינתטי. **חוסם** את Research Readiness Review (T3.4) באותו אופן כמו KI-001 — Blackout אמיתי לא ניתן לחשב בלי לוח אמיתי. | high | src/session/calendar_engine.py (ייבנה) | Phase 3 / 2026-07-09 | open (חוסם T3.4 בלבד, לא Phase 3 עצמו) |

| KI-011 | `M4EntryModel.on_bar_close_1m()` (T3.2) יכול להחזיר `OrderIntent | None` יחיד בלבד לפי חוזה INTERFACES.md — אם שני Setups שונים (מ-4H zones שונים) הופכים ל-ARMED וממתינים לנר-דחייה, ושניהם נענים בדיוק באותו נר 1M, רק הראשון (סדר איטרציה של dict) מקבל כניסה באותו נר; השני ימשיך להמתין לנר-דחייה עתידי. תרחיש נדיר ביותר (שני אזורי 4H פעילים בו-זמנית, אותו כיוון Bias, נרות-דחייה בדיוק על אותה דקה). אינו דורש שינוי חתימה (multi-return) — עלות/תועלת לא מצדיקה. | low | src/entry/m4.py | Phase 3 / 2026-07-09 | open |
| KI-012 | `Orchestrator._close_trade()` (T3.3) כותב ל-`trades.cost_spread`/`cost_slippage` ערך קבוע `0.0` — אין עדיין הפרדה בין רכיב-הספרד לרכיב-ה-Slippage בתוך מחיר המילוי הסופי (`Fill.price`) ברמת ה-Orchestrator; `cost_commission` כן מחושב במדויק (`cost_model.commission(units)`). `result_r`/`apply_realized_pnl`/`net_pnl` (הקריטיים למחקר ול-Sizing) **אינם** מושפעים — הם מחושבים ישירות מ-`Fill.price` בפועל, לא מהפירוק לרכיבים. תג`overnight`/`weekend`/`news_cross`/`concurrent`/`bias_flip` (`trades.tag_*`) גם קבועים `False` זמנית. Phase 4 (T4.2 Statistics Engine, "כל המדדים + פילוחים + MAE/MFE") הוא הבעלים הטבעי של פירוק-עלויות מלא ותיוג; ייסגר שם. | low | src/backtest/orchestrator.py | Phase 3 / 2026-07-09 | open (ייסגר ב-T4.2) |
| KI-013 | `trades.mae_r`/`mfe_r` (Maximum Adverse/Favorable Excursion, ב-R) נכתבים כ-`NULL` — דורשים מעקב תוך-עסקה אחר המחיר הקיצוני שהושג (טרם קיים ב-Orchestrator, ומיועד במפורש ל-T4.2 "MAE/MFE" לפי PHASE_PLAN.md). | low | src/backtest/orchestrator.py | Phase 3 / 2026-07-09 | open (ייסגר ב-T4.2, כמתוכנן) |
| KI-014 | מסלולי המילוי של M1 (Limit order) ו-M4 (נר-דחייה) דרך ה-Orchestrator המלא (Order → Fill בפועל) מכוסים רק ב-`tests/test_entry_models.py` (יחידה, ללא Orchestrator) או בתרחיש שאינו ממלא בפועל (M1 בבדיקת ה-Blackout-cancel ב-`test_orchestrator.py`). אף בדיקת Orchestrator קיימת לא מוכיחה את שני המסלולים האלה מקצה-לקצה עד Fill בפועל — רק M2 (Market מיידי) מוכח כך (`test_end_to_end_armed_setup_fills_and_closes_trade_updates_equity`). זוהה ב-Critical Review עצמי לפני דוח ההתקדמות של T3.1-T3.3. **תוקן (D-062, Critical Review סופי של Phase 3):** `tests/test_full_pipeline_9_arms.py` מוכיח את שלושת מודלי הכניסה (M1 Limit, M2 Market, M4 נר-דחייה) ממלאים בפועל, כל אחד בזרוע נפרדת, באותה ריצת Orchestrator אחת עם 9 התיקים המלאים דרך `build_orchestrator`. | medium | tests/test_full_pipeline_9_arms.py | Phase 3 / 2026-07-09 | closed (ר' D-062) |
| KI-015 | תצורת 9 התיקים המלאה ({M1,M2,M4}×{R_body,S_body,S_wick}) יחד באותה ריצת Orchestrator אחת מעולם לא נבדקה — הבדיקות הקיימות מריצות לכל היותר 2 זרועות (עם אותו זוג (entry,sl_anchor), לצורך בידוד בדיקת המכסה). אין הוכחה בפועל ש-9 התיקים אינם מתערבים זה בזה (למשל: שיתוף Setup Stream זהה, אך בידוד State/הזמנות/מכסה נכון בין 9 תיקים בו-זמנית) . זוהה ב-Critical Review עצמי לפני דוח ההתקדמות של T3.1-T3.3. **כיסוי משמעותי נוסף (D-062):** `tests/test_full_pipeline_9_arms.py` מוכיח ש-9 תיקים רצים יחד, שלושה מהם ממלאים בפועל (אחד לכל מודל-כניסה) והשישה האחרים נשארים לא-מושפעים לחלוטין (`realized_equity`/`fills_today` ללא שינוי) — בידוד מלא מוכח בפועל, לא רק בהנחה. **עדיין לא מכוסה (בזמנו):** אינטראקציית **מכסה** (2 מילויים/יום) כשיותר מ-2 Setups שונים מגיעים ל-ARMED על אותו תיק באותו יום — נשאר open עד שזה ייבדק ספציפית. **Closed (Stage A / B-2, D-069).** שני הפערים שנותרו נסגרו במלואם: (1) דטרמיניזם דו-ריצתי על תרחיש-הריבוי הספציפי (3 ממלאות/6 לא) — `tests/test_at3_15_multiarm_determinism.py` (AT-3.15), מייבא את `_scenario` הזה עצמו ללא שינוי. (2) אינטראקציית מכסה עם 3 Setups נפרדים ל-ARMED על אותו תיק/יום — `tests/test_at3_16_quota_arm_interaction.py` (AT-3.16); הבדיקה הראשונה חשפה כשל אמיתי (`M4EntryModel._watching` דולף setup_id אחרי invalidation פוסט-ARM, גורם ל-`KeyError` לא-מטופל בהמשך הריצה על Setup לא-קשור) שתוקן בהיקף מינימלי מאושר (D-069). | medium | tests/test_full_pipeline_9_arms.py, tests/test_at3_15_multiarm_determinism.py, tests/test_at3_16_quota_arm_interaction.py, src/backtest/orchestrator.py | Phase 3 / 2026-07-09 | closed (ר' D-069, AT-3.15, AT-3.16) |
| KI-016 | `src/config/models.py` (Pydantic v2: `RulesV1`, `Parameters`, `RunConfig`, `config_hash`) מומש ונבדק ביחידה (`tests/test_config_models.py`), אך אינו מחובר ל-Orchestrator בפועל — `Orchestrator` מקבל את כל הפרמטרים שלו (SL buffer, cost model, risk params וכו') דרך הזרקת constructor ידנית בלבד, לא דרך טעינת `config/rules_v1.yaml`/`config/parameters.yaml`/`config/run_default.yaml` בפועל. ריצה אמיתית מול קונפיג דורשת תרגום ידני כרגע. זוהה ב-Critical Review עצמי לפני דוח ההתקדמות של T3.1-T3.3. **תוקן (D-057):** `src/backtest/run_builder.py::build_orchestrator(rules, parameters, run_config, ...)` מתרגם את שלושת מודלי הקונפיג (כולל 9 התיקים המלאים מ-`run_config.arms`) ל-Orchestrator מחווט במלואו, במקום אחד בלבד. איתר וסגר גם פער חדש בדרך: `FillSimulator.in_news_checker` היה מחווט תמיד ל-Stub `lambda ts: False` בכל קריאה קיימת (גם ב-Orchestrator עצמו) — כעת מחווט ל-`calendar.in_blackout` האמיתי בכל תיק. מכוסה ב-`tests/test_run_builder.py` (4 בדיקות, כולל ריצה מלאה מקצה-לקצה מול קובצי ה-YAML האמיתיים ו-9 התיקים יחד — סוגר גם חלק מ-KI-015). **עדכון (Stage A / B-1, D-068):** `config_hash()` שהוזכרה כאן כמומשת-אך-לא-מחוברת עברה חיווט מלא למסלול הריצה דרך `build_orchestrator` — ר' KI-018 (Closed). | medium | src/backtest/orchestrator.py, src/config/models.py, src/backtest/run_builder.py | Phase 3 / 2026-07-09 | closed (ר' D-057; מכוסה ע"י tests/test_run_builder.py) |
| KI-017 | `Orchestrator.run()` בפועל מוגדר כ-`run(self) -> RunResult` (כל הדאטה/קונפיג מוזרקים ב-constructor), בעוד `docs/INTERFACES.md` מצהיר `run(self, cfg: RunConfig) -> RunResult`. סטייה בין התיעוד למימוש בפועל — דורשת הכרעה: לעדכן את INTERFACES.md כך שישקף את דפוס ה-constructor-injection בפועל, או ל-Refactor את ה-Orchestrator כך שיקבל `cfg: RunConfig` בזמן `run()`. זוהה ב-Critical Review עצמי לפני דוח ההתקדמות של T3.1-T3.3. **תוקן (D-057):** נבחרה האופציה הראשונה — `docs/INTERFACES.md` עודכן לשקף את `run(self) -> RunResult` בפועל; ה"תרגום מ-config" עצמו עבר ל-`build_orchestrator` (KI-016) ולא ל-`run()`, ששומר על היותו parameterless ונטול-תלות-בקונפיג ישירות (D-037: Orchestrator עדיין data/config-agnostic, רק constructor-injected). | low | src/backtest/orchestrator.py, docs/INTERFACES.md | Phase 3 / 2026-07-09 | closed (ר' D-057) |
| KI-018 | טבלאות `runs`/`experiments` ב-Journal (זהות-ריצה: `config_hash`, `data_version`, `code_version` — ר' `ExperimentTracker.register_run` ב-INTERFACES.md) מעולם לא נכתבות ע"י ה-Orchestrator — אין קריאה ל-`register_run` בשום מקום ב-`src/`. כתוצאה מכך אין כרגע דרך לאמת בפועל את עקרון הדטרמיניזם של CLAUDE.md ("אותו (config_hash, data_version, code_version) → אותו יומן, ביט-לביט") מול ריצה אמיתית, כי אין רשומת-זהות מתמשכת בכלל. זוהה ב-Critical Review עצמי לפני דוח ההתקדמות של T3.1-T3.3. **כיסוי חלקי (D-059):** התברר בפועל (בזמן מימוש T3.6) שזה לא רק "נחמד שיהיה" — בלי `experiments`/`runs`/`portfolios`, שום כתיבה ל-`orders`/`setup_arm_outcomes`/`trades`/`context_snapshots` לא הייתה יכולה להצליח בכלל מול FK אמיתי. `_write_run_identity_rows` כותב כעת את שלוש הטבלאות פעם אחת בתחילת `run()`, אך `config_hash`/`code_version`/`data_version`/`split_type`/`seed` הם **placeholder מוצהר** ("unknown"/0) — ה-Orchestrator הבסיסי (D-037) לא יודע אותם; חיווט אמיתי (למשל דרך `run_builder`/`config_hash()`) עדיין נדרש. **Closed (Stage A / B-1, D-068).** חיווט אמיתי של `config_hash`/`code_version`/`data_version`/`split_type`/`seed` דרך `build_orchestrator` (`src/backtest/run_builder.py`) — מיקום החיווט נעול ל-`run_builder` בלבד (D-037/D-057 נשמרים; `Orchestrator` עדיין לא מחשב דבר, רק מחזיק את הערכים שהוזרקו). מאומת ב-AT-3.14 (דטרמיניזם דו-ריצתי אמיתי על ה-fixture המלא של D-064 + היעדר "unknown" + `seed IS NULL` + רשומת Registry). הפתרון הסופי ל-`seed` (D-067, שינוי סכימה מאושר) ול-`config_hash` (D-068) הופך גם את ה-Placeholder-ים המוצהרים של D-059 למיותרים. הפניה: KI-016 (`config_hash()`/`build_orchestrator` שחוברו כעת בפועל למסלול הריצה, לא רק קיימים ביחידה). **הערה שנותרה מחוץ להיקף (Follow-up פתוח, עדיפות Low):** קריאות `Orchestrator(...)` ישירות שעוקפות `build_orchestrator` (`tests/fixtures/orchestrator.py::make_orchestrator`) ממשיכות לקבל את ה-Placeholder-ים הישנים — לא נחשב רגרסיה (B-1 נעול ל-`run_builder`, WORK_ORDER_B1.md §2), אך יישאר כך עד שקורא כזה יעבור בעצמו ל-`build_orchestrator`. **עדיפות: Low · יעד: לכל המאוחר ב-Pre-Flight של B-3** (מועד טבעי לבדוק אם ה-Blocker הבא נוגע באותם call sites; אם לא — יישאר פתוח ויועבר הלאה). | medium | src/backtest/orchestrator.py, src/journal/duckdb_writer.py, src/backtest/run_builder.py | Phase 3 / 2026-07-09 | closed (ר' D-068, AT-3.14) |

| KI-019 | `setup_arm_outcomes` (D-060, אושר במפורש ע"י המשתמש): פוזיציה שעדיין **פתוחה בתום הריצה** (הזמנה מולאה, אך לא יצאה לפני שנגמרו ה-Ticks/Bars שהוזנו — Overnight/Weekend מותר לפי SPEC §11) לעולם לא מקבלת שורת `setup_arm_outcomes`, כי הכתיבה היחידה קורית ב-`_close_trade`/`_cancel_for_setup`, ואף אחד מהם לא רץ עבור פוזיציה שנשארת פתוחה. מקרה-קצה גבולי (משפיע רק על העסקאות האחרונות ליד סוף תקופה/Holdout), לא שיטתי, ואינו משפיע על דטרמיניזם (אין החלטת-מסחר תלוית-Journal). פתרון עתידי: Event Log אמיתי (PK מגורסן) אם/כשיידרש — הוערך ונדחה במפורש מהיקף Phase 3 (D-060). | low | src/backtest/orchestrator.py | Phase 3 / 2026-07-10 | open (מקרה-קצה מתועד, לא חוסם) |
| KI-020 | **זוהה ב-Critical Review סופי של Phase 3:** צינור זיהוי המבנה מ-4H גולמי — `Orchestrator._apply_4h` → `StructureEngine.step` + `FVGEngine.step_bar_close` → FVG/Bias אמיתיים ב-`StateStore` — **מעולם לא הופעל דרך ה-Orchestrator עצמו**, באף בדיקה של Phase 3. כל בדיקת Orchestrator קיימת (כולל `tests/test_full_pipeline_9_arms.py` שנוסף בביקורת הזו) "מזריעה" FVG+Bias ישירות דרך `store.put(...)`, עוקפת את המנוע לגמרי — תבנית שחוזרת בעקביות מ-T3.2 ועד עכשיו. `StructureEngine`/`FVGEngine` עצמם נבדקים היטב (AT-1.1–AT-1.6, כולל Prefix-Consistency) אך **בבידוד**, לא כפי ש-`_apply_4h` בפועל קורא להם (bar בודד + store, לא batch). ל-D-063 (סדר 1M→5M→4H) יש כעת בדיקה ישירה על `_merged_timeline`, אך אין שום בדיקה שמוכיחה שרצף 4H אמיתי, שעובר דרך ה-Orchestrator, אכן מניב FVG/Bias-flip ש-SetupStream מזהה ומגיב לו נכון בהמשך אותה ריצה. סיכון ארכיטקטוני עיקרי שנותר לפני Phase 4/T3.4. **תוקן/נסגר (D-064):** `tests/test_full_pipeline_from_raw_4h_bars.py` (3 בדיקות) מריץ רצף 4H אמיתי (זהה בצורתו ל-AT-1.3+AT-1.4, שני מקטעים מוכחים בנפרד ומחוברים כאן לראשונה) דרך `build_orchestrator` **ללא שום `store.put`** — BOS→Bias אמיתי, FVG אמיתי [128,116], Engagement→R→S→iFVG→Armed→M2→RiskEngine→FillSimulator→TP, וכן שורות Journal מלאות (`setups`/`context_snapshots`/`trades`) שנבדקות ישירות. הריצה הצליחה בניסיון הראשון לאחר אימות-מקדים בסקריפט-טיוטה (לא נדרש שינוי ארכיטקטוני/קוד לסגירה) — מוכיח שהחיווט האמיתי תקין, לא רק שהמנועים הבודדים תקינים. | medium | src/backtest/orchestrator.py, tests/test_full_pipeline_from_raw_4h_bars.py | Phase 3 / 2026-07-10 | closed (ר' D-064) |
| KI-021 | **זוהה ב-Self-Review אדוורסרי של B-1 Closure (WORK_ORDER_B1_CLOSURE.md, משימה 3 — לא תוקן בשקט, ר' §1.1 שם).** `_append_registry_record` (`src/backtest/run_builder.py`) כותבת ל-`data/registry/runs.jsonl` באמצעות `registry_path.open("a")` + `write()` פשוט, ללא שום נעילת-קובץ (`fcntl`/`flock`) וללא בדיקת אטומיות. אם `build_orchestrator` ייקרא אי-פעם מכמה תהליכים במקביל שכותבים לאותו `registry_path` בו-זמנית (למשל ריצות מחקר מקבילות ב-Phase 4/5 — עדיין לא קיימות ב-Stage A), רשומות JSONL עלולות להשתזר/להיפגם. **לא סיכון פעיל כרגע:** כל שימוש קיים (בדיקות Phase 3/Stage A) הוא חד-תהליכי בלבד; ה-Registry עצמו זמני עד T5.5 (Experiment Tracker, שאמור לטפל בכתיבה מקבילה כראוי מלכתחילה). | low | src/backtest/run_builder.py | Stage A / B-1 / 2026-07-21 | open (Follow-up: לטפל אם/כש-Phase 4/5 מריצות ריצות מקבילות בפועל, או כחלק מ-T5.5 Experiment Tracker — לא חוסם את B-1 עצמו) |
| KI-022 | **זוהה תוך כדי סגירת KI-001/KI-002 (ר' D-071) — אימות מול דאטה אמיתית ראשונה (נובמבר 2022) חשף ש-`Validator` (`src/data/validator.py`) מייצר 18 flagged gaps ו-2,998 flagged spikes בחודש בודד.** ניתוח מבוסס-ראיות (`scripts/diagnostics/analyze_spikes.py`, מחקר-רשת, הצלבה עם `build_spread_report`) קבע: רוב ה-gaps ורוב ה-spikes מוסברים כ-Natural market behavior מתועד (rollover יומי 17:00 ET, Thanksgiving, אירועי CPI/גיאופוליטיקה מתוארכים) — אך גודל-move חציוני של spikes מדוגללים (0.2305) קטן מהספרד הטיפוסי (~0.40–0.45), מרמז על רגישות-יתר אפשרית של ה-baseline הרוליג (500 ticks, `spike_z_threshold=8.0`) שמעולם לא נבדק מול דאטה רב-session אמיתי, רק Fixtures סינתטיים. `Validator` אינו מבחין כיום בין (א) Data corruption, (ב) Market behavior טבעי, (ג) Calibration sensitivity — כולן מדוגללות לאותה דגל בודדת. **לא תוקן — נדרש Work Order ייעודי לניסוי-כיול מבוקר (חלונות/ספים חלופיים) לפני כל שינוי threshold/window בפועל.** **נסגר (B-6, D-076/RA-29, Commit 4).** ניסוי-הכיול המבוקר שנדרש כאן בוצע במלואו: Grid-Sweep + הצלבה מול דוח-ספרד אמיתי (`build_spread_report`, B-4) על 3 חודשים אמיתיים ממשטרי-תנודתיות שונים (2022-11 חדשותי, 2024-02 שקט, 2024-11 תנודתי) — לא Fixtures. ב-8.0 (המקורי): 87%-94% מהספייקים המדוגללים קטנים מ-p95-הספרד האמיתי של שעתם, בעקביות בשלושת החודשים. הוכרע מעבר ל-**12.0**, מחווט דרך Config (לא קבוע-שקוף) — ר' KI-008 לעדכון-המנגנון. **מגבלות מוצהרות (RA-29):** כויל על 3 חודשים מייצגים בלבד, לא Sweep מלא על 39 החודשים; מבוסס Spread-Proxy, לא אימות-Slippage/תוצאה-כלכלית-ישירה או בדיקת-False-Negative מלאה. | medium | src/data/validator.py | KI-001 Follow-up / 2026-07-28 | closed (ר' D-076, RA-29, Commit `355d18c`) |
| KI-023 | **זוהה ב-Pre-Flight/Commit 1 של B-4 (RA-10 Real Spread Report) — אין מנגנון-קוד מרכזי (`holdout_unlock=True`) שאוכף את גבול-ה-Holdout (`config/run_default.yaml::holdout`) על סקריפטים חיצוניים שקוראים ל-`TickParquetStore.read_month()` ישירות.** `data/ticks/` ו-`data/holdout/` "מופרדים פיזית" לפי מבנה-הריפו (`CLAUDE.md`), אך בפועל כל 39 חודשי-ה-Backfill (כולל חלון-ה-Holdout, יולי-דצמבר 2025) יושבים יחד תחת `data/ticks/`, ללא הגנת-קוד אוטומטית שמונעת מסקריפט-אבחון "לראות" אותם. **נפתר במלואו נקודתית ב-B-4 עצמו (D-073):** שתי הגנות ידניות (CLI-level + post-resolution) נבנו בתוך `scripts/diagnostics/run_full_spread_report.py` עצמו ואומתו בפועל (שני מסלולים נבדקו — refuse על חריגה, resolve נכון על טווח חוקי) — B-4 **אינו** תלוי בפתרון-מערכת ואינו חסום על ידי הרשומה הזו. **הבהרה מפורשת (מניעת הרחבת-Scope):** רשומה זו היא **Follow-up בלבד**, **אינה Blocker של B-4** (B-4 סגור במלואו בלעדיה), **ואינה משנה את סטטוס ה-RRR** (סעיף 3 ב-RESEARCH_READINESS_REVIEW.md נותר GO כפי שנקבע ב-D-074, ללא תלות ברשומה זו). ה-Scope שלה מיועד במפורש ל-**B-6/KI-022 או Hardening עתידי** — בניית מנגנון-אכיפה מרכזי ב-`TickParquetStore`/`Checkpoint` עצמם (שינוי-קוד שדורש אישור נפרד) — ו**אין** לפתוח ממנה כל עבודת-קוד נוספת בתוך B-4 עצמו. | medium | src/data/tick_store.py | Stage A / B-4 / 2026-08-07 | open (Follow-up בלבד ל-B-6/KI-022 או Hardening עתידי; לא Blocker של B-4; לא משנה RRR) |
| KI-024 | **זוהה תוך כדי B-5 Commit 3 (הרצת Quality Gates מחדש, `bash scripts/ci.sh`).** דרישת "Golden Regression" — מוצהרת במפורש ב-`docs/QUALITY_GATES.md` §6 ("יומן תקופת-מדגם זהה ביט-לביט; שינוי מכוון = הסבר + Golden חדש באישור משתמש") וב-`docs/ARCHITECTURE.md` ("בדיקות-על") — **אין לה שום Implementation בריפו:** חיפוש מקיף (`grep -rln "golden" tests/ scripts/`) לא החזיר שום script/test/reference-artifact. אין Baseline-run קיים להשוואה, אין מנגנון-השוואה-ביט-לביט. **לא Blocker ל-B-5 — Follow-up עתידי בלבד.** | medium | tests/ (ייבנה, טרם קיים) | Stage A / B-5 / 2026-08-07 | open (Follow-up: מתודולוגיית Golden Regression + Baseline-run ראשוני טרם הוגדרו; לא נפתח Work Order כרגע) |

**נוהל:** תקלה שהתגלתה ולא תוקנה מיידית → שורה כאן לפני כל בקשת אישור Phase. סגירת תקלה → סטטוס `closed` + בדיקה שמכסה אותה. אסור למחוק שורות — היסטוריה מלאה.
