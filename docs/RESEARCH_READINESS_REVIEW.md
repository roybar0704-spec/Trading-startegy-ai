# RESEARCH READINESS REVIEW — חובה לפני T3.4

**מעמד:** שער חובה, לא הצהרתי (D-037). ריצת בקטסט אמיתית ראשונה (T3.4, PHASE_PLAN.md) וכל עבודה ב-Phase 4–5
אסורות עד שהסקירה הזו הושלמה בפועל והניבה **GO**. בדיקות טכניות ירוקות (pytest/ruff/Quality Gates) **אינן
תחליף** לסקירה הזו — הן תנאי הכרחי אחד מתוכה, לא מספיק.

**נוהל כשל:** אם סעיף כלשהו למטה = NO-GO → עוצרים את הפרויקט לפני T3.4. לא ממשיכים "באופן זמני", לא עוקפים
"רק כדי לבדוק". מפיקים את הדוח הזה במלואו, עם כל סעיף שסומן NO-GO וסיבתו, ומציגים למשתמש לפני כל המשך.

---

## Checklist (למלא בכל הרצה של הסקירה)

| # | סעיף | דרוש להיות GO | עדות/הפניה |
|---|---|---|---|
| 1 | **KI-001 נסגר?** (חסימת רשת ל-Dukascopy) | גישת רשת אמיתית קיימת; `DukascopyDownloader` הורץ בפועל מול `datafeed.dukascopy.com` והביא דאטה אמיתי | הפניה ל-KNOWN_ISSUES.md, סטטוס KI-001 |
| 2 | **KI-002 נסגר?** (סקאלת `point_value` ל-XAUUSD לא מאומתת) | `point_value` אומת מול דאטה אמיתי (למשל: מחיר Bid/Ask גולמי מפוענח נופל בטווח סביר ליום מסחר ידוע ב-XAUUSD) | הפניה ל-KNOWN_ISSUES.md, סטטוס KI-002 |
| 3 | **RA-10 כויל?** (Slippage-Stop) — ✅ **GO** (B-4, D-074) | דו"ח ספרד **אמיתי** (לא סינתטי) הופק ל-3 שנות הדאטה; RA-10 עודכן או אושר מחדש מול המדידה, ונרשם ב-RESEARCH_ASSUMPTIONS_V1.md + DECISIONS_LOG לפי הנוהל שם | `scripts/diagnostics/run_full_spread_report.py`, D-074, `docs/RESEARCH_ASSUMPTIONS_V1.md` RA-10. **הבהרה מוצהרת:** הדו"ח כיסה `2022-10-01`…`2025-06-30` (33/39 חודשים) — **לא** את מלוא 39 החודשים; יולי-דצמבר 2025 הוחרגו במכוון כחלון-Holdout (D-073), לא נמדדו לצורך RA-10. `costs.slippage_stop_usd`: `0.10`→`0.70` (p95-Spread, Proxy — לא מדידת-Slippage ישירה, ר' D-074). |
| 4 | **נתוני Dukascopy אומתו?** — ✅ **GO with explicit limitations documented** (B-5, D-075) | AT-0.1/AT-0.2/AT-0.6/AT-0.7 (ומומלץ גם AT-0.3–0.5) הורצו **מול הדאטה האמיתי בפועל**, לא רק מול Fixtures סינתטיים; 3 שנות דאטה + 90 יום Warm-Up נקיים לפי הגדרת T0.4 | `scripts/diagnostics/run_at0_real_data_checks.py` (AT-0.1/0.4/0.6, Sandbox, נובמבר-2022 האמיתי — 3,641,776 ticks, 29,929/29,929 ברים תואמים), `scripts/diagnostics/run_at0_7_holdout_mechanism_check.py` (AT-0.7, בדיקת-מנגנון על עותק-דאטה-אמיתי — **לא** הפרדה-פיזית, ר' KI-023), `scripts/diagnostics/run_at0_2_cache_check.py` (AT-0.2, מחשב-הבית, `data/raw/` אמיתי — PASS אושר ע"י Roy). D-075. **הבהרות מוצהרות:** (א) הכיסוי הוא **חודש-דוגמה אמיתי אחד** (נובמבר 2022, המאומת-כבר ב-D-070/D-072) לכל AT-0.1/0.4/0.6/0.7 — לא הורץ כ-AT פורמלי על פני כל 39 החודשים; "3 שנות דאטה נקיות" מכוסה בנפרד ע"י אימות-Spike/Gap שבוצע לכל אחד מ-39 החודשים בפועל בזמן ה-Backfill (Batch 1-7 Closure Reports). (ב) AT-0.7 הוא **בדיקת-מנגנון בלבד** — Holdout הפיזי (יולי-דצמבר 2025) עדיין לא הופרד בפועל, ר' KI-023. (ג) AT-0.3 מכוסה ע"י D-071 (לא Gate); AT-0.5 מסומן N/A (ללא-תלות-בדאטה, לא Gate). |
| 5 | **Quality Gates עדיין ירוקים?** — ✅ **GO (עדכון: B-11B, D-091)** | כל ששת השערים (QUALITY_GATES.md) ירוקים על מצב הריפו הנוכחי, כולל Regression על Phase 0–2 | `bash scripts/ci.sh` (Sandbox, HEAD=`ff23929`): Code-Quality ✅ (ruff — "All checks passed!"; pylint duplicate-code — 10.00/10), Architecture ✅ (lint-imports — "Contracts: 4 kept, 0 broken"). `ci.sh`'s own "frozen-config integrity" step failed (exit 49) — diagnosed as a pre-existing `python3`→Windows-Store-stub environment bug on this machine, unrelated to `rules_v1.yaml` itself; independently re-verified outside `ci.sh` via direct SHA-256 recomputation (`uv run python`): computed == recorded (`ba14c88a...ec3a1`), MATCH=True, on HEAD `ff23929`. Functional **✅ GO** — `uv run pytest -q` on HEAD `ff23929`: exit code 0, zero F/E/x/X/s markers across all output; `uv run pytest --collect-only -q` independently confirms 179 tests collected (exit code 0) — up from 170 at the prior `40df439` citation (9 tests added since, from already-committed work). **הבהרה:** שורת-הסיכום המילולית של pytest ("N passed") לא נדפסת עקב Bug ידוע ומתועד-מראש (`pyproject.toml` `addopts="-q"` + `-q` נוסף = `-qq`, ר' `PROJECT_COMPLETION_CHECKLIST.md` §15) — סטטוס Pass/Fail מאומת במקום זאת דרך exit code 0 (הסימן הסמכותי של pytest) והיעדר-מוחלט של סמני-כשל/שגיאה/דילוג בכל 179 הנקודות. Regression **✅ GO** — אותה הרצת `pytest -q` כוללת את Prefix-Consistency ו-Golden Regression (`tests/test_at1_6_prefix_consistency.py`, `tests/test_golden_regression.py`) בתוך ה-179, שניהם בתוך התוצאה כולה-עברה/exit-0; `tests/golden/at3_14_baseline.sha256` מאומת ללא שינוי (`git diff --stat` ריק). **Documentation → ✅ GO (B-11A, D-090)** — §5 נסגר פורמלית: DECISIONS_LOG מעודכן, מסמכים מושפעים עודכנו (`7fc6454`), README משקף יכולות חדשות. **Performance → ✅ GO (B-11B, D-091)** — הרצה אמיתית בוצעה מול 2024-01..03: 103.3839 שניות מתוך תקציב 300 שניות, Peak RSS 3,426.7MB מתוך תקציב 8GB, `benchmarks/phase3_bench_real.json`. **שער 5 עצמו כעת GO — כל ששת תת-הסעיפים ירוקים.** **חשוב — אין להסיק מכך RRR GO כולל:** נדרשת הרצה-מחדש מלאה של כל תשעת שורות ה-RRR, כולל שורה 9 (שיפוט מפורש, לא ברירת-מחדל), ואישור מפורש של Roy. **T3.4 נשאר חסום עד אז.**

**עדכון מכני (2026-09-07), HEAD `99c8e6e` (D-097: resolve non-protected Ruff findings — commit יציב, לא working tree זמני; מחליף את ההערה הקודמת שהתייחסה ל-working tree לא-מקומיט):** אימות-מחדש READ-ONLY מלא בוצע מול HEAD זה. **Functional/Regression ✅** — `pytest -q`: 257/257 עברו, 0 נכשלו, 0 דולגו, 0 xfailed, exit 0 (עלה מ-179 בציטוט ההיסטורי `ff23929`, מתוספות D-093/D-095 שנשמרות כראיה היסטורית ולא נדרסות). `tests/test_at3_12_spread_pit.py` + `tests/test_golden_regression.py`: 4/4 עברו בנפרד. **Code Quality — pylint ✅** (`--enable=duplicate-code`: 10.00/10, ללא שינוי). **Architecture ✅** (`lint-imports`: "Contracts: 4 kept, 0 broken", 57 קבצים/94 תלויות). **Frozen-config ✅** — `verify_frozen_rules()` וחישוב-SHA-256 ישיר תואמים: `ba14c88a35c66fdfc80921f21cb140e9612768ce2118253d38687de4fb7ec3a1`, זהה לרשום ב-`config/rules_v1.yaml.sha256`; `rules_v1.yaml` ללא שינוי.

**Ruff — ⚠️ CONDITIONAL/ACCEPTED PRE-EXISTING EXCEPTION, לא "Ruff נקי לחלוטין":** `ruff check src tests` מחזיר **exit 1**, **5 ממצאים בדיוק**, כולם בשלושה קבצים מוגנים בעלי שינויי-working-tree טרום-קיימים שהיו במפורש מחוץ ל-Scope של D-097 — ממצאי תיעוד/פורמט-Docstring בלבד, לא לוגיים/פונקציונליים: `src/backtest/orchestrator.py:271,644` (D205), `src/data/tick_store.py:216` (D205), `src/viz/trade_page.py:139` (D205+D209). **אין ממצאי Ruff נותרים בשום קובץ אחר** לאחר D-097 — 21 הממצאים המקוריים על HEAD זה תוקנו במלואם למעט חמשת אלה. שלושת הקבצים המוגנים **לא נערכו ולא נוספו ל-Staging** במסגרת D-097 — דיפם נשמר בדיוק כפי שהיה. אין רגרסיה פונקציונלית שנצפתה בשום שלב (257/257 יציב לפני ואחרי תיקוני-D-097).

**אושר במפורש ע"י Roy** לקבל את חמשת הממצאים כ-Conditional/Accepted Pre-Existing Exception לצורך RRR זה בלבד — לא נעשה ניסיון להביא Ruff ל-exit 0 ע"י נגיעה בקבצים המוגנים. **החריגה הזו אינה משנה את קונפיגורציית Ruff ואינה נוגעת ב-`rules_v1.yaml`.** יש לייצג מצב זה במדויק כחריגה מתועדת ומאושרת — **לא** כ-"Ruff עבר נקי".

**הציטוט ההיסטורי לעיל (HEAD `ff23929`) נשמר כראיה היסטורית ואינו נדרס** — זהו עדכון-בנוסף, לא תיקון רטרואקטיבי, תואם את התבנית הקיימת של המסמך.

**שוב, במפורש: אין להסיק RRR GO כולל מעדכון זה.** שורה 9 נותרת פתוחה, טעונה שיפוט מפורש של Roy, ולא שונתה במסגרת עדכון זה. **T3.4 נשאר חסום עד להכרעה מפורשת בשורה 9.** |
| 6 | **KI-007 נסגר?** (Point-in-Time SpreadReport, D-049) | `SpreadReport` המוזרק לריצה אמיתית הוא Rolling/Expanding בפועל — נבדק שאין שימוש ב-SpreadReport שמכיל נתונים עתידיים ביחס לכל נקודת-זמן בבקטסט | הפניה ל-KNOWN_ISSUES.md, סטטוס KI-007 + בדיקה ייעודית |
| 7 | **KI-010 נסגר?** (לוח חדשות אמיתי, RA-23) — ⚠️ **GO with explicit limitations documented (B-7, D-077)** | לוח חדשות אמיתי (CSV, אדום, USD) אותר ואומת; `CalendarEngine` הורץ מולו בפועל, לא רק מול Fixture סינתטי | `src/data/news_loader.py`, `data/news/bls_calendar.csv` (BLS, 66 אירועים אמיתיים), D-077, RA-23. **הבהרה מוצהרת:** ה-GO מתייחס ל-**Pipeline** (Data Contract/Loader/CalendarEngine Integration מוכחים במלואם מול דאטה אמיתי) ול-**Phase 1 בלבד** — CPI + Employment Situation (NFP), 2 מתוך 7 סוגי-אירוע High-Impact-USD מוכרים (`KI010_DECISION_DOC.md` §3). **אינו** Coverage מלא של כל "חדשות אדומות USD" לפי `docs/SPEC_V1_FROZEN.md` שורה 67 — Core PCE/GDP/FOMC Rate Decision/ISM PMI/Retail Sales עדיין לא מכוסים; Backtest על דאטה זה לא יחסום סביבם. Open Future Work — לא משויך ל-B-8; קשור ל-D-078 ותנאי-הפקיעה שלו (ר' D-084). |
| 8 | **KI-006 נסגר?** (`Portfolio.apply_realized_pnl` מחווט) | `Orchestrator`/Journal flow קורא בפועל ל-`apply_realized_pnl` בכל סגירת עסקה; Sizing מריצה אמיתית משתמש בהון ממומש-בפועל, לא רק בהון ההתחלתי | הפניה ל-KNOWN_ISSUES.md, סטטוס KI-006 + בדיקה ייעודית |
| 9 | **סיבה כלשהי שלא להתחיל Backtesting אמיתי?** — ✅ **GO with Guardrails** (הכרעת Roy, 2026-09-07) | בדיקה פתוחה: אנומליות דאטה לא-פתורות, חריגה מהחלטות RA-01–09, שינוי לא-מתועד ב-Rules/RA, כל דגל אדום אחר | **שיפוט מפורש של Roy (2026-09-07): GO with Guardrails.** אין כרגע סיבה מהותית שחוסמת את תחילת T3.4. עם זאת, ריצת T3.4 האמיתית הראשונה היא **ריצת-אימות מבוקרת**, **לא עדיין ראיה לפרשנות מחקרית** — כי T3.4 **מעולם לא הושלמה בהצלחה מקצה-לקצה** עד כה (הניסיון הריאלי הראשון קרס; ה-Root Cause תוקן במלואו ב-D-092, אך התיקון טרם נבחן בריצה חיה מחדש). **הריצה חייבת להיעצר על כל אנומליה מהותית.** לפני שמסיקים מסקנות מחקריות: יש להשלים את אימות-T3.4 הנדרש במלואו, כולל **AT-3.10** ואימות ידני של **20 עסקאות מדגם** מול היומן/הגרף (ר' `PROJECT_COMPLETION_CHECKLIST.md` §6.B). **אם הריצה נכשלת או חושפת אנומליה מהותית — לעצור, לאתר Root Cause, ולעדכן את ה-RRR הזה לפני כל המשך.**

**תיקון-עובדתי D-100 (2026-09-08), בעקבות Audit READ-ONLY שגילה ראיה קיימת-בריפו שלא נבדקה לפני D-099:** המשפט לעיל ב-D-099 — "T3.4 מעולם לא הושלמה בהצלחה מקצה-לקצה עד כה" — **אינו מדויק עובדתית, ומתוקן בזאת.** Audit READ-ONLY של `data/journal/t3_4_in_sample.duckdb` העלה: קיימת ריצת T3.4 שהושלמה במלואה, ללא קריסה — `run_id=t3.4-in_sample-2024-01-2024-03-M2xS_body`, `code_version=f49d7af41d507e19ae3ab13f2cd5a86519fc29a6+dirty`, 1 עסקה (`TRADE-ORD-P-M2-S_body-1`, Setup `SETUP-6809` — **אותו Setup שצוין בראיית-הקריסה המקורית של D-092**), exit=`tp`, R≈2.62. **המשמעות המדויקת:** זו ראיה לביצוע מוצלח **תחת התיקון של D-092**, אך **לא אימות-נקי של ה-HEAD-המקומיט-הנוכחי** — הריצה בוצעה על עץ-עבודה `+dirty` (שינויים לא-מקומיטים בזמן הריצה), לפני ש-D-092 עצמו קומיט (`4cf2da6`). **ההכרעה עצמה אינה משתנה: GO with Guardrails נשארת בתוקף במלואה** — תיקון זה הוא עובדתי-בלבד, לא שינוי-החלטה, ואין להסיק ממנו אימות-נקי של הקוד המקומיט הנוכחי.

**בנוסף נמצא (Audit READ-ONLY נפרד):** `data/journal/at3_10_population_2022_10_2025_06.duckdb` מכיל ריצת-Population קיימת ומאומתת-פנימית: 46 עסקאות, 46 הזמנות (יחס 1:1, ללא רשומות-יתומות), 85,336 Setups, 46 קובצי-Viz תואמים בדיוק (`TRADE-ORD-P-M2-S_body-1`…`-46`), טווח-זמן חופף למלוא החלון המוצהר (כניסה ראשונה 2022-11-09, יציאה אחרונה 2025-06-10). **זהו Population Artifact קיים המספיק במספרו (46 ≥ 20) לבחירת 20 עסקאות-המדגם הנדרשות ל-AT-3.10** — אך קיומו **אינו** מהווה מימוש של AT-3.10 עצמו: AT-3.10 עדיין דורש **אימות ידני מפורש** של 20 עסקאות מול Viz + Journal + Chart (`docs/ACCEPTANCE_TESTS.md:41`), ואין להסיק כל מסקנה מחקרית מה-Population הזה לפני שהשער הידני הזה מתמלא במלואו. אם תתגלה אנומליה מהותית בתהליך האימות — לעצור, לאתר Root Cause, ולעדכן את ה-RRR הזה לפני כל המשך (כפי שכבר נקבע ב-D-099 לעיל, ונשאר בתוקף ללא שינוי). |

## פסיקה

**GO** רק אם **כל תשעת** הסעיפים GO. סעיף אחד NO-GO ⇒ הפסיקה הכוללת NO-GO.

## תוצר

- **GO:** מתועד כאן (תאריך + חתימת-הרצה config_hash/data_version/code_version) ונרשם ב-DECISIONS_LOG; T3.4 יכול להתחיל.
- **NO-GO:** דוח מפורט לפי הטבלה למעלה — כל סעיף שנכשל, למה, ומה התנאי המדויק לסגירתו — מוצג למשתמש. הפרויקט
  עוצר לפני T3.4 עד שהתנאים נסגרים ומתבצעת סקירה חוזרת.
