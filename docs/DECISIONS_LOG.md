# DECISIONS LOG
תיעוד כל החלטה משמעותית: מהות, נימוק, סיווג, תנאי פתיחה מחדש, השפעת שינוי.
**חובת תחזוקה:** כל החלטה משמעותית במהלך הפיתוח = שורה חדשה. כל Refactor למודול שעבר AT = שורה חדשה (Stability Rule).
סיווגים: **Rule** = חוק אסטרטגיה (קפוא) | **RA** = הנחת מחקר | **Arch** = ארכיטקטורה | **Process** = תהליך עבודה.

כל הרשומות D-001…D-027: **2026-07-08, ספרינט תכנון V1.**

| ID | החלטה | נימוק | סוג | פתיחה מחדש | השפעת שינוי |
|---|---|---|---|---|---|
| D-001 | נכס יחיד XAUUSD; פלטפורמת מחקר, לא אינדיקטור | מיקוד; אמינות לפני התרחבות | Process | לא ב-V1 | פרויקט אחר |
| D-002 | מקור אמת יחיד: Dukascopy Bid/Ask Ticks; כל הנרות ממנו | עקביות בין TF; ספרד נמדד | Arch | אם Phase 0 יפסול איכות | data_version חדש + כל הריצות מחדש |
| D-003 | חלון מעוגן NY: ‏08:30–10:30 ET (לא שעון ישראל) | הלוגיקה של השוק מונעת מ-NY; יציבות DST | Rule | ניסוי עתידי בלבד | משנה את כל הבקטסט |
| D-004 | עוגן נרות 4H = NY-Close ‏17:00 ET | הקונבנציה שסוחרי SMC רואים | RA-21 | Experiment מלא חדש | משנה אילו FVG קיימים |
| D-005 | Bias = State Machine ‏4H; Neutral = No-Trade | הגדרה חישובית חד-משמעית | Rule | FE-01 | מוסיף/גורע עסקאות Neutral |
| D-006 | BOS = סגירה מלאה; Wick בלבד = Sweep | הבחנה חדה, ניתנת לקידוד | Rule | v2 בלבד | משנה Bias וסיווג FVG |
| D-007 | FVG תקף עד Mitigation ‏100%; ללא מגבלת גיל | פשטות v1 | Rule | FE-02 | מסנן אזורים ישנים |
| D-008 | דירוג L1–L3; עדיפות: BOS→Disp→רגיל→קרבה | הכרעה דטרמיניסטית בין אזורים | Rule | v2 בלבד | בחירת Setup שונה |
| D-009 | טריגר = קריאה 2: R→S→iFVG (פורמליזציה מלאה) | נאמן לכוונת הסוחר; אושר במפורש | Rule | v2 בלבד | מהות האסטרטגיה |
| D-010 | iFVG: ‏Inversion בסגירה, הראשון אחרי S, ללא Re-Arm | דטרמיניזם; שמרנות | Rule | Re-Arm = FE-04 | כמות ואיכות כניסות |
| D-011 | H1: סגירת R, סגירת S והכניסה — בתוך החלון | "כניסות בלבד בחלון" באופן עקבי | Rule | FE-07 | כמות Setups |
| D-012 | SL: שלוש היפותזות R_body/S_body/S_wick | המשתמש דחה הכרעה מראש; הנתונים יכריעו | Rule (מבנה מחקר) | בסיום מחקר הזרועות → ננעל כ-v1.2 | קובע את זרוע הלייב |
| D-013 | TP ‏3R קבוע, סגירה מלאה | פשטות; בסיס השוואה נקי | Rule | ניהול פוזיציה = FE-11 | פרופיל התוצאות |
| D-014 | Sizing ‏0.5% (משתמש) על יתרה ממומשת (RA-22) | שמרני, שחזורי | Rule + RA | Dynamic = FE-03 | גדלים, לא איתותים |
| D-015 | מכסה: 2 מילויים/יום; pending לא נספר; ביטול ב-10:30 | הגדרה תפעולית חד-משמעית | Rule | FE-12 (Cooldown) | כמות עסקאות |
| D-016 | חדשות: ‏±30 דק' אדום USD; ביטול pending ב-Blackout; פוזיציה ממשיכה + תג | הגנה מספייקים; בלי סגירה בכוח | Rule + RA-23 | Grid חלונות = FE-05 | חשיפה לחדשות |
| D-017 | Overnight/Weekend מותר + תיוג מלא | אין סגירה בכוח; ניתוח בדיעבד דרך תגים | Rule | החרגה דרך התגים | חשיפת גאפים |
| D-018 | ביצוע שמרני: Bid/Ask אמיתי; Limit=‏Ask≤P; SL-First; Gap-Through ריאלי | בקטסט פסימי > אופטימי | Arch | לא נפתח | אמינות התוצאות |
| D-019 | עיבוד דו-שלבי לכל timestamp (H2) | פותר מרוץ 09:00; דטרמיניזם | Arch | לא נפתח | — |
| D-020 | ‏9 זרועות {M1,M2,M4}×{R,S,Wick}, תיקים מבודדים, Stream זהה, כניסה זהה בתוך מודל | השוואה זוגית, מינימום רעש | Arch + מחקר | נסגר בהכרעת v1.2; מודלים נוספים FE-06 | מסקנות המחקר |
| D-021 | פרוטוקול: תקופה קבועה; ‏≥150 עסקאות; WF; Hold-Out חד-פעמי; Baseline; מטרה נעולה מראש | חסינות מ-Snooping | Rule (עקרונות) | ערכים בלבד — דרך RA-01…09 | תוקף ההסקה |
| D-022 | הפרדת Rules / Parameters / RA + Tracker + config_hash | Optimization בלי לשנות זהות אסטרטגיה | Process | קבוע | שחזוריות |
| D-023 | Scoring = Log-Only; AI = Analyst Read-Only | מניעת Overfitting במדגם קטן | Rule | פילטרים = FE-08/09 | אין השפעה על v1 |
| D-024 | V1 Freeze; כל רעיון חדש → FUTURE_EXPERIMENTS | ייחוס שיפורים לגרסה, לא לזחילת חוקים | Process | קבוע | משמעת גרסאות |
| D-025 | Working Software Rule: כל Phase מסתיים בתוצר עובד ניתן לבדיקה + AT ירוקות | קוד בלי תוצר נראה = לא סיום | Process | קבוע | שערי Phase |
| D-026 | Stability Rule: אין Refactor למודול שעבר AT בלי סיבה מתועדת כאן; אחרי Refactor — כל ה-AT מחדש | יציבות > אלגנטיות | Process | קבוע | משמעת קוד |
| D-027 | DECISIONS_LOG כמקור אמת היסטורי להחלטות | שחזור "למה" בעוד שנה | Process | קבוע | — |
| D-028 | Feature Store: ‏Registry+EAV+Context Snapshots; כל עסקה = וקטור Features; הרחבה בלי מיגרציות ובלי נגיעה במנוע; Features תיאוריים בלבד ב-v1 | מחקר עתידי רטרואקטיבי בלי ריצות מחדש | Arch | קבוע | יכולת המחקר של הפלטפורמה |
| D-029 | 7 הגדרות Feature מוצעות (CHoCH, sweep_type, displacement_score, volatility_score, volume, confirmation_time, ATR-14) — סטטוס proposed עד אישור משתמש | עמימות = עצירה; הוצגו במפורש | RA (מוצע) | באישור המשתמש → RA-24/25 + approved | הגדרות בלבד; אפס השפעה על מסחר |
| D-030 | פורמט דוח סיום Phase: 7 סעיפים + אישור מפורש לפני מעבר | דרישת משתמש בתחילת הפיתוח | Process | קבוע | משמעת שערים |
| D-031 | שכבת QA: ששה Quality Gates לכל Phase + Project Health Report; דוח סיום תלת-חלקי; אכיפה בפקודות (pytest, ruff, pylint-dup, import-linter, bench) ולא בהצהרות | דרישת משתמש; שער לא-אכיף = שער מת | Process | קבוע | משמעת איכות לכל חיי הפרויקט |
| D-032 | תקציבי ביצועים: ערכים התחלתיים בטבלת QUALITY_GATES; כיול במדידה ראשונה על חומרת היעד; מכאן ואילך אין רגרסיה >‎20% ללא תיעוד | יעדי ביצועים לא הוגדרו מעולם — בחירה התחלתית שקופה במקום המצאה שקטה | Process (Initial Choice) | בכל עת, בהחלטת משתמש | ספי שער הביצועים |
| D-033 | סגירת שלב התכנון; חבילת Handoff (17 קבצים, HANDOFF_MASTER.md כאינדקס-על) = מקור אמת יחיד; כל טיוטה מחוץ לחבילה Superseded; המשך העבודה ב-Claude Code מ-Phase 0 בלבד | מסירה נקייה ללא תלות בהיסטוריית השיחות | Process | קבוע | מקור האמת של הפרויקט |
| D-034 | סביבת הפיתוח חוסמת רשת ל-`datafeed.dukascopy.com` (KI-001). באישור המשתמש: T0.1–T0.6 מומשו במלואם כקוד אמיתי (לא Mock-לצורך-Mock) ונבדקו מול Fixtures סינתטיים שמדמים במדויק את פורמט ה-wire של Dukascopy (bi5+LZMA, מבנה רשומת 20 בייט); הורדת 3 שנות הדאטה האמיתי, דו"ח הספרד האמיתי וכיול RA-10 נדחים לסביבה עם גישת רשת. | חסימת רשת היא אילוץ סביבתי, לא עמימות בדרישה; המשתמש בחר "לבנות הכל, לדחות את ההורדה האמיתית" מתוך שלוש חלופות שהוצגו | Process | כשתהיה גישת רשת אמיתית — להריץ T0.2/T0.3 בפועל ולסגור KI-001 | דו"ח סיום Phase 0 אינו יכול להצהיר Functional Gate ירוק במלואו עד סגירת KI-001 |
| D-035 | Architecture Gate (lint-imports): חוזה השכבות המלא מ-QUALITY_GATES.md מוצהר ב-pyproject.toml בהדרגה, מודול-מודול, ככל שכל Phase בונה אותו בפועל — לא כל השכבות בבת אחת מראש. Phase 0: רק חוזה "src.config אינו תלוי ב-src.data". | import-linter דורש שהמודולים המוצהרים בחוזה יתקיימו בפועל; הצהרת שכבות עתידיות שלא נבנו עדיין תשבור את הכלי | Arch (Process) | בכל Phase הבא — להוסיף את השכבה שהפאזה בונה לחוזה ב-pyproject.toml | אכיפת Architecture Gate בפועל, לא רק בהצהרה |
| D-036 | שער Phase 0 מפוצל לשני תת-שערים, באישור משתמש: **תת-שער קוד** (AT-0.1–AT-0.7 ירוקות מול Fixtures סינתטיים + כל Quality Gates) — **סגור**. **תת-שער דאטה** (3 שנות דאטה אמיתי + דו"ח ספרד אמיתי + כיול RA-10, ר' KI-001/KI-002) — **פתוח**, אינו חוסם Phase 1–2 (ה-AT שלהם מתוכננים על Fixtures סינתטיים לפי ACCEPTANCE_TESTS.md, ללא תלות בדאטה אמיתי), אך חוסם באופן מוחלט את T3.4 ואת Phase 4–5 (הסקה סטטיסטית על דאטה מזויף = חסרת משמעות ומפרה את חוק-על #3). PHASE_PLAN.md §Phase 0/1/3 עודכן בהתאם. | ניתוח תלויות בפועל של ACCEPTANCE_TESTS.md: אף AT-1.\*/AT-2.\* לא דורש דאטה אמיתי; המחקר הסטטיסטי (Phase 4–5) לעומת זאת תלוי בו לחלוטין — קו גבול חד בין "בטוח להמשיך" ל"אסור להמשיך" | Process | לפני תחילת T3.4 — לוודא שני תת-השערים סגורים (ר' RESEARCH_READINESS_REVIEW.md); אם יתגלה ש-AT-1.\*/AT-2.\* בפועל דורשים דאטה אמיתי במהלך המימוש — לעצור ולעדכן כאן | קובע את גבול ה-Phase שממנו דאטה אמיתי הופך לחוסם קשיח; מאפשר להתחיל Phase 1 עכשיו |
| D-037 | שני כללים נוספים באישור משתמש: **(1) עצמאות ממקור נתונים** — כל מודול מ-Phase 1 ואילך פועל זהה על Fixture סינתטי ועל דאטה אמיתי; ההבדל היחיד המותר הוא מקור הקלט (`DataProvider`/`Bar`/`Tick`), אף פעם לא ענף קוד (תועד ב-ARCHITECTURE.md עקרון #7). **(2) Research Readiness Review חובה לפני T3.4** — סקירה מסודרת (לא רק טכנית) לפי `docs/RESEARCH_READINESS_REVIEW.md`: KI-001 סגור? KI-002 סגור? RA-10 כויל? דאטה Dukascopy אומת? Quality Gates ירוקים? סיבה כלשהי שלא להתחיל Backtesting אמיתי? כשל בסעיף כלשהו ⇒ עצירת הפרויקט לפני T3.4 + דוח מפורט, לא המשך. | דרישת משתמש; מבטיח שהמעבר מ"קוד נבדק על Fixtures" ל"מסקנות מחקר על דאטה אמיתי" הוא החלטה מפורשת ומתועדת, לא מדרון חלקלק | Process | הכלל קבוע; הסקירה עצמה חוזרת בכל פעם שמנסים לפתוח מחדש את T3.4 | שער חובה נוסף בין Phase 3 ל-Phase 4–5; לא ניתן לדלג עליו גם אם הבדיקות הטכניות ירוקות |
| D-038 | Refactor (Stability Rule): טיפוסי הליבה `TF`/`Tick`/`Bar` מועברים מ-`src/data/types.py` ל-`src/core/types.py`; מתווספים אליהם `Swing`/`FVG` כלשונם מ-INTERFACES.md. `src/core` הוא מודול חסר-תלויות (אף import מ-`src`) שכל שכבה — data/store/structure/fvg/entry/... — רשאית לייבא ממנו בלי להפר את חוזה השכבות. Phase 0 AT-0.1–AT-0.7 (וכל שאר הבדיקות) נדרשות לרוץ מחדש ולעבור אחרי ה-Refactor. | INTERFACES.md מגדיר Tick/Bar/Swing/FVG/Setup/OrderIntent/ArmId כקבוצת "טיפוסי ליבה" אחת, בנפרד מהחתימות של Data/Store/Structure/FVG. `store` (מתחת ל-structure/fvg בחוזה השכבות) חייב להחזיק Swing/FVG בחתימות שלו (`active_fvgs`, `confirmed_swings`) בלי ליצור תלות מעגלית store↔structure/fvg — פתרון: מודול core משותף, נטול-תלויות, מתחת לכל השכבות | Arch | אם בעתיד יתגלה שטיפוס ליבה נוסף (Setup/OrderIntent/ArmId) יוצר את אותה בעיה — מרחיבים את `src/core/types.py` באותו אופן | ממשקי המודולים; import-linter contract; כל import של Tick/Bar בקוד קיים |
| D-039 | `MarketContext` (T1.1) מיושם עם חתימת INTERFACES.md המלאה ללא שינוי, אך `in_window()`/`in_blackout()`/`median_spread()` מעלים `NotImplementedError` מתועד (לא מדמים ערך שרירותי) עד שהמנועים התומכים נבנים בפועל: Session/Calendar Engines ב-T3.1 עבור השניים הראשונים, חיווט ל-`src.data.spread_report` ב-Phase 2 עבור השלישי. `bias()`/`active_fvgs()`/`confirmed_swings()` מיושמים במלואם עכשיו. | המשך ישיר לתבנית שכבר אושרה ב-D-035 (הכרזת חוזים בהדרגה, מודול-מודול, ככל שכל Phase בונה את התומך בפועל); "no half-finished implementations" — NotImplementedError מפורש ומתועד עדיף על מימוש מזויף שיחזיר ערך שגוי בשקט | Arch | ב-T3.1 (in_window/in_blackout) וב-Phase 2 (median_spread) — להחליף את ה-NotImplementedError במימוש אמיתי; לעדכן כאן | קוד שקורא ל-in_window()/in_blackout()/median_spread() לפני שהמנוע התומך קיים ייכשל בבירור, לא ישתוק |
| D-040 | פערי-פירוט ב-SPEC §2 שנסגרו בפרשנות הנדסית מפורשת (לא עמימות לעצירה — תוצאה יחידה סבירה): **(1)** ה-Swing הנבחן ל-BOS/Sweep הוא תמיד ה-Swing המאושר **האחרון** מאותו סוג, מוחלף בכל אישור Swing חדש **ללא תלות** אם הישן נלקח. **(2)** BOS **מנקה** את ההפניה (עד שיאושר Swing חדש) — השבירה אמיתית וסופית. **(3)** Sweep **אינו מנקה** את ההפניה — ה-Wick לא שובר מבנה; אותה רמה ממשיכה לשמש הפניה לבר הבא. **(4)** `taken_at` נקבע רק על BOS, לא על Sweep. | בלי (2)+(3): בר עתידי שחוצה שוב את אותה רמה שכבר נשברה ב-BOS היה יוצר אירועי BOS/Sweep "רפאים" חוזרים נגד רמה שכבר נצרכה — מנוגד לכוונת "BOS נמדד מול ה-Fractal המאושר האחרון" וליציבות ה-State Machine (D-006, §2) | Arch | v2 בלבד (זה פרשנות מימוש לחוק §2 הקפוא, לא שינוי בחוק עצמו) | תוצאות Fractal/BOS/Sweep/Bias לכל הפאזות הבאות שמסתמכות עליהן |
| D-041 | תיקון קריטי ל-`StateStore` שנתפס ע"י AT-1.6 (Prefix-Consistency) עצמו: "הכתיבה האחרונה מנצחת" (D-039/התיעוד המקורי של T1.1) **אינו** נכון-לזמן-אמת כאשר `as_of(ts)` נשאל **רטרואקטיבית** על Store שכבר קיבל כתיבות עתידיות (בדיוק מה ש-Prefix-Consistency בהכרח בודק: השוואת ריצה מלאה מול ריצה קטומה, שתיהן נשאלות ב-`ts` היסטורי). נמצא באג ממשי: `Swing.taken_at` מבר עתידי "דלף" ל-`as_of(ts)` של בר קודם. תוקן: `StateStore` שומר **היסטוריית גרסאות** מלאה לכל Swing/FVG (לא רק את הגרסה האחרונה); `effective_ts` של כל גרסה נגזר משדותיה (`taken_at`/`invalidated_at` אם נקבעו, אחרת `confirmed_at`); `as_of(ts)` בוחר את הגרסה האחרונה עם `effective_ts ≤ ts`. `all_swings`/`all_fvgs` (לצריכה פנימית של מנועים, לא Point-in-Time) ממשיכים להחזיר את הגרסה העדכנית ביותר. מגבלה ידועה שנותרה: `FVG.mitigation_pct` הביניים (לא-סופי, לפני 100%) עדיין לא משוחזר נקודתית-בזמן במלואו כי לאובייקט אין שדה timestamp לעדכון-הביניים עצמו — לא בעייתי כרגע כי Phase 1 אינו מזין Ticks אמיתיים ל-`on_price`; ייבדק שוב כש-Phase 2/3 יחברו הזנת Tick אמיתית. | "No-Lookahead by Construction" (ARCHITECTURE.md עקרון #1) הוא לא סיסמה — AT-1.6 תפס הפרה אמיתית שלו. תיקון תוך-פאזה, לפני שהקוד יצא כ"עובד"; לא Refactor למודול שכבר עבר AT (עדיין בתוך T1.1/T1.6 באותה משימה) | Arch | אם יתגלה שגם `mitigation_pct` הביניים נדרש נקודתית-בזמן — להוסיף שדה timestamp ל-Snapshot נפרד (לא ל-FVG עצמו, ששדותיו קפואים ב-INTERFACES.md) | תוקף AT-1.6 ותוקף כל שאילתת `as_of()` רטרואקטיבית בכל הפאזות הבאות |
| D-042 | **אושר במפורש ע"י המשתמש** (אחרי Review עצמי של Phase 1 שהעלה זאת כפער-ניסוח שהוחלט עליו חד-צדדית): פרשנות A מ-D-040 מאומצת כ-**Rule** קפוא, לא כ"פרשנות מימוש". `docs/SPEC_V1_FROZEN.md` §2 עודכן בהבהרה מפורשת: BOS שובר מבנה סופית ומנקה את ההפניה; רק Swing מאושר חדש הופך להפניה הבאה; Sweep הוא אירוע נזילות בלבד ואינו מאפס הפניה. אין שינוי בהתנהגות המיושמת (הקוד כבר מימש פרשנות A מ-D-040) — זו סגירת פער-ניסוח בטקסט הקפוא, **לא** גרסת Spec חדשה (v2) ולא שינוי המחייב תהליך ניסוי (אין השפעה על תוצאת בקטסט קיימת). | חוק-על #2 (עמימות=עצירה): D-040 הוחלט חד-צדדית בלי לעצור ולהציג שתי חלופות למשתמש כנדרש; זוהה ב-Review עצמי (לא ע"י בקשת המשתמש) ותוקן על ידי הצגת שתי החלופות בדיעבד וקבלת הכרעה מפורשת | Rule | v2 בלבד (זהו כעת חלק מהחוק הקפוא) | קובע רשמית את קצב היפוכי Bias וכמות ה-Setups לכל הפאזות הבאות; לא ניתן לשנות בלי גרסת Spec חדשה |
| D-043 | שתי פעולות היגיינה שזוהו ב-Review העצמי של Phase 1, בוצעו לפני Phase 2 באישור משתמש: **(1)** `StateStore.bias_history() -> list[BiasEvent]` נוסף כ-Accessor ציבורי; כל גישה חיצונית ל-`store._bias_events` (ב-scripts/demo_phase1.py, tests/test_at1_3_bias_transitions.py, וב-`MarketContext.bias()` הפנימי) הוחלפה בקריאה ל-Accessor; אפס `# noqa: SLF001` נותרו. **(2)** `D1BodyRatio.evaluate()` (src/displacement/d1_body.py) הפסיק לקבל Default שקט ל-`body_vs_avg_n`/`ratio_min` — כעת מעלה `KeyError` מפורש אם הם חסרים מ-`params`; `D1_DEFAULT_PARAMS` נוסף כקבוע מפורש-לייבוא לצרכי בדיקה/Demo (לא כברירת מחדל אוטומטית). KI-004 נוסף ל-KNOWN_ISSUES, סגור מיד, מתעד את הממצא והתיקון. | דוגמה קונקרטית ל"Config שקוף": CONFIG_SCHEMA.md כבר מצהיר "שדה לא מוכר = שגיאה, לא התעלמות שקטה" עבור `extra=forbid`; אותו עיקרון חל גם על שדה **חסר** בפרמטרי Displacement — Default שקט יכול להסתיר Grid/RA-17 שהשתנה ב-`parameters.yaml` בלי שאף בדיקה תתפוס זאת, כי אף בדיקת Phase 1 לא טענה את קובץ הקונפיג בפועל | Arch (Process) | קבוע; אם מודל Displacement עתידי (D2–D5) יתווסף — אותו עיקרון (params נדרשים, אין Default שקט) חל עליו | נקיון ה-API הציבורי של StateStore; מניעת סטייה שקטה בין `parameters.yaml` לקוד |
| D-044 | **אושר במפורש ע"י המשתמש**, לפני Phase 2 (RiskEngine.Sizing): יחידות Sizing (0.5% מהיתרה הממומשת ÷ מרחק סטופ) **אינן מעוגלות** — Float מדויק, ללא Lot Step/גודל-חוזה-מינימלי. נרשם כ-**RA-26** (RESEARCH_ASSUMPTIONS_V1.md §C). | שלב המחקר בודק את ביצועי האסטרטגיה עצמה, לא מגבלות ברוקר; מגבלות מעבר-ללייב (Lot Step וכו') יטופלו בנפרד ולא ישפיעו על תוצאות ה-v1; אם ירצו לבדוק השפעת Lot Step בעתיד — זו תהיה RA/זרוע ניסוי נפרדת, לא שינוי ב-v1 | RA | בנוהל החלפת RA הרגיל (RESEARCH_ASSUMPTIONS_V1.md, אישור משתמש, לא באמצע Experiment רץ) | Position Sizing בכל עסקה, לכל 9 הזרועות ב-Phase 4 ואילך |
| D-045 | **עמימות אמיתית שנתפסה לפני מימוש (חוק-על #2), אושרה במפורש ע"י המשתמש:** `OrderIntent.price` מוגדר סמנטית כ-**Reference Entry Price** — המחיר שעל בסיסו נוצרה כוונת הכניסה — לא מחיר ביצוע. מאוכלס **תמיד**, בכל מודל כניסה כולל Market (M2 = מחיר סגירת נר ה-Inversion; M1/M4 = `gap.top`). `RiskEngine` בודק גאומטריה (`entry − SL ≥ min_stop_distance`) מול `OrderIntent.price` בלבד, לעולם לא מול מחיר ביצוע. `FillSimulator` בלבד קובע Execution Price (Spread/Slippage/Gap) — עשוי להיות שונה מה-Reference, וזה תקין ואינו נוגע לאישור העסקה. `docs/SPEC_V1_FROZEN.md` §8/§9 ו-`docs/INTERFACES.md` (הערות ליד `OrderIntent.price` ו-`RiskEngine.approve`) עודכנו בהבהרת סמנטיקה בלבד — **אפס שינוי בחתימות הציבוריות**, אפס שינוי בהתנהגות אסטרטגיה. | `MarketContext` (כפי שמוגדר) אינה חושפת מחיר נוכחי (`mid()`/`current_price()` לא קיימים), ו-`OrderIntent.price` מוצהר `float \| None` — בלי ההבהרה הזו, `RiskEngine` לא היה יכול לבדוק גאומטריה לפקודת Market בלי או לשנות Interface קפוא (הוספת מתודה) או לנחש סמנטיקה חד-צדדית | Rule | v2 בלבד (זו כעת סמנטיקה קפואה של OrderIntent.price) | כל אישור/דחיית Setup דרך RiskEngine, מכל מודל כניסה, לכל הפאזות הבאות |
| D-046 | המשך D-038 (טיפוסי ליבה בהדרגה): `Side`, `ArmId`, `OrderIntent` נוספים ל-`src/core/types.py` כלשונם מ-INTERFACES.md (Phase 2 צריך אותם). `Order`, `Fill`, `Rejection`, `PortfolioId` — **אינם** מוגדרים ב-INTERFACES.md בכלל (רק נזכרים כטיפוסי-החזרה של Protocol methods, בלי שדות). `Order` נבנה כתעתוק ישיר של טבלת `orders` ב-`db/schema.sql` (מקור קיים, לא המצאה). `Fill`/`Rejection`/`PortfolioId` הם המצאה ארכיטקטונית מינימלית (Fill: order_id/ts/price/kind; Rejection: setup_id/reason; PortfolioId: alias ל-str). כל הארבעה הועברו ל-`src/core/types.py` (לא ל-`src/risk` או `src/execution` בנפרד) כי `RiskEngine.approve()` **בונה** Order (צריך את הטיפוס), ו-`FillSimulator` **צורך** Order ו**מייצר** Fill — שני המודולים זקוקים לטיפוסים בלי שאחד תלוי במודול-המימוש של השני (עקרון זהה בדיוק ל-D-038 עבור Swing/FVG). | מונע תלות מעגלית/היררכית מיותרת בין risk↔execution (עמיתים לפי ARCHITECTURE.md, לא אחד-מעל-השני); `Order` במיוחד — שדותיו כבר "קפואים בפועל" ע"י db/schema.sql, אין כאן החלטת-לוגיקה, רק תעתוק סכימה קיימת | Arch | אם יתגלה שטיפוס נוסף (Trade וכו') יוצר אותה בעיה — מרחיבים את `src/core/types.py` באותו אופן | ממשקי risk/execution; import-linter contract (risk/execution כשכבת-עמיתים מעל structure/fvg/displacement/store/data) |
| D-047 | **היקף מוצהר, לא שינוי-לוגיקה (בדומה למיקום Execution Delay שכבר אושר):** `RiskEngine.approve()` ב-Phase 2 בודק מכסה לפי **מילויים בפועל בלבד** (`Portfolio.fills_today`) — התנאי המלא מה-SPEC ("Setups חמושים במקביל ≤ המכסה שנותרה", §10) דורש לדעת על כל הפקודות הממתינות-לא-ממולאות בו-זמנית בכל התיק, מה שדורש תיאום Setup Stream (T3.2, Phase 3) שעדיין לא קיים. עד אז: אישור מבוסס רק על `fills_today ≥ 2 → blocked_quota`; אין עדיין "שריון" מכסה לפקודות Pending. AT-2.7 (המילוי השלישי נחסם) מתקיים במלואו תחת ההיקף הזה. | AT-2.7 בודק ספציפית "מילוי שלישי נחסם" — לא תרחיש עם כמה Setups חמושים בו-זמנית; אין ב-Phase 2 מנגנון שיודע על כל הפקודות התלויות בו-זמנית ברחבי המערכת (זה בדיוק תפקיד Setup Stream) | Arch (Process) | כש-Setup Stream (T3.2) נבנה — להרחיב את בדיקת המכסה לכלול שריון לפקודות Pending, ולעדכן כאן | כמות Setups חמושים בו-זמנית שיאושרו ב-Phase 3 ואילך; לא משפיע על AT-2.7 עצמה |

| D-048 | שלוש פעולות היגיינה שזוהו ב-Critical Review העצמי של Phase 2, בוצעו עצמאית (חוק-עבודה #2 — אינן משנות התנהגות): **(1)** נוסף `tests/test_cost_model.py` (4 בדיקות, כולל `in_news=True` שלא נבדק כלל קודם). **(2)** נוסף `tests/test_portfolio.py` (3 בדיקות, כולל `apply_realized_pnl` שלא נבדק כלל קודם; חושף את KI-006). **(3)** נוסף `if intent.side not in ("long", "short"): raise ValueError(...)` ל-`RiskEngine.approve()` — בדיקת-גבול הגנתית תואמת לזו הקיימת על `intent.price is None` (D-045: הפרת-חוזה של מפיק ה-Intent, לא דחייה לגיטימית); כוסתה ב-`tests/test_risk_engine_contracts.py` יחד עם בדיקת `price is None` שגם היא התגלתה כלא-מכוסה. שני ממצאי-תיעוד נוספים נרשמו כ-KI-007 (`median_spread` lookahead — high, מוצג למשתמש להכרעה) ו-KI-008 (ספי Validator לא רשומים כ-RA — low). | Code Quality Gate דורש שכל נתיב-קוד קריטי (כולל branches מותנים כמו news multiplier, ותביעות `raise` על הפרת-חוזה) יהיה מכוסה בבדיקה מפורשת, לא רק "עובד באינטגרציה"; נמצא ע"י Review אדברסרי מכוון (`grep` לאיתור קריאות/כיסוי), לא ע"י בדיקה שנכשלה | Process (Code Quality) | קבוע | כיסוי בדיקות בלבד; אפס שינוי בהתנהגות הרצה |

| D-049 | **אושר במפורש ע"י המשתמש**, בעקבות ממצא Critical Review (KI-007): `SpreadReport`/`build_spread_report(ticks, symbol)` (`src/data/spread_report.py`) לא ביצע חלוקת-זמן — `median_spread(hour_et)`, שמשמש את `RiskEngine.approve()` לחישוב `min_stop_distance` (§9), היה עלול לדלוף מידע עתידי אם ב-Phase 3 ה-Orchestrator היה בונה SpreadReport יחיד מכל התקופה. **הכרעה:** ברירת המחדל היחידה של v1 היא Rolling/Expanding SpreadReport — בכל נקודת-זמן `t` בבקטסט, `median_spread(hour_et)` נגזר **אך ורק** מנתונים זמינים עד `t`. אין פשרה (לא אופציה B/RA-27 של אגרגט תקופתי מלא, לא אופציה C/מפולח-WF) — "No-Lookahead" הוא Rule, לא Trade-off, גם אם ההשפעה הצפויה קטנה. SpreadReport מסוג אחר (אגרגט מלא, מפולח-WF וכו') מותר רק כזרוע-מחקר נפרדת שאינה משנה את התנהגות v1. `docs/SPEC_V1_FROZEN.md` §9 עודכן בהבהרה מפורשת. **טרם מומש** — נדרש Refactor אמיתי ל-`SpreadReport`/Orchestrator ב-Phase 3 (אין שינוי קוד כרגע: Phase 2 עדיין מזריק SpreadReport מבוקר-ידנית דרך Fixtures, שאינו לוקה בבעיה בפועל). | חוק-על #3 (זיהוי הטיה ⇒ עצירה) וכלל-העבודה #1 (החלטה שמשנה תוצאות מחקר ⇒ עצירה ושאלה) — לא ניתן לפתור עצמאית; המשתמש בחר את החלופה המחמירה ביותר במפורש, בלי פשרה על "השפעה קטנה" | Rule | v2 בלבד (מגדיר את מקור-הנתונים החוקי היחיד ל-median_spread ב-v1) | חוסם את T3.4/Phase 3 Orchestrator design עד שה-Rolling/Expanding SpreadReport ימומש בפועל; KI-007 נשאר open עד המימוש, עם כיוון-פתרון מוכרע |

| D-050 | **פער-סקופ שנתפס ב-Critical Review של Phase 2 (SPEC §12 + PHASE_PLAN T2.1 מצהירים "Execution Delay: 0ms ברירת מחדל; ריצות עמידות 250/500ms", ‏אך לא היה מנגנון Delay כלל ב-`FillSimulator`/`CostModel`). המשתמש אישר לממש עכשיו, ואישר במפורש עמימות-משנה: ה-Delay חל **גם על Entry וגם על Exit** (SL/TP), לא רק על כניסות חדשות.** מומש ב-`FillSimulator(execution_delay_ms: int = 0)`: כשמתקיים תנאי-מילוי (Entry נכנס לתנאי Limit, Market תמיד, SL/TP נוגע), הפקודה **נחמשת** (Armed) ולא ממולאת מיד; המילוי בפועל מתבצע ב-Tick המאוחר הראשון שבו `tick.ts ≥ trigger_ts + delay_ms`, לפי נתוני השוק **של אותו Tick מאוחר** (כולל סטטוס-חדשות לחישוב Slippage). מחיר Limit/SL/TP נשאר מעוגן ל-`order.price`/`sl`/`tp` per D-018 — ה-Delay משנה **מתי** הפקודה מתמלאת ו**איזה Tick** קובע את עלויות-הזמן, לעולם לא את רמת המחיר עצמה. `execution_delay_ms=0` (ברירת מחדל v1) משאיר את הקוד זהה-בייט להתנהגות הקודמת — כל 45 הבדיקות הקיימות עברו ללא שינוי, מוכיח רגרסיה-אפס. מוגבל לנתיב ה-Tick בלבד: ה-Fallback של 1M (SL-First/Gap-Through) אינו מושפע (משטר חסר-Ticks מטבעו; Delay תת-שניתי חסר-משמעות ברזולוציית-דקה). AT-2.8 נוסף ל-ACCEPTANCE_TESTS.md ומכוסה ב-`tests/test_at2_8_execution_delay.py`. | חוק-על #7 (Working Software Rule: Phase לא נחשב הושלם אם תוצר מוצהר ב-PHASE_PLAN חסר) + עמימות-משנה אמיתית (Entry-בלבד מול Entry+Exit) שהוצגה למשתמש ולא הוכרעה חד-צדדית | Rule | v2 בלבד (זו כעת סמנטיקה קפואה של execution_delay_ms) | תזמון ומחיר כל מילוי דרך FillSimulator כש-`execution_delay_ms>0` (ריצות עמידות בלבד); אפס השפעה בברירת המחדל |

| D-051 | **אושר במפורש ע"י המשתמש:** Phase 2 נסגר רשמית כ-Green-Conditional (דפוס זהה ל-D-036): 48/48 בדיקות (AT-2.1–AT-2.8 + Phase 0–1) ירוקות, כל שאר Quality Gates ירוקים, Demo רץ. KI-001/KI-007 (`high`) נשארים פתוחים ואינם חוסמים את הסגירה, אך הופכים לחוסמים מחייבים לפני T3.4 (Research Readiness Review, D-037). הוכרע גם: KI-006/KI-007 ייסגרו כחלק מעיצוב ה-Orchestrator הראשוני (T3.3) ולא כ-Refactor מאוחר — Phase 3 יתחיל ב-**Design Review מלא** (זיהוי מראש של כל עמימות עסקית/מחקרית/לוגית, הצגת חלופות, המתנה להכרעה) לפני כתיבת קוד כלשהי. `docs/PHASE_PLAN.md` §Phase 2 עודכן עם ה-Gate הרשמי. | המשך ישיר של דפוס D-036; מונע את התרחיש שבו KI-006/007 מתגלים כ"הפתעה" תוך-כדי מימוש T3.3 ומחייבים Refactor למודול שכבר "עבד" | Process | קבוע | סוגר רשמית את Phase 2; פותח את Phase 3 בדרישת Design-Review-לפני-קוד |

| D-052 | **אושר במפורש ע"י המשתמש, לפני כתיבת קוד Phase 3 (Design Review):** `Setup`/`setups` הופך ל-Entity מחקרי model-agnostic **בלעדי** — `outcome` מוגבל לארבעת הערכים הזהים ל-9 התיקים (`armed/expired/invalidated/no_ifvg`). כל תוצאה תלויית-תיק (`closed/blocked_news/blocked_quota/invalid_geometry`) עוברת לטבלה חדשה `setup_arm_outcomes` (מפתח `(setup_id, portfolio_id)`; `outcome` מתחיל כ-`pending` כשה-Setup המקביל מגיע ל-ARMED, מתעדכן לערך סופי פר-תיק). "Setups חמושים במקביל ≤ המכסה שנותרה" (§10) נאכף כעת **פר-תיק** ברגע ה-ARM, לא כבדיקה גלובלית — סוגר את KI-005 בפועל (לא רק fills). `db/schema.sql` עודכן (CHECK מצומצם + טבלה חדשה, נבדק שנטען נקי ב-DuckDB); `SPEC_V1_FROZEN.md` §10/§14 עודכנו בהבהרה (Journal/Data-Model בלבד, אינה משנה חוק אסטרטגיה/מכסה); `INTERFACES.md` עודכן (`Setup.outcome` מצומצם + `SetupArmOutcome` חדש); `ACCEPTANCE_TESTS.md` עודכן (AT-3.5 מורחב + AT-3.11 חדש לאכיפת מכסה פר-תיק). | 9 התיקים חייבים להיות עצמאיים לחלוטין (עקרון #4, ARCHITECTURE.md) — עמודת `outcome` יחידה על `setups` לא יכלה לייצג נכון מצב שבו תיק אחד `closed` ותיק אחר `blocked_quota` על אותו Setup בדיוק; נמצא ב-Design Review מכוון לפני כתיבת T3.1–T3.3, לא תוך-כדי מימוש | Rule + Arch | v2 בלבד (סמנטיקת outcome קפואה כעת); שינוי סכימה נוסף דורש אותו נוהל | מבנה `setups`/Journal לכל הפאזות הבאות; תוצאות דוחות Phase 4 (השוואת 9 זרועות) תלויות בהפרדה הזו |

| D-053 | **תוקן (לא מודגל, זוהה בביקורת עצמית תוך-כדי עיצוב ה-Orchestrator, לפני שהוטמע/נשלח כ"מוגמר"):** `SetupStream` (T3.2) לא אכף בפועל את חוק H1 ("סגירת R, סגירת S והכניסה — כולם בתוך החלון", SPEC §6): `_track_r_and_s`/`_track_ifvg_candidates` קיבלו R/S/Inversion חדשים ללא בדיקת `ctx.in_window()` כלל — רק ה-Guard הכללי (`step()`) היה מפקיע את ה-Setup **בדיעבד**, אחרי ש-R/S "לא-חוקיים" (שנוצרו מחוץ לחלון) כבר נכתבו ל-`setup.r_bar`/`s_bar`. תוקן: `_on_5m_close`/`_track_r_and_s` (R/S) ו-`_track_ifvg_candidates` (Inversion/ARMED) מקבלים כעת `in_window: bool` ודוחים כל היווצרות-חדשה מחוץ לחלון; Engagement ואיפוס Close-Through (לא-היווצרויות-חדשות) נשארו לא-מוגבלים כמכוון. נוסף `test_r_candle_closing_before_window_open_is_rejected` (AT-3.6). | H1 קובע איפה **מותר** לרצף לקרות, לא רק איפה **מסתיים**; בלי האכיפה המפורשת, `setup.r_bar` היה עשוי להכיל R "רפאים" שנוצר מחוץ לחלון — משפיע על מה שנרשם ל-Journal גם אם התוצאה הסופית (expired) זהה | Arch | v2 בלבד (זו תיקון תוכנה לחוק קפוא קיים, לא שינוי בחוק עצמו) | תוכן `setup.r_bar`/`s_bar` בכל Setup שנרשם ליד גבול החלון; אין השפעה על AT-3.1–3.5/3.8 הקיימים (כולם רצו בתוך החלון ממילא) |

| D-054 | **תוקן (זוהה תוך-כדי אותה ביקורת עצמית שמצאה D-053, לפני שהוטמע כ"מוגמר"):** `SetupStream` לא בדק Blackout כלל — SPEC §11 קובע במפורש "אין Setups חדשים ואין פקודות חדשות" בזמן Blackout, אך `_on_1m_close`'s Engagement-scanning פעל ללא תלות ב-Blackout. תוקן: Engagement נבדק כעת גם מול `ctx.in_blackout()` (בנוסף ל-Bias). `tests/fixtures/setup_stream.seed_store()` עודכן לחווט `CalendarEngine` ריק (ללא Blackout) כברירת מחדל, כדי שכל הבדיקות הקיימות ימשיכו לעבוד ללא שינוי; שתי בדיקות חדשות מוכיחות חסימה+חידוש. | אותו עיקרון כמו D-053 — SPEC §11 קובע חוק-על ("אין Setups חדשים"), ולא רק "אין פקודות" (שהיה כבר ברור כרמת-Orchestrator); Engagement עצמה היא תחילת Setup, ולכן חייבת להיחסם | Arch | v2 בלבד (תיקון לחוק קפוא קיים) | Setups שנוצרים בתוך חלון Blackout; אין השפעה על AT-3.1–3.8 (אף אחד לא השתמש ב-Blackout) |
| D-055 | **אושר במפורש ע"י המשתמש (שני חלקים).** (1) עמימות אמיתית שהתגלתה בפועל בעת המימוש: `SPEC_V1_FROZEN.md` §9/D-049 כתבו "Rolling/Expanding SpreadReport" כמונח מצורף מבלי להכריע ביניהם — שתי אפשרויות עם תוצאה מספרית שונה על פני ריצה של 3 שנים. הוצג למשתמש: Expanding (מצטבר מתחילת הריצה, ללא פרמטר חדש) מול Rolling (חלון-נגרר קבוע, דורש RA/Grid חדשים). **נבחר: Expanding.** (2) מומש בפועל וסוגר את KI-007: `ExpandingSpreadReport` חדש (`src/data/spread_report.py`) — `update(tick)` מצטבר לפי `hour_et`, `median_spread(hour_et)` מחזיר `KeyError` על שעה ללא תצפיות (אין ברירת-מחדל שקטה, לפי כלל "אין try/except pass על נתונים"), `warm_start(ticks)` מאפשר זריעה מנתוני-עבר לגיטימיים (למשל חלון Warm-Up בן 90 יום) בלי הפרת No-Lookahead. `Orchestrator` בנוי כעת סביב `spread_tracker: ExpandingSpreadReport` פנימי (לא מזריקים `SpreadReport` סטטי יותר); שדה הבנאי `spread_report` הוחלף ב-`spread_warmup_ticks` + `symbol`; `_apply_tick` מזין את ה-Tracker לפני שכל שאילתת `median_spread` לאותו timestamp יכולה לרוץ (Stage 1 לפני Stage 2). `SpreadReport`/`build_spread_report` המקוריים (T0.6) נשארים ללא שינוי — ייעודם דוח כיול חד-פעמי בלבד (RA-10), לא קבלת החלטות חיה; `SpreadSource` (Protocol חדש) הוא הטיפוס המשותף שכל הצרכנים (`StateStore`, `Orchestrator`) תלויים בו. נוסף AT-3.12 (`tests/test_at3_12_spread_pit.py`, 3 בדיקות: אי-תלות-בעתיד ברמת יחידה, `warm_start`, ואי-השפעת Tick עתידי על אישור-הזמנה שכבר בוצע ב-Orchestrator). כל 104 הבדיקות הקודמות עברו ללא שינוי טענה אחת (רק תיקוני חיווט Fixture/Demo/Bench מ-`spread_report=` ל-`spread_warmup_ticks=`) — מוכיח רגרסיה-אפס על נתוני Phase 2. | חוק-על #2 (עמימות ⇒ עצירה) — הבחירה בין Expanding/Rolling היא הכרעת-מחקר אמיתית, לא פרט-מימוש; חוק-על #3 (זיהוי הטיה ⇒ עצירה) — KI-007 חסם את תקפות המחקר, לא רק ניקיון-קוד | Rule + Arch | v2 בלבד (בחירת Expanding קפואה כעת; מעבר ל-Rolling דורש RA חדש + נוהל RA מלא) | `median_spread` בכל ריצה אמיתית מעתה ואילך; זהו התנאי שהיה חסר לסגירת סעיף 6 ב-Research Readiness Review |
| D-056 | **אושר במפורש ע"י המשתמש, לפני מימוש KI-016:** עמימות אמיתית שהתגלתה בפועל — בניית Orchestrator מ-Config דורשת `Portfolio.initial_equity` לכל תיק, אך ערך זה לא הוצהר בשום `config/*.yaml` או RA קיים; כל בדיקות Phase 2/3 השתמשו ב-$10,000 כערך-נוחות בלתי-מוצהר. הוצגו למשתמש: (A) שדה חדש `initial_equity_usd` ב-`parameters.yaml` + RA חדשה (כמו כל קבוע-בקטסט אחר) מול (B) שדה ב-`run_default.yaml` (רמת-Run, לא-RA). **נבחר: (A).** נוסף RA-28 ל-`RESEARCH_ASSUMPTIONS_V1.md` (‏$10,000, מתועד כאינו-משפיע על איתותים/R-multiples — Sizing הוא אחוזי מהיתרה הממומשת, RA-22). `config/parameters.yaml`/`src/config/models.py::Parameters` עודכנו (`initial_equity_usd: float`, שדה חובה). | חוק-על #2 (עמימות ⇒ עצירה) — ערך-$ שרירותי לא-מוצהר הוא בחירת-מחקר, גם אם לא משפיע על התוצאה היחסית (R) | RA | נוהל RA מלא (כמו כל RA אחר) | סקאלת ה-$ בכל דוח/Drawdown; אפס השפעה על R-multiples/איתותים |
| D-057 | **מימוש KI-016 (חיבור config/models.py ל-Orchestrator בפועל).** נוסף `src/backtest/run_builder.py::build_orchestrator(rules, parameters, run_config, *, bars_1m, bars_5m, bars_4h, ticks, news=(), spread_warmup_ticks=(), journal=None, run_id="run") -> Orchestrator` — מתרגם את שלושת מודלי הקונפיג (`RulesV1`/`Parameters`/`RunConfig`) ל-constructor args במקום אחד יחיד: בונה את כל 9 התיקים מ-`run_config.arms.entry_models × run_config.arms.sl_anchors` (או פחות, אם ה-Grid המוצהר קטן יותר), `SessionEngine`/`CalendarEngine` מ-`rules`, `RiskEngineParams`/`CostModelParams`/`sl_buffer_usd` מ-`rules`/`parameters`, ומודל Displacement (רק D1 קיים במימוש — בחירת D2-D5 מעלה `NotImplementedError` מפורש, לא נופלת חזרה בשקט ל-D1). דרך אגב, איתר ותיקן פער נוסף: `FillSimulator.in_news_checker` היה מחווט תמיד ל-Stub `lambda ts: False` בכל קריאה קיימת ל-Orchestrator (התיעוד המקורי של FillSimulator כבר סימן זאת כמכוון-לתיקון ברגע ש-Calendar יהיה קיים) — כעת מחווט ל-`calendar.in_blackout` האמיתי בכל תיק, כך שמכפיל-Slippage-חדשות (RA-11) יכול לחול בפועל על יציאות שחוצות Blackout. גם `Orchestrator.__post_init__` עצמו עודכן לחווט `model.get_setup = self.setup_stream.get_setup` באופן פנימי (במקום שכל קורא יעשה זאת ידנית אחרי הבנייה) — ניקוי זהה-משמעות, לא שינוי לוגיקה. `docs/INTERFACES.md` עודכן לשקף `run(self) -> RunResult` בפועל (סוגר את KI-017: התרגום מ-Config עבר ל-`build_orchestrator`, לא ל-`run()` עצמה — Orchestrator עצמו נשאר data/config-agnostic, D-037). מכוסה ב-`tests/test_run_builder.py` (4 בדיקות: 9 תיקים מחווטים נכון, ערכים תואמים ל-Config המוצהר, דחיית מודל Displacement לא-ממומש, וריצה מלאה מקצה-לקצה מול קובצי ה-YAML האמיתיים). כל 111 הבדיקות הקודמות עברו ללא שינוי טענה אחת. | סוגר את KI-016 (חוסם מחייב לפני T3.4, Research Readiness Review סעיף implicit); "לא באמצעות תרגום ידני" היה דרישה מפורשת של המשתמש | Arch | v2 בלבד (מבנה `build_orchestrator` קפוא-כברירת-מחדל; שינוי דורש תיעוד כאן) | כל ריצה אמיתית מעתה נבנית מ-Config מוקפא, לא מתרגום ידני; `in_news_checker` האמיתי משפיע על Slippage בפועל בכל ריצה חדשה |
| D-058 | **אושר במפורש ע"י המשתמש, לפני מימוש T3.6 (Context Snapshots):** `context_snapshots.setup_id` הוצהר עוד ב-Phase 0-kickoff, לפני D-052, ולא עודכן בעקבותיו — engagement/armed הם model-agnostic (משותפים ל-9 התיקים, בדיוק כמו `setups.outcome`), אך entry/exit הם פר-תיק במהותם (זמן/מחיר כניסה/יציאה שונים לכל אחת מ-9 הזרועות). הוצגו למשתמש: (A) `order_id TEXT REFERENCES orders(order_id)` חדש, Nullable — NULL ל-engagement/armed, מחייב ל-entry/exit, מראה עקרון D-052 בדיוק. (B) Snapshot כניסה אחד פר-מודל-כניסה (לא פר-זרוע-SL) + מפתח א-סימטרי ליציאה. **נבחר: (A).** `db/schema.sql` עודכן ונבדק שנטען נקי. | אותו עיקרון בדיוק כמו D-052 (Setup-vs-Arm) — לא זוהה בזמנו כי Context Snapshots תוכננו לפני D-052 | Arch | v2 בלבד (מבנה `context_snapshots` קפוא כעת) | `context_snapshots` לכל הפאזות הבאות; `trade_features` (T4.4) יצטרך JOIN על `order_id` ל-entry/exit |
| D-059 | **תוקן (לא-מודגל, זוהה בביקורת עצמית תוך-כדי כתיבת הבדיקה הראשונה בפרויקט שמפעילה `DuckDBJournal` אמיתי מול `Orchestrator` — כל בדיקה קודמת השתמשה ב-`journal=None`):** `setups`/`experiments`/`runs`/`portfolios` **מעולם לא נכתבו בפועל** על-ידי ה-Orchestrator, למרות ש-`orders`/`trades`/`setup_arm_outcomes`/`context_snapshots` כולן FK-תלויות בהן — כל כתיבה עם Journal אמיתי הייתה נכשלת על הפרת Foreign-Key מהאירוע הראשון. תוקן: (1) `_finalize_setup_journal` כותב שורת `setups` ברגע התוצאה הסופית המודל-אגנוסטית הראשונה של כל Setup (armed/expired/invalidated/no_ifvg — לא ניתן מוקדם יותר כי `outcome NOT NULL`); ה-Snapshot של "engagement" נאגר בזיכרון (`_pending_engagement_snapshots`) עד לאותו רגע ומשוחרר יחד אתה. (2) `_write_run_identity_rows` כותב `experiments`/`runs`/`portfolios` פעם אחת בתחילת `run()`, בערכי-placeholder כנים ומתועדים עבור שדות שה-Orchestrator הבסיסי (data/config-agnostic, D-037) אינו יודע (`config_hash`/`code_version`/`data_version`/`split_type`/`seed` — עדיין open ב-KI-018 לחיווט האמיתי). (3) `db/schema.sql`: כל `CREATE TABLE` הפך ל-`CREATE TABLE IF NOT EXISTS` — פתיחת חיבור שני לאותו קובץ DuckDB (למשל לקריאה מאוחרת ל-Viz, T3.5) הייתה נכשלת אחרת. מכוסה ב-`tests/test_context_snapshots.py` (7 בדיקות, כולל ריצה מלאה מקצה-לקצה עם Journal אמיתי). | חוק-על #3 (זיהוי הטיה/כשל ⇒ עצירה, אך זהו תיקון-שלמות חד-משמעי, לא עמימות עסקית — כמו D-053/D-054) | Arch | v2 בלבד (תיקון תוכנה לפער-שלמות; אינו משנה חוק אסטרטגיה) | כל כתיבת Journal אמיתית מעתה ואילך; לפני התיקון, אף ריצה עם Journal אמיתי לא הייתה יכולה להשלים בהצלחה |
| D-060 | **אושר במפורש ע"י המשתמש, לאחר עצירה על סתירה ארכיטקטונית אמיתית שנחשפה תוך-כדי תיקון D-059:** הניסוח המקורי של D-052 ("outcome מתחיל כ-pending... מתעדכן לערך סופי") מתאר מחזור-חיים בר-עדכון, אך `setup_arm_outcomes` הוא PK יחיד `(setup_id,portfolio_id)` לא-מגורסן, ו-CLAUDE.md אוסר UPDATE/DELETE על טבלאות מחקר ללא יוצא-מן-הכלל — כתיבת שורת "pending" בהצבת-הזמנה ואז "עדכונה" לתוצאה סופית אינה אפשרית בלי להפר append-only. הוצג למשתמש: (A) Event Log אמיתי — PK מגורסן (למשל `(setup_id,portfolio_id,seq)`), שומר "pending" כרשומת-ביניים אמיתית, פותר בנאמנות את D-052 המקורי אך דורש שינוי סכימה + עדכון כל הצרכנים העתידיים (T4.x). (B) כתיבה חד-פעמית במצב סופי בלבד — "pending" נשאר ב-CHECK כערך חוקי אך **לעולם לא נכתב בפועל**; שורה אחת בלבד לכל (setup_id,portfolio_id), ברגע שהתיק מגיע לתוצאה סופית אמיתית (`closed` ב-`_close_trade`, או `expired`/`invalidated`/`blocked_news`/`blocked_quota`/`invalid_geometry` ב-`_handle_approval`/`_cancel_for_setup`) — תואם בדיוק את הדפוס הקיים כבר ב-`orders`/`trades`. **נבחר: (B), במפורש כפתרון-ביניים תיעודי, לא סופי** — אם/כשיידרש Event Log אמיתי, זה יישאר ניסוי/Refactor מתועד עתידי (v2+), לא הרחבת-סקופ ל-Phase 3. תוקן גם באג-לוואי: `_cancel_for_setup` איטרציה מחדש על `_pending_by_setup` (לעולם לא מנוקה חלקית) הייתה עלולה לנסות לבטל/לתעד-מחדש הזמנה שכבר מולאה — נוסף Guard על `order.status == "pending"` לפני כל ביטול. נוסף KI-019: פוזיציה שעדיין פתוחה **בתום הריצה** (Overnight מותר, SPEC §11) לא מקבלת שורת `setup_arm_outcomes` כלל תחת (B) — מקרה-קצה גבולי, לא שיטתי, אינו משפיע על דטרמיניזם (אין הימור/החלטה תלוית-Journal). | חוק-על #2 (עמימות ⇒ עצירה, סתירה ארכיטקטונית אמיתית בין D-052 המקורי לעקרון append-only) — המשתמש אישר במפורש לא לוותר על append-only ולא לבצע UPDATE | Arch | v2+ (Event Log אמיתי, אם יידרש, הוא Refactor מתועד נפרד — לא v2 אוטומטי) | כתיבת `setup_arm_outcomes` בכל ריצה עתידית; דוחות Phase 4 שמניחים "כל תיק-שהוזמן-לו-פקודה מקבל שורה" חייבים לקחת בחשבון את KI-019 |
| D-061 | **T3.5 (Viz בסיסי, "דף עסקה עם כל הסימונים"):** `src/viz/trade_page.py::build_trade_page(journal, trade_id, bars_1m) -> plotly.Figure` — קורא אך ורק מה-Journal (trades/orders/portfolios/context_snapshots, כולל ה-Snapshot `armed` שנבנה ב-T3.6 עבור fvg/r_bar/s_bar/ifvg) + Bars 1M שסופקו חיצונית (לא מיומנים) לצורך נרות; לעולם לא ניגש למצב-מנוע חי. מציג: נרות 1M, אזור FVG-4H, אזור iFVG, הדגשת נרות R/S, קווי entry/SL/TP, סמני כניסה/יציאה, כותרת עם מודל-כניסה/זרוע-SL/תוצאה-ב-R. נוסף חוזה import-linter חדש ("Viz has no engine dependencies") — `src.viz` אסור לייבא ישירות מ-`entry/risk/execution/structure/fvg/displacement/store/backtest`, תואם את ה-comment שכבר היה קיים מראש ב-`pyproject.toml` (§Phase-לא-עדיין: "{features, ai, viz, stats, validation, scoring} forbidden from importing any engine module"). תלות חדשה `pytz` נוספה (`duckdb`'s Python client דורש אותה כדי להחזיר עמודות TIMESTAMPTZ בפועל — התגלה תוך-כדי כתיבת הבדיקה הראשונה שקוראת TIMESTAMPTZ מה-Journal; ללא Feature/Rule השפעה, תלות תשתית בלבד). מכוסה ב-`tests/test_trade_page.py` (2 בדיקות) + `scripts/demo_phase3.py::demo_context_snapshots_and_viz` (כותב `data/demo/phase3_trade_page.html` הניתן לפתיחה בדפדפן — Working Software Rule). | חוק-על #7 (Working Software Rule); T3.5 מוצהר מפורשות ב-PHASE_PLAN.md | Arch | v2 בלבד | תשתית Viz לכל הפאזות הבאות; אין השפעה על מנוע/איתותים (Viz הוא read-only לחלוטין) |
| D-062 | **Critical Review סופי של Phase 3 (ר' בקשת המשתמש לביקורת עצמאית מקיפה לפני החלטת סגירת Phase). שני ממצאים נוספים נמצאו ותוקנו במהלך אימות בפועל (לא ביקורת-קוד בלבד):** (1) `RunResult.fills`/`RunResult.orders_cancelled` הוצהרו כשדות אך **מעולם לא עודכנו בשום מקום ב-`orchestrator.py`** — כל ריצה, גם מוצלחת עם מילויים אמיתיים, הייתה מדווחת `fills=0`/`orders_cancelled=0` באופן שקט ומטעה. תוקן: `result` הועבר כפרמטר דרך `_apply_tick`→`_on_fill` ו-`_cancel_all_pending`→`_cancel_for_setup`; `result.fills` סופר כל Fill (כניסה+יציאה), `result.orders_cancelled` סופר כל ביטול פקודה-ממתינה בפועל. (2) נבנה תרחיש-אימות מלא (לא רק ביקורת) של 9 התיקים יחד עם Ticks אמיתיים המספיקים למילוי ולמימוש שלושת מודלי הכניסה בו-זמנית (M1 Limit, M2 Market, M4 נר-דחייה) — מעולם לא נבדק קודם (KI-014/KI-015). התוצאה: כל שלושת המודלים מתמלאים בהצלחה, כל אחד בזרוע נפרדת, ושש הזרועות האחרות (R_body/S_body, שנכשלות בגאומטריה בתרחיש הזה) נשארות לחלוטין לא-מושפעות — בידוד 9 התיקים מוכח בפועל. נוסף `tests/test_full_pipeline_9_arms.py` (3 בדיקות). | חוק-על #3 (זיהוי הטיה/כשל ⇒ עצירה, אך תיקון-שלמות חד-משמעי, לא עמימות עסקית) + דרישת המשתמש לביקורת עצמאית שמאמתת בפועל, לא רק קוראת קוד | Arch | v2 בלבד (תיקון-שלמות; אינו משנה חוק אסטרטגיה) | `RunResult` מדויק מעתה ואילך לכל קורא (demo/bench/דוחות עתידיים); סוגר את KI-014 וחלק משמעותי מ-KI-015 |
| D-063 | **תוקן (חמור, זוהה תוך-כדי Critical Review סופי של Phase 3, לפני שדווח כ"בדוק"):** `_ENTRY_ORDER = {"bar4h": 0, "bar5m": 1, "bar1m": 2, "tick": 3}` ב-`orchestrator.py` היה **הפוך** מהחוק המוצהר במפורש (תיעוד המודול עצמו + CLAUDE.md: "עיבוד דו-שלבי... סדר 1M→5M→4H") — מספר-עדיפות נמוך יותר ממוין ראשון ב-`events.sort`, כך שבפועל 4H עבר לפני 1M/5M בכל התנגשות-חותמת-זמן, ההפך הגמור מ-H2/Race-09:00 שהמנגנון הזה נועד לפתור. משמעות מעשית: בכל סגירת 4H (17/21/01/05/09/13 ET — פעם ב-4 שעות, כל יום, לא מקרה-קצה נדיר) 5M/1M היו רואים Bias/FVG "טרי" מה-4H שנסגר **באותה חותמת-זמן בדיוק**, במקום המצב הקודם לה כפי שה-SPEC דורש. הפער היה בלתי-נראה כי אף בדיקת Orchestrator קיימת ב-Phase 3 לא הזינה `bars_4h` לא-ריק (כולן "מזריעות" FVG/Bias ישירות דרך `store.put`, עוקפות את `_apply_4h` לגמרי). נמצא גם `_TF_PRIORITY` — Dict שני, לא-בשימוש, עם הערכים **הנכונים** — ראייה חזקה שמדובר בבלבול בין שתי גרסאות ולא בכוונת-מכוון. תוקן: `_ENTRY_ORDER` תוקן ל-`{"bar1m":0,"bar5m":1,"bar4h":2,"tick":3}`; ה-Dict הכפול/המת נמחק. נוסף `tests/test_h2_merge_order.py` הבודק ישירות את `_merged_timeline()` על התנגשות-חותמת-זמן אמיתית — אומת שהבדיקה אכן הייתה נכשלת מול הקוד הישן (הרצה ידנית עם ההיפוך). | חוק-על #3 (זיהוי הטיה/כשל ⇒ עצירה, אך תיקון-שלמות חד-משמעי מול חוק כבר-מוצהר, לא עמימות חדשה) | Arch | v2 בלבד (תיקון-באג לחוק H2 קיים, לא שינוי בחוק) | סדר עדכון ה-State האמיתי בכל ריצה שכוללת 4H+1M/5M על אותה חותמת-זמן; ייתכן שינוי-תוצאות אילו נבדק על דאטה אמיתי בעבר (לא נבדק — KI-001) |
| D-064 | **סוגר את KI-020, לפי הנחיית המשתמש המפורשת:** נבנה תרחיש-אימות אחד המפעיל את הצינור המלא — 4H גולמי → `StructureEngine` → `FVGEngine` → Bias → SetupStream → מודל-כניסה → RiskEngine → FillSimulator → Journal — **ללא שום `store.put` בשום שלב**. רצף ה-4H (`tests/test_full_pipeline_from_raw_4h_bars.py::_raw_4h_bars`) משלב שני מקטעים מוכחים בנפרד בעבר (AT-1.3: BOS→Bias, AT-1.4: זיהוי-FVG תלת-נרות) לרצף אחד רציף, מחובר לראשונה. FVG-4H-bull אמיתי [128,116] מזוהה ומאושר על ידי `FVGEngine` בפועל, ה-Bias הופך ל-bullish דרך `StructureEngine` בפועל; רצף 1M/5M (זהה בצורתו לתבנית שהוכחה שוב-ושוב במהלך הביקורת, מוזז לטווח החדש) מפעיל Engagement→R→S→iFVG→Armed→M2→מילוי→TP. שלוש בדיקות: (1) Bias/FVG אמיתיים לא-מוזרעים, (2) הפייפליין המלא מגיע למילוי אמיתי, (3) שורות Journal מלאות ועקביות (`setups`/`context_snapshots`/`trades`) נבדקות ישירות. **לא נדרש שום שינוי ארכיטקטוני** — התרחיש עבד בהצלחה בניסיון הראשון (אחרי אימות-מקדים בסקריפט-טיוטה נפרד, לא בקובץ הבדיקה עצמו) — מוכיח שהחיווט האמיתי של Phase 3 תקין. כל 127 הבדיקות עדיין ירוקות; אין רגרסיה; אין ממצא חדש. | תנאי מפורש שהמשתמש קבע לפני אישור סגירת Phase 3: "אם הבדיקה הזו עוברת... כל הבדיקות עדיין ירוקות, אין באגים חדשים, KI-020 נסגר" | Arch | v2 בלבד (בדיקת-רגרסיה קבועה מעתה) | מוכיח בפועל, לא רק בהנחה, שהחיווט המלא של Orchestrator תקין; מסיר את החסם הקודי האחרון לפני שקילת סגירת Phase 3 |
| D-065 | **אושר במפורש ע"י המשתמש:** הצד הקודי של Phase 3 (T3.1–T3.3, T3.5, T3.6) נסגר רשמית כ-Green-Conditional, בדיוק בדפוס D-036/D-051. התנאי המדויק שהמשתמש קבע מראש התקיים במלואו: תרחיש-4H-גולמי עבר (D-064), כל 127 הבדיקות ירוקות, אפס באגים חדשים, KI-020 סגור. `docs/PHASE_PLAN.md` §Phase 3 עודכן עם שני שערים נפרדים: שער-קודי (סגור) ושער-מלא (עדיין פתוח, חסום ע"י Research Readiness Review בלבד — KI-001/KI-010, חסימות-סביבה לא-קודיות). KI-005 סומן `closed` בדיעבד (התיקון כבר היה קיים מ-T3.2/T3.3, רק לא סומן). | המשתמש קבע מראש את תנאי-הסגירה המדויקים לפני שהעבודה בוצעה, ואישר אחריהם במפורש | Process | לפני T3.4 — נדרשת Research Readiness Review = GO בנפרד (לא באחריות דוח זה) | סוגר את Phase 3 מבחינת עבודת הפיתוח; T3.4/Phase 4 ואילך ממתינים לגישת-דאטה אמיתית בלבד |
| D-066 | **מאושר (Roy).** אימוץ `WORK_ORDER_PROTOCOL.md` v1.0: כל Blocker (מ-B-1 ואילך, כולל Stage B/C/D וכל עבודה עתידית) מבוצע תחת פרוטוקול מחייב — Pre-Flight Review עם תוצר `PREFLIGHT_<Blocker>.md` (הנחות מאומתות / לא-ניתנות-לאימות / סטיות / הכרעה) ועצירה על סתירה מהותית לפני כל שינוי; דו"ח סיום אחיד הכולל סעיף Lessons Learned עם חובת עדכון מסמכים או Follow-up מפורש לכל ממצא; **Definition of Done אחיד בן תשעה סעיפים (§4)** — הכרזת "Closed" מותרת רק כשכולם מסומנים, אחרת "Partially Closed" עם פירוט החסר; ו**מדד פרויקט קצר בכל סיום Blocker** (§3.11). הפרוטוקול **FROZEN** עם אימוצו; תנאי פתיחה מחדש בלעדיים: (1) כשל אמיתי בפרוטוקול שהתגלה במהלך העבודה; (2) מניעת ביצוע נכון או תקורה בלתי-מוצדקת; (3) החלטה ארכיטקטונית חדשה שאושרה ומחייבת התאמה. אין הוספת שכבות תהליך ללא אישור מפורש של Roy. **השלכה נוספת (B-1, C3):** `.gitignore` קיבל חריג ממוקד `!data/registry/.gitkeep` כדי לשמר את מבנה תיקיית ה-Registry הזמנית (עד T5.5) בריפו, בעוד `/data/` נשארת מוחרגת ו-`runs.jsonl` עצמו נשאר untracked. | סבבי הביקורת לפני Phase 4 הוכיחו ששכבת התיעוד יכולה לסטות מהקוד (D-063) ומעצמה (שגיאות במסמך ה-Audit הקפוא), ושמעקבי-המשך מובטחים נשכחים (KI-003, סגירות D-039). הפרוטוקול הופך את האימות והלמידה לחלק מובנה מכל Blocker במקום מאמץ ביקורת בדיעבד. ההקפאה מבטיחה שהאנרגיה מופנית מעתה לפיתוח המערכת ולא לשיפור מתמיד של התהליך. | Process | (1) כשל אמיתי בפרוטוקול שהתגלה במהלך העבודה; (2) מניעת ביצוע נכון או תקורה בלתי-מוצדקת; (3) החלטה ארכיטקטונית חדשה שאושרה ומחייבת התאמה — בלעדיים, אין הוספת שכבות ללא אישור Roy | תקורה קטנה וקבועה לכל Blocker; שיפור עקביות ארוך-טווח; נראות מצב-פרויקט שוטפת ללא מעבר על כל המסמכים |
| D-067 | **מאושר במפורש ע"י Roy, לאחר Decision Proposal (`PREFLIGHT_B1.md` §ד, ממצא C1).** `db/schema.sql`: `runs.seed BIGINT NOT NULL` → `runs.seed BIGINT` (Nullable). שינוי יחיד — עמודה אחת, טבלה אחת (`runs`); אפס שינויים נוספים בסכימה. **אין מיגרציה:** אומת ש-`*.duckdb` כלל אינו עוקב git (`.gitignore:4`) ו-`git ls-files \| grep duckdb` מחזיר רק את קוד המקור (`src/journal/duckdb_writer.py`) — לא קיים יומן DuckDB פרסיסטנטי בריפו טרם T3.4, ולכן אין שורות קיימות שדורשות תיקון בדיעבד. **אינווריאנט מחליף** (סוגר את הסתירה שנחשפה ב-Pre-Flight B-1): כל אקראיות עתידית חייבת לצרוך את `runs.seed`; ריצות מנוע (נטול-RNG, D-037): `seed IS NULL` — נאכף ב-AT-3.14 (B-1, Commit 4); ריצות Baseline (`Validation.random_baseline`, Phase 5): `seed IS NOT NULL` — ייאכף בקוד ב-Phase 5, לא ב-B-1 ולא בסכימה עצמה. | ה-Placeholder הקודם (D-059/KI-018: `seed=0`) הוא שקר-מסוג-KI-018 בדיוק כמו "unknown" היה — `0` הוא ערך Seed חוקי לחלוטין (למשל ל-`random_baseline`), ולכן אינו יכול לשמש בו-זמנית גם כערך אמיתי-אפשרי וגם כ"סימן שאין Seed רלוונטי". `NULL` הוא הדרך היחידה לבטא "אין Seed" מבלי להתנגש עם טווח-הערכים החוקי של Seed אמיתי. | Arch | הכנסת RNG כלשהו למנוע (ואז `seed` יידרש NOT NULL גם בריצות מנוע, לא רק ב-Baseline) | אפס שינוי בהתנהגות ריצות-עבר (לא קיימות); `run_builder` (B-1, Commit 3) יכתוב `seed=NULL` בפועל לריצות מנוע; `AT-3.14` (B-1, Commit 4) אוכף `seed IS NULL`; Phase 5 יאכוף `seed IS NOT NULL` לריצות Baseline בקוד |
| D-069 | **מאושר (Roy) — Stage A / B-2, סוגר את KI-015.** שני הפערים האחרונים שנשארו פתוחים ב-KI-015 (אחרי D-062) נבדקו בפועל, לא בהנחה: (1) **דטרמיניזם דו-ריצתי, תרחיש-ריבוי מלא** — `tests/test_at3_15_multiarm_determinism.py` (AT-3.15) מייבא (לא משכפל) את `_scenario` מ-`tests/test_full_pipeline_9_arms.py` (D-062: 3 זרועות ממלאות/6 לא) ומריץ אותו פעמיים-מאפס עם `Journal` מצורף post-construction (`orch.journal = journal`, אותו דפוס כמו `test_context_snapshots.py`/`test_trade_page.py`) — ייצוא קנוני זהה (SHA256, עותק-מקומי-עצמאי של פונקציית הייצוא, לא ייבוא מ-`test_at3_14_determinism.py`, לפי כלל היציבות ב-WORK_ORDER_B2.md §1.5). אומת מראש (לפני כתיבת הבדיקה) ש-`RiskEngine._order_seq`/`SetupStream._seq` הם `default_factory` פר-מופע, לא גלובליים — תנאי-מקדים הכרחי לכך שהשוואת-Hash תהיה משמעותית. **ירוק בניסיון הראשון, ללא ממצא.** (2) **אינטראקציית מכסה, >2 Setups ל-ARMED על אותו תיק/יום** — `tests/test_at3_16_quota_arm_interaction.py` (AT-3.16): פיקסצ'ר עם 3 אזורי-FVG נפרדים (מחיר **יורד** לאורך הזמן — נדרש כדי לא להפעיל בטעות מיטיגציה-מלאה על אזורים שטרם הגיעו, ר' `src/fvg/mitigation.py`; ניסיון ראשון עם מחיר עולה נכשל בדיוק מהסיבה הזו — כל 3 האזורים "נגעו" ומוטגו על-ידי הבר הראשון). **הבדיקה הראשונה (לפני כל תיקון) חשפה כשל אמיתי, לא ארטיפקט:** `M4EntryModel._watching` (`src/entry/m4.py`) אף פעם לא מנוקה כשSetup מתבטל אחרי-ARM (S-low-break, `post_arm=True`), כי `Orchestrator.run()` (`src/backtest/orchestrator.py`) קרא ל-`model.on_event()` רק על הסתעפות ה-`armed`, לא על `invalidated/expired/no_ifvg` — למרות שה-docstring של `M4EntryModel.on_event` כבר הבטיח "stop watching on any terminal/post-arm-cancel event". Setup-id דלוק נשאר ב-`_watching`, ומאוחר יותר (על נר-דחייה כשיר של Setup **אחר** לגמרי) M4 ניסה ליצור Intent עבורו → `SetupStream.get_setup()` → `KeyError` לא-מטופל, קורס את כל הריצה. **תוקן (אישור Roy, "Option A", היקף מינימלי):** שורה פונקציונלית אחת ב-`Orchestrator.run()` — `self._open_orders_for_event(event, ctx, result)` גם על הסתעפות `invalidated/expired/no_ifvg` (בדיוק אותה קריאה הקיימת כבר על `armed`); לא נוסף מנגנון חדש, לא צומד Orchestrator ל-M4 ישירות — משתמש ב-`on_event()` הקיים בדיוק כפי שתוכנן. אומת מראש ש-M1/M2 `on_event` מחזירים `None` ללא-תנאי על אירועים לא-armed (נקרא הקוד המלא של שניהם) — כך שהקריאה החדשה היא ניקוי-בלבד עבורם, לא סיכון ליצירת הזמנה שלא-מכוונת. אומת ש-`git stash` על השינוי לבדו משחזר את אותה קריסה בדיוק (`KeyError('SETUP-1')`) לפני שהוחזר. תיקון-לוואי, לא-קשור, בפיקסצ'ר הבדיקה עצמו: נחשף צורך ב-Tick-יציאה נקי (TP) לכל אזור שנסגר לפני שהאזור הבא מתחיל, אחרת פוזיציה פתוחה מאזור קודם נחשפת ל-Tick של האזור הבא (מחיר שונה בהכרח) כ-Gap-Through SL/TP על סף-גיאומטריה זעיר — לא קשור ל-M4, תוקן בפיקסצ'ר בלבד. | KI-015 (הבדיקה שהתגלתה ב-Critical Review לפני Phase 4) דרשה הוכחה בפועל, לא הנחה, לדטרמיניזם-רב-זרועות ולאינטראקציית-מכסה — בדיוק כפי ש-`PREFLIGHT_B2.md` v2 קבע כ-Scope היחיד. הכלל "כותבים את הבדיקה לפני שינוי קוד" (WORK_ORDER_B2.md §1.2) הוא זה שחשף את כשל M4 בפועל, לא ביקורת-קוד תיאורטית. | Arch | הכנסת מודל-כניסה רביעי או שינוי מהותי ב-`SetupStream`'s event lifecycle יידרש בדיקה חוזרת של אותה הנחה (כל EntryModel.on_event נשאר no-op/ניקוי-בלבד על אירועים לא-armed) | `Orchestrator.run()`'s event-dispatch loop קורא כעת ל-`on_event` על כל אירוע, לא רק armed — כל מודל-כניסה עתידי חייב לשמור על ההנחה הזו; KI-015 סגור במלואו; אפס שינוי סכימה/ל-Entry Models עצמם |
| D-068 | **מאושר (Roy) — Stage A / B-1, סוגר את KI-018.** חיווט זהות ריצה ורישום ריצות מינימלי: `config_hash` מחושב אוטומטית ב-`run_builder` מ-`(rules, parameters, run_config, identity.data_version, code_version)` דרך `config_hash()` הקיימת (KI-016) — הקורא אינו מספק אותו; `identity.config_hash != None` ⇒ `ValueError`. `code_version` מזוהה מ-git (`detect_code_version`, `src/backtest/run_builder.py`): `<sha>` או `<sha>+dirty` (`git status --porcelain` לא ריק); כשל git ⇒ `RuntimeError` קולני; override מפורש קבוע מותר לטסטים בלבד (`tests/fixtures/orchestrator.py::TEST_CODE_VERSION`). `data_version` באמצעות helper אחוד חדש (`src/data/versioning.py`): `data_version_for_files`/`data_version_for_ticks` — האחרון משמר D-037 (גם ריצת fixture/סינתטית מקבלת `data_version` אמיתי, לא Placeholder). `split_type` מאוצר ערכים סגור (`RunIdentity.split_type`, `src/core/types.py`) כולל `"fixture"`. `seed=NULL` לריצות מנוע (D-067: שינוי הסכימה שאיפשר זאת) + אינווריאנט "מנוע נטול-RNG; כל אקראיות עתידית תצרוך את `runs.seed`". רישום append-only של כל ריצה (כש-`journal is not None`) ב-`data/registry/runs.jsonl` עד שיוחלף ב-Experiment Tracker (T5.5). `RunIdentity` (`src/core/types.py`, D-038 dependency-free) נוסף ל-`build_orchestrator(..., identity: RunIdentity, registry_path: Path \| None = None)` ותועד ב-`INTERFACES.md`. **מיקום החיווט נשאר `run_builder` בלבד** — `Orchestrator` קיבל חמישה שדות פשוטים (`config_hash`/`code_version`/`data_version`/`split_type`/`seed`, ברירת מחדל "unknown"/`None`) שהוא רק מחזיק ומשתמש בהם ב-`_write_run_identity_rows`, אך לעולם לא מחשב אותם (D-037 נשמר במלואו) — אותו דפוס בדיוק כמו `run_id`/`symbol`/`journal` הקיימים. קריאות `Orchestrator(...)` ישירות שעוקפות `build_orchestrator` (`tests/fixtures/orchestrator.py::make_orchestrator`, בשימוש ב-`test_context_snapshots.py`/`test_trade_page.py`) ממשיכות לקבל את ה-Placeholder הקודם — מחוץ להיקף B-1 שנעול ל-`run_builder` (WORK_ORDER_B1.md §2). מאומת ב-AT-3.14: שתי ריצות זהות-מאפס על ה-fixture המלא של D-064 מפיקות ייצוא קנוני זהה (SHA256) של 18 טבלאות ה-Journal; שורת `runs` ללא "unknown" באף שדה; `config_hash` שווה לחישוב עצמאי חוזר, לא רק "לא unknown"; `seed IS NULL`; רשומת Registry קיימת ותקינה. | SPEC §13 ("כל ריצה = `config_hash`, `data_version`, `code_version`, רישום Append-Only") ו-CLAUDE.md §דטרמיניזם מחייבים זהות ריצה אמיתית לפני T3.4/Phase 4; ה-Placeholder-ים "unknown"/`0` (D-059/KI-018) הפכו את היומן לבלתי-ניתן-לשחזור עקרונית — לא ניתן לאמת "אותו קונפיג ⇒ אותו יומן" בלי זהות אמיתית להשוואה. | Arch | T5.5 מחליף את ה-Registry הזמני; הוספת RNG כלשהו למנוע (ואז גם `seed` בריצות מנוע יידרש NOT NULL, ר' D-067) | שינוי חתימת `build_orchestrator` (מתועד ב-INTERFACES.md, כל אתרי הקריאה בטסטים עודכנו); טסטים קיימים עודכנו מציפיית "unknown" לערכים אמיתיים; אכיפת "SHA נקי" (לא `+dirty`) לריצות מחקר אמיתיות תיעשה בשער RRR/T3.4, לא כאן; `.gitignore` שוחזר ל-`/data/*` + חריגים ממוקדים כדי לשמר `data/registry/.gitkeep` בריפו (C3) |
| D-069 | **מאושר (Roy) — סוגר את KI-001.** Root cause זוהה ואומת באופן דטרמיניסטי: חסימת ה-429 המתמשכת מול `datafeed.dukascopy.com` נבעה מ-**client fingerprint**, לא מ-IP ולא מנפח-בקשות — `_default_transport` (`src/data/dukascopy_downloader.py`, ללא שינוי) שולח בקשת `urllib.request.urlopen` גולמית: `User-Agent: Python-urllib/X.Y`, ללא `Accept`/`Accept-Language`, `Accept-Encoding: identity`, ו-ALPN מוגבל ל-`http/1.1` בלבד (מאומת ישירות מקוד ה-stdlib, `http/client.py`). אומת בניסוי מבודד ומבוקר (`scripts/diagnostics/test_h1_fingerprint.py`): אותו URL בדיוק שנכשל 12/12 (100% 429) עם `urllib` הצליח מיידית עם fingerprint דמוי-דפדפן (`httpx`, HTTP/2, headers מלאים), **מאותו IP, אותה רשת, אותו מחשב** — משתנה יחיד שהוחלף. תוקן ב-Production באמצעות **Option B**: `src/data/browser_transport.py` (`BrowserLikeTransport`, client מתמשך) מוזרק דרך פרמטר ה-constructor הקיים של `DukascopyDownloader` (`transport: Transport = _default_transport`) — **`src/data/dukascopy_downloader.py` לא נערך כלל**, החוזה (`Transport = Callable[[str], bytes]`) נשמר במלואו (מאומת ב-`tests/test_browser_transport.py`, כולל בדיקת תאימות-חוזה מפורשת). אומת End-to-End: ריצת `backfill_full_range.py` אמיתית על חודש שלם (נובמבר 2022, 720 שעות) מהמחשב הביתי — `months_succeeded=1 months_failed=0 transport_calls=720 429s=0 errors=0`, 3,641,776 ticks, Parquet נכתב ואומת בהצלחה ע"י `validate_full_range.py`. | KI-001 חסם הורדת 3 שנות Ticks אמיתיים, דו"ח ספרד אמיתי לכיול RA-10, והרצת AT-0.1/AT-0.2/AT-0.7 מול דאטה אמיתי — נדרשה חקירה שיטתית מבוססת-ראיות (בדיקות מדורגות מ-Claude Code להרצה ביתית, ניסוי-ביקורת דפדפן-מול-סקריפט על אותו URL/IP, ניתוח קוד-מקור stdlib) לפני כל שינוי, כדי לוודא תיקון שורש ולא סימפטום. | Arch | אם ריצת ה-backfill המלאה (39 חודשים) תפגין כישלון חוזר — לפתוח KI חדש, לא לערוך רטרואקטיבית רשומה זו | `scripts/backfill_full_range.py` משתמש כעת ב-`BrowserLikeTransport` במקום `_default_transport`; `pyproject.toml`/`uv.lock` כוללים `httpx`/`h2` כתלויות ייצור חדשות; `scripts/diagnostics/` (3 קבצים) נשמרים כתיעוד היסטורי של החקירה |
| D-070 | **מאושר (Roy) — סוגר את KI-002.** `point_value=0.001` (סקאלת XAUUSD, `src/data/dukascopy_downloader.py:XAUUSD_POINT_VALUE`) אומת מול דאטה אמיתי לראשונה (חודש נובמבר 2022 מלא, 3,641,776 ticks): טווח מחירים שפוענח — bid 1616.477–1786.367, ask 1616.912–1786.783 — תואם היסטורית באופן מדויק, כולל **התאמה כמעט-לדולר** ל-high של 15.11.2022 המתועד בעיתונות פיננסית (~$1,780–1,786, "3-month high" בעקבות אירוע-הטיל בפולין) וללרמות-הפתיחה הידועות של תחילת נובמבר (~$1,620) לפני ראלי ה-CPI (10.11.2022). | KI-002 היה תלוי לחלוטין ב-KI-001 (לא ניתן לאמת סקאלת-המרה בלי דאטה אמיתי); ההסתמכות הקודמת הייתה על מוסכמה ציבורית לא-מאומתת מכלי Dukascopy קוד-פתוח. | RA | אם מטבע/מכשיר נוסף (לא XAUUSD) ייבדק בעתיד — נדרש אימות `point_value` נפרד, לא בהכרח אותו ערך | אין שינוי קוד — `XAUUSD_POINT_VALUE=0.001` נשאר כפי שהיה, כעת עם ביסוס אמפירי מלא במקום הנחה בלבד |
| D-071 | **מאושר (Roy).** במהלך אימות KI-001/KI-002 מול דאטה אמיתי (נובמבר 2022) התגלה: `Validator` (`src/data/validator.py`, ללא שינוי) מייצר 18 flagged gaps ו-2,998 flagged spikes בחודש בודד. ניתוח מבוסס-ראיות (מחקר-רשת ממקורות ממשיים + הצלבה עם `build_spread_report` + חישוב-לוח עצמאי): 17/18 ה-gaps תואמים בדיוק לחלון rollover יומי מתועד (17:00 ET, לפני/אחרי מעבר DST ב-6.11.2022 בהתאמה מלאה), מאושש גם ע"י היעדרות שעה 17 ET לחלוטין מדוח הספרד ו-widening של spread (p95 כפול) בשעות הסמוכות; ה-gap ה-18 (24.11, Thanksgiving) מאומת בלוח ומוסבר באותו מנגנון עם התחלה-מוקדמת. ה-spikes מעורבים: ריכוז בשעות 13:00-15:00 UTC תואם שעת-פרסום נתוני-מאקרו אמריקאית סטנדרטית (8:30 ET), וימי-השיא תואמים אירועי-שוק אמיתיים ומתועדים (CPI ב-10.11, אירוע-הטיל בפולין ב-15.11) — אך גודל-move חציוני של spikes מדוגללים (0.2305) קטן מהספרד הטיפוסי (~0.40–0.45), מרמז על רגישות-יתר אפשרית של ה-baseline הרוליג (500 ticks) שמעולם לא נבדק מול דאטה רב-session אמיתי, רק Fixtures סינתטיים. **Lesson Learned:** `Validator` בצורתו הנוכחית אינו מבחין בין שלוש קטגוריות סיבתיות שונות מהותית — **(א) Data corruption, (ב) Market behavior טבעי (rollover/חג/אירוע-חדשות), (ג) Calibration sensitivity** של ה-Validator עצמו — ומדגיל את כולן לאותה דגל בודדת (`flagged_gap`/`Spike`), ללא הבחנה. **הוחלט:** לא לשנות threshold/window/לוגיקת-סיווג כעת ללא ניסוי-כיול מבוקר. נפתח **KI-022** (open, `docs/KNOWN_ISSUES.md`) לתיעוד הצורך. | שינוי סף/חלון ללא ניסוי מבוקר עלול לפתור symptom (ספירה גבוהה) תוך פגיעה ביכולת-הגילוי האמיתית (למשל אם threshold יועלה סתם, ייתכן שיפספס תנודתיות אמיתית עתידית) — נדרש Work Order ייעודי לכיול, לא שינוי אד-הוק תוך-כדי ניתוח. | Process | כאשר Work Order ייעודי לכיול ה-Validator ייפתח ויאושר ע"י Roy | אין שינוי קוד; `KI-022` נפתח למעקב בלבד |
| D-072 | **מאושר (Roy) — Stage A / B-3, Real-Data Diagnostic Validation Gate (Commit 1).** `build_orchestrator`/`Orchestrator.run()` (שניהם ללא שינוי) הופעלו לראשונה אי-פעם מול דאטה אמיתי — נובמבר 2022 המלא (3,641,776 Ticks אמיתיים, D-069/D-070), תיק בודד M2×S_body, לוח-חדשות ריק (Non-Goal מוצהר). **תוצאה: הריצה הושלמה עד הסוף ללא Exception, ללא Crash, `exit code 0`.** מונים: `engaged=3312 reaction_seen=4 sweep_confirmed=1 armed=1 expired=3309 invalidated=2 no_ifvg=0 orders_placed=1 orders_rejected=0 fills=2 orders_cancelled=0`. **לא נמצא כשל — B-3 Commit 2 (STOP→תיקון) לא בוצע, כי לא נדרש.** דאטה הנובמבר-2022 סופק ישירות ע"י Roy (חלופה B, WORK_ORDER_B3.md §4 Commit 0 — Data Availability Gate בלבד, לא ניסיון-רשת, לא נגיעה ב-Downloader/KI-001), הועתק ל-`data/ticks/XAUUSD/2022/11.parquet` (gitignored, לא ב-git) ואומת מול D-069/D-070 (row_count/גודל-קובץ/טווחי-מחיר תואמים בדיוק) לפני ההרצה. **Non-Goals מוצהרים (חלים על ממצא זה במלואם):** (א) KI-010 אינו מאומת — Blackout/`news_cross` לא הופעלו כלל, לוח-חדשות ריק בכוונה. (ב) KI-022 אינו נסגר — `Validator` לא הופעל בסקריפט זה כלל, אין קשר. (ג) **אין מסקנה מחקרית מהמונים** — לא Win Rate, לא תוצאת ה-Trade היחיד שבוצע, לא פרשנות לכמות ה-`armed` הנמוכה. (ד) **B-3 אינו סוגר RRR ואינו מהווה T3.4** — ר' טבלת RRR מעודכנת ב-`WORK_ORDER_B3_CLOSURE.md`, נשארת NO-GO על אותם 3 סעיפים כפי שהייתה. | המנוע (Structure/FVG/iFVG/SetupStream/Orchestrator) עד כה אומת אך ורק מול Fixtures סינתטיים בכל Gate קודם (Phase 0–3, B-1, B-2) — מעולם לא נגע בדאטה אמיתי. זהו סיכון ארכיטקטוני מאותו סוג שכבר התממש פעמיים (KI-015: באג M4 שנחשף רק בתרחיש-ריבוי מורכב; KI-022: רגישות-כיול Validator שנחשפה רק בחשיפה ראשונה לדאטה אמיתי) — B-3 נועד לחשוף סיכון כזה מוקדם ובזול, לפני שהוא נחשף בתוך T3.4 המוגן. | Process | אם/כש-RRR ייסגר (KI-010 + כיול RA-10 + 3 שנות דאטה) ו-T3.4 יורשה להתחיל — ריצה זו **אינה** תחליף לכך ואינה נספרת כחלקה | אין שינוי קוד, אין שינוי Schema; `scripts/diagnostics/run_b3_real_data_diagnostic.py` נשמר כתיעוד היסטורי של הריצה (אותו דפוס כמו `scripts/diagnostics/analyze_spikes.py`, KI-001) |

---

## טמפלט לרשומה חדשה
```
| D-0XX | <ההחלטה בתמצית> | <נימוק> | Rule/RA/Arch/Process | <מתי מותר לפתוח> | <מה משתנה אם נשנה> |
```
Refactor למודול מאושר: לציין את המודול, הסיבה, ואישור שכל ה-AT שלו עברו מחדש.
